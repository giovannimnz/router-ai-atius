---
phase: 21-feat-pt-native-pr
plan: "04"
type: execute
wave: 3
depends_on:
  - 21-03
files_modified:
  - web/classic/src/i18n/i18n.js
  - web/classic/src/i18n/language.js
  - web/classic/src/i18n/locales/pt.json
  - web/classic/src/components/layout/headerbar/LanguageSelector.jsx
  - web/classic/src/components/settings/personal/cards/PreferencesSettings.jsx
autonomous: true
requirements:
  - PHASE-21-UPSTREAM-NATIVE-I18N
  - PHASE-21-REUSE-EXISTING-TRANSLATIONS
  - PHASE-21-PT-BR-COVERAGE
  - PHASE-21-LOCAL-FIRST-VALIDATION
must_haves:
  truths:
    - Classic frontend has an independent i18next registry and hardcoded language controls.
    - Classic PT must reuse matching default PT strings where source text is identical.
    - Most classic keys require classic-specific translation/review because no complete classic PT source exists.
  artifacts:
    - `web/classic/src/i18n/locales/pt.json`
  key_links:
    - classic supportedLanguages -> i18n resources -> header/profile language controls
    - classic en.json -> classic pt.json -> build/i18n checks
---

<objective>
Add Portuguese to the classic frontend through its native i18next setup, reusing matching default PT translations and translating classic-specific gaps.
</objective>

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-07/D-08 | Classic frontend native PT | 21-04 | COVERED | Adds classic registry, language list, selectors, and locale. |
| CONTEXT | D-14 | Reuse default PT for matching classic keys | 21-04 | COVERED | Matching source text reuse comes first. |
| REQUIREMENTS | PHASE-21-PT-BR-COVERAGE | Full classic key parity | 21-04 | COVERED | Node parity assertion. |
| REQUIREMENTS | PHASE-21-LOCAL-FIRST-VALIDATION | Classic build/i18n checks | 21-04 | COVERED | Runs build and available i18n checks. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md
@web/classic/src/i18n/i18n.js
@web/classic/src/i18n/language.js
@web/classic/src/i18n/locales/en.json
@web/default/src/i18n/locales/pt.json
@web/classic/src/components/layout/headerbar/LanguageSelector.jsx
@web/classic/src/components/settings/personal/cards/PreferencesSettings.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build classic `pt.json` with reuse-first mapping</name>
  <files>web/classic/src/i18n/locales/pt.json</files>
  <action>
    Create classic `pt.json` with key parity against `web/classic/src/i18n/locales/en.json`. Reuse matching default frontend PT strings for identical English source text first, then translate/review classic-specific gaps. Do not copy stale historical PT-BR files wholesale.
  </action>
  <acceptance_criteria>
    - Classic `pt.json` exists.
    - Classic PT key set exactly matches classic English key set.
    - Placeholder tokens match English for every shared key.
    - Inventory summary records how many keys were reused from default PT versus newly translated/reviewed.
  </acceptance_criteria>
  <verify>
    <automated>test -f web/classic/src/i18n/locales/pt.json</automated>
  </verify>
  <done>Classic PT locale exists and is not a stale whole-file copy.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PT into classic i18n and normalization</name>
  <files>web/classic/src/i18n/i18n.js, web/classic/src/i18n/language.js</files>
  <action>
    Import/register `pt` in classic `i18n.js`, add `pt` to `supportedLanguages`, and normalize `pt-BR` and `pt_BR` to `pt` while preserving existing Chinese variant behavior.
  </action>
  <acceptance_criteria>
    - Classic resources include `pt`.
    - `supportedLanguages` includes `pt`.
    - `pt-BR` and `pt_BR` normalize to `pt`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\"pt\"|'pt'|pt-BR|pt_BR" web/classic/src/i18n/i18n.js web/classic/src/i18n/language.js</automated>
    <automated>cd web/classic && bun --eval "import { normalizeLanguage } from './src/i18n/language.js'; for (const input of ['pt', 'pt-BR', 'pt_BR']) { if (normalizeLanguage(input) !== 'pt') { throw new Error(`${input} did not normalize to pt`) } }"</automated>
  </verify>
  <done>Classic i18n can resolve Portuguese natively.</done>
</task>

<task type="auto">
  <name>Task 3: Add Portuguese to classic language controls</name>
  <files>web/classic/src/components/layout/headerbar/LanguageSelector.jsx, web/classic/src/components/settings/personal/cards/PreferencesSettings.jsx</files>
  <action>
    Add `Português` to the top-bar language selector and profile preferences in the same hardcoded style as existing languages. Keep existing persistence behavior: `i18n.changeLanguage`, `localStorage.setItem('i18nextLng', lang)`, and `/api/user/self` update.
  </action>
  <acceptance_criteria>
    - `Português` appears in both classic language controls.
    - Both controls use `pt` as the value passed to existing change/persistence paths.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Português|'pt'|\"pt\"|changeLanguage|i18nextLng|/api/user/self" web/classic/src/components/layout/headerbar/LanguageSelector.jsx web/classic/src/components/settings/personal/cards/PreferencesSettings.jsx</automated>
  </verify>
  <done>Classic users can select and persist Portuguese through existing UI paths.</done>
</task>

<task type="auto">
  <name>Task 4: Run classic parity and build checks</name>
  <files>web/classic/*</files>
  <action>
    Run explicit JSON key/placeholder parity checks plus the classic frontend build. Run available classic i18n/lint checks if they are configured for the upstream tree, and document exact blockers if a tool itself is not configured.
  </action>
  <acceptance_criteria>
    - Classic key parity assertion passes.
    - Classic build passes.
    - Classic i18n/lint checks pass or unrelated tool/config blockers are recorded exactly.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const en=JSON.parse(fs.readFileSync('web/classic/src/i18n/locales/en.json','utf8')).translation; const pt=JSON.parse(fs.readFileSync('web/classic/src/i18n/locales/pt.json','utf8')).translation; const keys=o=>Object.keys(o).sort(); const miss=keys(en).filter(k=>!(k in pt)); const extra=keys(pt).filter(k=>!(k in en)); const toks=s=>(String(s).match(/\\{\\{[^}]+\\}\\}/g)||[]).sort().join('|'); const ph=keys(en).filter(k=>toks(en[k])!==toks(pt[k])); if(miss.length||extra.length||ph.length){console.error({miss:miss.slice(0,10),extra:extra.slice(0,10),ph:ph.slice(0,10)}); process.exit(1)}"</automated>
    <automated>cd web/classic && bun run build</automated>
    <automated>cd web/classic && bun run i18n:lint</automated>
    <automated>cd web/classic && bun run lint</automated>
  </verify>
  <done>Classic frontend PT support is complete enough for local review.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Classic frontend omitted because default already works | medium | Classic is its own required plan and files_modified set. |
| Reusing default PT strings where English source differs | high | Reuse only exact English-source matches. |
| Massive stale historical file copy | high | Inventory says historical PT-BR is fallback wording only. |
| Classic selector lacks `Português` despite locale registration | medium | Separate language-control task and grep. |
</threat_model>

<verification>
<automated>cd web/classic && bun --eval "import { normalizeLanguage } from './src/i18n/language.js'; for (const input of ['pt', 'pt-BR', 'pt_BR']) { if (normalizeLanguage(input) !== 'pt') { throw new Error(`${input} did not normalize to pt`) } }"</automated>
<automated>node --input-type=module -e "import fs from 'node:fs'; const en=JSON.parse(fs.readFileSync('web/classic/src/i18n/locales/en.json','utf8')).translation; const pt=JSON.parse(fs.readFileSync('web/classic/src/i18n/locales/pt.json','utf8')).translation; const miss=Object.keys(en).filter(k=>!(k in pt)); const extra=Object.keys(pt).filter(k=>!(k in en)); if(miss.length||extra.length){console.error({miss,extra}); process.exit(1)}"</automated>
<automated>cd web/classic && bun run build</automated>
<automated>git diff --check upstream/main...HEAD -- web/classic</automated>
</verification>

<success_criteria>
- Classic frontend exposes `Português` through both existing language controls.
- Classic `pt.json` has full key and placeholder parity with classic `en.json`.
- Classic build passes and configured i18n/lint checks pass or blockers are documented.
- No classic PT source is copied wholesale from stale historical artifacts.
</success_criteria>

<output>
Create `.planning/phases/21-feat-pt-native-pr/21-04-SUMMARY.md` outside the upstream PR branch after classic frontend validation passes.
</output>
