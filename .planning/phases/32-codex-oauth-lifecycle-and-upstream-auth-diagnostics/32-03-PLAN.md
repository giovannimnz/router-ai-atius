# Phase 32-03 PLAN — Browser-assisted operator flow, docs and fork-sync guards

## Objetivo

Documentar e proteger o fluxo definitivo de credencial Codex, incluindo operacao com Brave/Chrome logado, fallback break-glass e guardas contra upstream sync.

## Escopo

- `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`
- `docs/CODEX-CI-AUTH-RELEASE.md` se existir e for aplicavel
- `docs/CI-UPSTREAM-SYNC.md`
- `.github/workflows/sync.yml`
- `/home/ubuntu/GitHub/omni-srv-admin/modules/fork-sync/projects/atius-router/sync.yaml`
- `/home/ubuntu/GitHub/omni-srv-admin/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md`
- Opcional: script helper local de operador para Chrome DevTools/Brave, se couber sem acoplar segredo.

## Tarefas

1. Atualizar runbook PT-BR do Router com:
   - diferenca entre refresh e regenerate
   - fluxo browser-assisted com Brave/Chrome ja logado
   - fallback manual de colar URL callback
   - break-glass via Codex CLI access-token-only, sem refresh_token
   - como diagnosticar `codex_upstream_token_invalidated`
2. Atualizar guardas fork-sync no `omni-srv-admin`:
   - contrato UI type `57` sem `Base URL`/`API Key`
   - rotas OAuth regeneration
   - erro upstream-auth especifico
   - paths protegidos efetivos
3. Atualizar `docs/CI-UPSTREAM-SYNC.md` e `.github/workflows/sync.yml` se o CI efetivo ainda nao protege `web/default/src/features/channels/`.
4. Registrar nota Obsidian sem segredos com decisoes e evidencias.
5. Registrar GBrain se o MCP/CLI estiver saudavel; se falhar, documentar o blocker.
6. Se criar helper DevTools, garantir que ele nunca imprima tokens e que aceite apenas URL/callback/metadata sanitizada.

## Validacao da Wave

Comandos obrigatorios:

```bash
git diff --check -- docs .github/workflows/sync.yml
git diff --check -- /home/ubuntu/GitHub/omni-srv-admin/modules/fork-sync/projects/atius-router/sync.yaml /home/ubuntu/GitHub/omni-srv-admin/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
scripts/check-upstream-sync-workflow.sh
```

Validacoes logicas:

- Docs nao contem token, refresh token ou bearer value.
- Guardas citam explicitamente o contrato Codex UI/OAuth/erro.
- CI/fork-sync continua protegendo paths necessarios para esta fase.

## Criterio de Conclusao

O comportamento novo fica operavel e protegido contra perda no proximo merge upstream.
