---
phase: 33-reranker-reliability-observability-and-readiness
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - dto/channel_settings_tei_rerank_test.go
  - relay/channel/api_request_test.go
  - relay/embedding_handler_test.go
  - controller/channel_readiness_test.go
  - router/channel_router_test.go
autonomous: true
requirements:
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-TEI-TIMEOUT-RETRY
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Parent cancellation reaches TEI, each attempt has a fresh bounded context, and only a safe transport/5xx failure gets one retry."
    - "Exactly 20 documents are accepted while 21 fail deterministically before governor acquisition."
    - "Admin readiness proves channel 10, alias, ability, route, cache, and normal selection under a bounded poll."
  artifacts:
    - path: "relay/channel/api_request_test.go"
      provides: "Outbound context propagation tests"
    - path: "relay/embedding_handler_test.go"
      provides: "Rerank timeout, governor, retry, and cap tests"
    - path: "controller/channel_readiness_test.go"
      provides: "Readiness state matrix"
    - path: "router/channel_router_test.go"
      provides: "ChannelOperate permission assertion"
  key_links:
    - from: "relay/embedding_handler_test.go"
      to: "relay/channel/api_request_test.go"
      via: "fresh request context per rerank attempt"
      pattern: "Context|Deadline|Retry"
    - from: "controller/channel_readiness_test.go"
      to: "router/channel_router_test.go"
      via: "POST /api/channel/:id/readiness protected by ChannelOperate"
      pattern: "readiness|ChannelOperate"
---

<objective>
Create Wave 0 tests for bounded TEI calls, the unchanged 20-document contract, and authenticated selection-based channel readiness.

Purpose: Lock PHASE-33-CHANNEL-READINESS, PHASE-33-TEI-TIMEOUT-RETRY, and PHASE-33-RERANK-BATCH-CONTRACT before modifying relay or channel code.
Output: Five focused test files covering context/retry/governor/cap/readiness behavior.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-PATTERNS.md
@dto/channel_settings.go
@relay/rerank_handler.go
@relay/channel/api_request.go
@relay/embedding_handler_test.go
@model/channel_cache.go
@model/channel_satisfy.go
@router/channel-router.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock timeout, cancellation, governor, retry, and document-cap contracts</name>
  <files>dto/channel_settings_tei_rerank_test.go, relay/channel/api_request_test.go, relay/embedding_handler_test.go</files>
  <read_first>dto/channel_settings.go, dto/channel_settings_tei_rerank_test.go, relay/rerank_handler.go, relay/channel/api_request.go, relay/channel/api_request_test.go, relay/embedding_handler_test.go, service/embeddinggovernor/governor.go</read_first>
  <behavior>
    - A positive channel override is honored; absent/zero configuration still resolves to a bounded positive timeout chosen by Plan 33-07.
    - Parent cancellation and attempt deadline are observed by httptest.Server.
    - Attempt two receives a fresh context and a fresh governor lease only after an allowlisted transport/5xx failure.
    - Upstream 4xx/429, invalid response, client validation, governor rejection, and routing failure do not retry.
    - 20 documents pass; 21 return deterministic invalid_request 4xx before governor/upstream.
  </behavior>
  <action>Expand the existing tests with deterministic httptest.Server and injected governor fixtures for PHASE-33-TEI-TIMEOUT-RETRY and PHASE-33-RERANK-BATCH-CONTRACT. Target the new `ChannelSettings.RerankTimeoutSeconds`, `ResolveRerankTimeout`, and context-aware HTTP construction. Assert a positive bound without locking the research-only 60-second assumption; Plan 33-07 selects the numeric fallback from sanitized SLO/runtime evidence. Preserve explicit zero semantics where applicable, the Advanced Custom TEI converter, stable result indices, common JSON wrappers, and the current max of 20 documents. Do not send real documents or provider traffic.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'set +e; /usr/local/go/bin/go test ./dto ./relay ./relay/channel -run "Test.*(Rerank|TEI|Context|Deadline|Retry|DocumentLimit)" -count=1 &gt;/tmp/phase33-w0-tei.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "undefined:.*(RerankTimeoutSeconds|ResolveRerankTimeout|DoApiRequestWithContext)" /tmp/phase33-w0-tei.log'</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover parent cancellation, fresh retry context, at most two attempts, governor lease count, no-retry matrix, 20 accepted, and 21 rejected.
    - No test requires a live TEI or contains secret/document fixtures.
    - The governed command passes or fails only on named Phase 33 symbols.
  </acceptance_criteria>
  <done>The TEI reliability and batch contract is executable before Plan 33-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock authenticated channel readiness and rollback-safe state transitions</name>
  <files>controller/channel_readiness_test.go, router/channel_router_test.go</files>
  <read_first>controller/codex_oauth.go, controller/channel.go, router/channel-router.go, router/channel_router_test.go, model/channel_cache.go, model/channel_satisfy.go, service/channel.go</read_first>
  <behavior>
    - POST /api/channel/:id/readiness requires AdminAuth plus authz.ChannelOperate.
    - Missing/disabled channel, absent alias, absent ability, wrong Advanced Custom route, stale cache, timeout, cancellation, and unauthorized access fail closed with fixed reason codes.
    - A synchronous cache rebuild followed by normal distributor selection of channel 10 marks the reranker ready.
    - Failed re-enable restores the previous disabled state and rebuilds cache again.
  </behavior>
  <action>Create the PHASE-33-CHANNEL-READINESS test matrix using injected cache-init/selection seams and a fake clock/poll function rather than sleeps. Assert the response exposes only ready, channel_id, model, ability_ready, route_ready, cache_ready, reason_code, attempts, and elapsed_ms. Fix the canonical target to channel `10`, alias `reranker-gte-multilingual-v1`, incoming path `/v1/rerank`, and converter `jina_rerank_to_tei_native`. Add the route-table handler identity and `authz.ChannelOperate` permission assertion.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'set +e; /usr/local/go/bin/go test ./controller ./router -run "Test.*ChannelReadiness" -count=1 &gt;/tmp/phase33-w0-ready.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "undefined:.*(ChannelReadiness|ProbeChannelReadiness|ReadinessReason)" /tmp/phase33-w0-ready.log'</automated>
  </verify>
  <acceptance_criteria>
    - The readiness test table includes all nine required negative/positive cases.
    - Router permission coverage names POST `/:id/readiness` and `authz.ChannelOperate`.
    - Failure restoration is asserted without mutating a real channel or cache.
  </acceptance_criteria>
  <done>Readiness semantics are locked before Plan 33-09 adds the endpoint and mutation wiring.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `controller/channel_readiness_test.go`: new readiness state matrix.
- Test cases in the existing DTO/relay/router files for `RerankTimeoutSeconds`, `ResolveRerankTimeout`, context propagation, retry policy, 20-document cap, and route authorization.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client to rerank relay | Untrusted size/status inputs cross validation, governor, and upstream boundaries. |
| Admin to readiness endpoint | Privileged cache synchronization exposes channel topology and can create load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-04 | Denial of Service | TEI retry | high | mitigate | Tests enforce bounded timeout, one retry, and governor lease per attempt. |
| T-33-05 | Elevation of Privilege | readiness route | high | mitigate | AdminAuth plus ChannelOperate route identity test. |
| T-33-06 | Information Disclosure | readiness/test output | medium | mitigate | Fixed metadata/reason codes only; no URLs with credentials or bodies. |
</threat_model>

<verification>
Run both focused test commands under the Router CPU wrapper and confirm any RED result is limited to the planned contract symbols.
</verification>

<success_criteria>
- Five focused test files cover all four assigned requirements.
- No production file changes or real runtime mutations occur.
- The timeout tests deliberately avoid locking 60 seconds before Plan 33-07.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-02-SUMMARY.md` when done.
</output>
