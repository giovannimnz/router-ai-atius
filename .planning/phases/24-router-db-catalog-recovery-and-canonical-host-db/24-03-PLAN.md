---
phase: 24-router-db-catalog-recovery-and-canonical-host-db
plan: "03"
type: execute
wave: 3
depends_on:
  - "24-02"
files_modified:
  - service/modelcatalog/catalog.go
  - controller/model_list_test.go
  - setting/ratio_setting/model_ratio.go
  - docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md
  - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
autonomous: true
requirements:
  - PHASE-24-CATALOG-RESTORE
  - PHASE-24-PROVIDER-CONSOLIDATION
  - PHASE-24-EMBEDDING-GOVERNOR-PRESERVE
must_haves:
  truths:
    - D-07 `OpenAI - Codex` returns without `-1m` aliases.
    - D-08 `gpt-5.4` is treated as the default long-context Codex model in catalog/docs/tests.
    - D-09 DeepSeek ends with one active consolidated channel.
    - D-10 MiniMax ends with one consolidated restored channel but disabled final state.
    - D-11 The Go embedding governor path remains unchanged as the canonical embeddings route.
  artifacts:
    - service/modelcatalog/catalog.go
    - controller/model_list_test.go
    - setting/ratio_setting/model_ratio.go
  key_links:
    - catalog code/tests -> restored DB rows -> public /v1/models
    - provider/channel design -> final DB enabled/disabled state
    - governor contract -> embeddings validation
---

<objective>
Align code, tests, and docs with the restored final provider/catalog design.

Purpose: ensure the restored DB state is not contradicted by local code/tests/docs and that the 1M-alias rollback is reflected in the codebase contract.
</objective>

## Artifacts this plan produces

- Updated Codex catalog/docs/test surfaces
- Updated provider restoration policy docs
- No new `-1m` alias expectations

<context>
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-CONTEXT.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-RESEARCH.md
@service/modelcatalog/catalog.go
@controller/model_list_test.go
@setting/ratio_setting/model_ratio.go
@service/embeddinggovernor/governor.go
@relay/embedding_handler.go
@docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove `-1m` alias assumptions from code/tests/docs where Phase 24 changes the final contract</name>
  <files>service/modelcatalog/catalog.go, controller/model_list_test.go, setting/ratio_setting/model_ratio.go, docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md</files>
  <action>
    Update local catalog-building assumptions, tests, and docs so the final restored contract no longer expects `gpt-5.4-1m` or `gpt-5.5-1m` to exist. Reflect that `gpt-5.4` is the default long-context Codex model and `gpt-5.5` has no 1M alias in the final restored runtime.
  </action>
  <acceptance_criteria>
    - Tests/docs no longer require `gpt-5.4-1m` or `gpt-5.5-1m` in the final Phase 24 state.
    - Docs explicitly say `gpt-5.4` is the default long-context Codex model.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "gpt-5.4|gpt-5.5|1m" service/modelcatalog/catalog.go controller/model_list_test.go setting/ratio_setting/model_ratio.go docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md</automated>
  </verify>
  <done>Local code/tests/docs match the desired final GPT/Codex contract.</done>
</task>

<task type="auto">
  <name>Task 2: Record final provider/channel policy</name>
  <files>docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</files>
  <action>
    Update the manual so it reflects the final restored policy:
    - `OpenAI - Codex` restored active
    - DeepSeek single consolidated channel active
    - MiniMax single consolidated channel restored but disabled
    - embeddings remain `embedding-gte-v1` under Go governor
  </action>
  <acceptance_criteria>
    - The manual explicitly mentions `OpenAI - Codex` active.
    - The manual explicitly mentions DeepSeek one active consolidated channel.
    - The manual explicitly mentions MiniMax restored but disabled.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "OpenAI - Codex|DeepSeek|MiniMax|restaurad|desabilitad|embedding-gte-v1" docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</automated>
  </verify>
  <done>Operational docs match the restored provider/channel state.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve the governor contract in verification surface</name>
  <files>docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md, docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md</files>
  <action>
    Add explicit verification lines stating that `embedding-gte-v1` remains the only governed public embedding alias and that `EMBEDDING_GOVERNOR_MODELS=embedding-gte-v1` remains unchanged through recovery.
  </action>
  <acceptance_criteria>
    - Docs contain `EMBEDDING_GOVERNOR_MODELS=embedding-gte-v1`.
    - Docs contain `embedding-gte-v1` as the only governed public embedding alias.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "EMBEDDING_GOVERNOR_MODELS=embedding-gte-v1|only governed public embedding alias|embedding-gte-v1" docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md</automated>
  </verify>
  <done>Governor preservation is explicit in the validation surface.</done>
</task>

</tasks>

<verification>
<automated>rg -n "OpenAI - Codex|DeepSeek|MiniMax|embedding-gte-v1" docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</automated>
<automated>rg -n "gpt-5.4|gpt-5.5|1m" docs/OPENAI-CODEX-PROVIDER-1M-CONTEXT.md controller/model_list_test.go setting/ratio_setting/model_ratio.go</automated>
</verification>

<success_criteria>
- Code/tests/docs reflect the desired final provider/catalog contract.
- No lingering requirement for the removed `-1m` aliases in the final restored state.
- Governor preservation is explicit and testable.
</success_criteria>

<output>
Create `.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-03-SUMMARY.md` when done.
</output>
