---
phase: 29-k3s-shadow-restore-and-go-no-go
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - bin/clianything
  - scripts/k3s-router-apply-shadow.sh
  - scripts/k3s-router-smoke.sh
autonomous: true
requirements:
  - PHASE-22-RUNTIME-PARITY
  - PHASE-22-STATEFUL-DATA
must_haves:
  truths:
    - "Redis e router so sobem depois de restore_passed e ficam no atius-srv-1."
    - "O shadow e acessado pelo ClusterIP diretamente do host, sem NodePort, Ingress, hostPort ou mudanca no Apache."
    - "Token ausente, auth incompleta, payload incorreto, embedding divergente ou evidencia ausente fazem o smoke falhar."
    - "CLIAnything opera status e banco no k3s sem depender de podman exec."
  artifacts:
    - path: "scripts/k3s-router-apply-shadow.sh"
      provides: "Segundo estagio do rollout para Redis e router"
    - path: "scripts/k3s-router-smoke.sh"
      provides: "Smoke autenticado fail-closed contra ClusterIP"
    - path: "bin/clianything"
      provides: "Backend k3s para operacoes do router e PostgreSQL"
  key_links:
    - from: "scripts/k3s-router-apply-shadow.sh"
      to: "29-02 restore evidence"
      via: "exige restore_passed e checksum antes de aplicar Redis/router"
      pattern: "restore_passed"
    - from: "scripts/k3s-router-smoke.sh"
      to: "Service router-ai-atius ClusterIP"
      via: "resolve spec.clusterIP e executa HTTP do host"
      pattern: "spec.clusterIP"
    - from: "bin/clianything"
      to: "statefulset/router-ai-atius-postgres"
      via: "kubectl exec no backend k3s"
      pattern: "k3s.*kubectl"
---

<objective>
Completar o shadow com Redis/router, validar paridade fail-closed pelo ClusterIP e tornar CLIAnything capaz de operar o backend k3s.

Purpose: Provar que o runtime restaurado funciona de ponta a ponta sem alterar o edge publico nem perder a via operacional do banco.
Output: Rollout shadow estagiado, smoke autenticado estrito e backend k3s do CLIAnything.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/debug/phase29-diskpressure-audit.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-02-SUMMARY.md
@bin/clianything
@scripts/k3s-router-apply-shadow.sh
@scripts/k3s-router-smoke.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar backend k3s do CLIAnything</name>
  <files>bin/clianything</files>
  <behavior>
    - Teste 1: backend k3s resolve namespace e pod PostgreSQL unicos e Ready; zero ou multiplos candidatos falham.
    - Teste 2: status, query/read-only e operacoes DB autorizadas usam kubectl exec sem imprimir credenciais.
    - Teste 3: backend Podman existente permanece selecionavel e inalterado para rollback.
  </behavior>
  <action>Adicionar selecao explicita de backend k3s ao CLIAnything, seguindo os contratos atuais de status e DB. Resolver recursos por namespace/labels, exigir pod unico Ready no node dedicado e executar comandos no container correto. Obter credenciais apenas no processo remoto a partir do Secret ja montado ou variaveis do container, sem ecoar valores. Preservar backend Podman para rollback e exigir escolha explicita quando ambos existirem. Cobrir status e as operacoes necessarias ao smoke/diagnostico antes de considerar aposentadoria do Podman.</action>
  <verify>
    <automated>bash -n bin/clianything &amp;&amp; bin/clianything self-test --backend k3s</automated>
  </verify>
  <done>CLIAnything valida status e acesso DB pelo k3s, falha em selecao ambigua e continua suportando Podman.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aplicar Redis e router somente apos restore aprovado</name>
  <files>scripts/k3s-router-apply-shadow.sh</files>
  <behavior>
    - Teste 1: restore_passed ausente/invalido impede qualquer apply de Redis/router.
    - Teste 2: Redis fica Ready antes do router e todos os pods comprovam nodeName atius-srv-1.
    - Teste 3: PVCs Bound, PVs Retain e EndpointSlice aponta para router Ready antes de sucesso.
    - Teste 4: Apache e Podman permanecem inalterados e o Service continua ClusterIP.
  </behavior>
  <action>Completar o segundo estagio do apply exigindo evidencia assinada por checksum do plano 02. Aplicar Redis, aguardar readiness, depois router por digest imutavel importado no containerd. Revalidar DiskPressure, taint, affinity/nodeName, PVC/PV Retain, Service ClusterIP e EndpointSlice. O script deve abortar em qualquer divergencia, nao editar/recarregar Apache, nao parar/reiniciar Podman e nao criar exposicao alternativa.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-apply-shadow.sh &amp;&amp; scripts/k3s-router-apply-shadow.sh --self-test-shadow-stage</automated>
  </verify>
  <done>Redis e router estao Ready no srv1, ligados ao Postgres restaurado, com ClusterIP/EndpointSlice validos e rollback Podman intacto.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Executar smoke shadow autenticado e fail-closed</name>
  <files>scripts/k3s-router-smoke.sh</files>
  <behavior>
    - Teste 1: K3S_ROUTER_BASE_URL ou ATIUS_ROUTER_TOKEN ausente falha; nenhum skip pode produzir sucesso.
    - Teste 2: health 200, /v1/models sem auth 401 e resposta autenticada com raiz somente data sao obrigatorios.
    - Teste 3: campos internos de pricing nao aparecem, modelos Go esperados existem e embedding-gte-v1 retorna dimensao 768.
    - Teste 4: qualquer chamada, assertion ou captura de evidencia incompleta resulta em status nao zero sem armazenar token/payload sensivel.
  </behavior>
  <action>Resolver o ClusterIP do Service e construir a URL shadow host-reachable comprovada pela auditoria. Tornar token e todos os gates obrigatorios; preservar health, auth negativa, shape publico Go-only, catalogo e embeddings governados. Registrar apenas status codes, assertions, commit/digest e timestamps. Executar smokes com carga pequena e sequencial para respeitar CPU menor ou igual a 20%; proibir NodePort, port-forward persistente ou mudanca no Apache.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-smoke.sh &amp;&amp; scripts/k3s-router-smoke.sh --self-test</automated>
  </verify>
  <done>Smoke completo passa contra ClusterIP com token; todos os cenarios ausentes/incorretos falham e a evidencia nao contem segredo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → Service ClusterIP | Requests shadow atravessam kube-proxy para o router sem edge publico. |
| CLIAnything → pod PostgreSQL | Operacoes administrativas entram no banco k3s. |
| token Vault → API router | Credencial real autentica o smoke sem poder persistir na evidencia. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-09 | Spoofing | CLIAnything pod selection | high | mitigate | namespace/label/node/Ready unicos e backend explicito |
| T-29-10 | Information Disclosure | smoke token/responses | high | mitigate | token somente em memoria; evidencia limitada a codigos e assertions |
| T-29-11 | Tampering | API contract | high | mitigate | assertions exatas de root data-only, campos proibidos e dimensao 768 |
| T-29-12 | Denial of Service | shadow smoke | medium | mitigate | requests pequenas, sequenciais, timeout e CPU total limitada |
| T-29-SC | Tampering | imagem router/Redis | high | mitigate | digest imutavel/importado e imagens auditadas; nenhuma instalacao |
</threat_model>

<verification>
Executar self-tests, rollout live somente com restore aprovado, CLIAnything status/DB no backend k3s e smoke estrito pelo ClusterIP. Confirmar por diff/read-only que Apache e Podman nao mudaram.
</verification>

<success_criteria>
Stack completa Ready no srv1, CLIAnything k3s funcional e smoke autenticado integral verde contra ClusterIP, com Podman/public edge intactos e nenhum segredo em evidencia.
</success_criteria>

<output>
Create `.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-03-SUMMARY.md` when done
</output>
