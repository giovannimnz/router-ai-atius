---
phase: 33-reranker-reliability-observability-and-readiness
plan: "08"
type: execute
wave: 4
depends_on: ["33-02", "33-06", "33-07"]
files_modified:
  - dto/channel_settings.go
  - relay/channel/api_request.go
  - relay/rerank_handler.go
  - controller/relay.go
autonomous: true
requirements:
  - PHASE-33-TEI-TIMEOUT-RETRY
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-METRICS-OUTCOME-TAXONOMY
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Every rerank attempt inherits parent cancellation and the evidence-backed timeout from Plan 33-07."
    - "At most one retry occurs only for allowlisted transport/5xx failures, with a fresh context and governor lease."
    - "The 20-document cap remains before governor/upstream and terminal/settlement accounting remains once per request."
  artifacts:
    - path: "dto/channel_settings.go"
      provides: "rerank_timeout_seconds channel override"
    - path: "relay/channel/api_request.go"
      provides: "Context-bound upstream HTTP request construction"
    - path: "relay/rerank_handler.go"
      provides: "Per-attempt timeout and unchanged cap/governor lifecycle"
    - path: "controller/relay.go"
      provides: "Rerank-specific bounded retry policy"
  key_links:
    - from: "controller/relay.go"
      to: "relay/rerank_handler.go"
      via: "each retry re-enters RerankHelper and reacquires governor"
      pattern: "shouldRetryRerank|RetryIndex"
    - from: "relay/rerank_handler.go"
      to: "relay/channel/api_request.go"
      via: "fresh child context reaches http.NewRequestWithContext"
      pattern: "WithTimeout|NewRequestWithContext"
---

<objective>
Implement bounded, cancelable TEI inference and a one-repeat safe retry policy while preserving the 20-document/governor/accounting contracts.

Purpose: Prevent hung or amplified TEI calls and classify terminal failures correctly even when global RELAY_TIMEOUT is zero.
Output: Channel timeout setting, context-bound outbound requests, and rerank-specific retry budget.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-02-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-06-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-07-SUMMARY.md
@docs/ATIUS-RERANKER-RELIABILITY.md
@dto/channel_settings.go
@relay/channel/api_request.go
@relay/rerank_handler.go
@controller/relay.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the evidence-backed channel timeout and context-bound HTTP seam</name>
  <files>dto/channel_settings.go, relay/channel/api_request.go, relay/rerank_handler.go</files>
  <read_first>.planning/phases/33-reranker-reliability-observability-and-readiness/33-07-SUMMARY.md, docs/ATIUS-RERANKER-RELIABILITY.md, dto/channel_settings.go, dto/channel_settings_tei_rerank_test.go, relay/channel/api_request.go, relay/channel/api_request_test.go, relay/rerank_handler.go</read_first>
  <behavior>
    - Positive rerank_timeout_seconds overrides the documented fallback N.
    - Zero/absent uses N even with RELAY_TIMEOUT=0.
    - Parent cancellation reaches client.Do and each attempt gets a new child context.
  </behavior>
  <action>Add `RerankTimeoutSeconds int` with JSON key `rerank_timeout_seconds,omitempty` and validation that accepts zero as unset and rejects negative values. Implement `ResolveRerankTimeout` using the exact N recorded by Plan 33-07. Change DoApiRequest to construct requests with `http.NewRequestWithContext(c.Request.Context(), ...)`. In RerankHelper, derive/cancel a fresh child context per attempt, temporarily scope the Gin request to that child only around adaptor.DoRequest, and restore the original parent request before returning so a retry never inherits an expired context. Keep `common.Marshal` and existing headers/proxy behavior.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./dto ./relay ./relay/channel -run 'Test.*(RerankTimeout|Context|Deadline|TEI)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Tests read the same numeric N as Plan 33-07.
    - Parent cancellation, timeout, and fresh attempt context tests pass.
    - No child context remains stored in Gin/RelayInfo after an attempt.
  </acceptance_criteria>
  <done>Every TEI attempt is bounded and cancelable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one safe rerank retry with per-attempt governor accounting</name>
  <files>controller/relay.go, relay/rerank_handler.go</files>
  <read_first>controller/relay.go, controller/relay_test.go, relay/rerank_handler.go, relay/embedding_handler_test.go, service/embeddinggovernor/governor.go, pkg/perf_metrics/types.go</read_first>
  <behavior>
    - Only retry index zero may repeat, and only for context-safe transport errors or explicit 5xx.
    - Client/local 4xx, governor rejection, routing failure, upstream 4xx/429, and invalid response do not retry.
    - Each attempt acquires/finishes one governor lease; one terminal outcome and settlement/refund remains per request.
  </behavior>
  <action>Add `shouldRetryRerank` before the generic retry decision for RelayModeRerank. Allow exactly one repeat for connection/reset/temporary transport failures and completed upstream 500/502/503/504; do not retry context cancellation by the parent, any 4xx including 429, validation/governor/routing errors, or malformed upstream response. Ensure RerankHelper retains `maxGovernedTEIRerankDocuments = 20` before governor acquisition, reacquires/finalizes a lease on each attempt, and maps deadline/transport/upstream/invalid-response to the Phase 33 terminal taxonomy only after the retry policy becomes terminal. Do not move retry inside the adaptor.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc '/usr/local/go/bin/go test ./controller ./relay ./relay/channel ./service/embeddinggovernor -run "Test.*(RerankRetry|DocumentLimit|Lease|Deadline|Context|Outcome)" -count=1 &amp;&amp; bash scripts/atius-user-quota-guard.sh'</automated>
  </verify>
  <acceptance_criteria>
    - Retry matrix and fresh-context/governor counts pass.
    - 20/21 document boundary remains unchanged and deterministic.
    - Terminal, settlement/refund, and user-quota guards pass once per request.
  </acceptance_criteria>
  <done>Rerank retry is bounded, safe, governed, and non-duplicating.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `ChannelSettings.RerankTimeoutSeconds` and `ResolveRerankTimeout`.
- Context-bound `DoApiRequest` request construction.
- `shouldRetryRerank` with an allowlisted maximum of one repeat.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Router to TEI | Network failure/status crosses a private upstream boundary. |
| Retry loop to governor/billing | Attempts must not bypass capacity or duplicate financial/terminal effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-21 | Denial of Service | timeout/retry | high | mitigate | Evidence-backed bound, parent cancellation, and one allowlisted retry. |
| T-33-22 | Tampering | governor/accounting | high | mitigate | Fresh lease per attempt; terminal/settlement once; focused tests. |
| T-33-23 | Information Disclosure | upstream errors | medium | mitigate | Fixed outcome codes and hidden/sanitized error messages only. |
</threat_model>

<verification>
Run the focused DTO/relay/controller/governor suites and quota guard under the CPU wrapper; no live TEI is required here.
</verification>

<success_criteria>
- Plan 33-02 TEI tests are green using the Plan 33-07 numeric default.
- Max two attempts and one terminal outcome are enforced.
- The 20-document contract and quota behavior are unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-08-SUMMARY.md` when done.
</output>
