---
phase: 22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/K3S-MIGRATION.md
  - scripts/k3s-router-preflight.sh
  - .planning/STATE.md
autonomous: true
requirements:
  - PHASE-22-K3S-PREFLIGHT
  - PHASE-22-RUNTIME-PARITY
must_haves:
  truths:
    - D-01 Podman remains the rollback source of truth until k3s shadow validation and cutover smokes pass.
    - D-02 Kubernetes resources use a dedicated `router-ai-atius` namespace.
    - D-03 Apache/Cloudflare remain the initial public edge; no ingress controller is introduced in this phase.
    - D-11 Current cluster DiskPressure/image pressure is treated as a preflight blocker or explicit node-selection constraint.
  artifacts:
    - docs/K3S-MIGRATION.md
    - scripts/k3s-router-preflight.sh
  key_links:
    - Podman baseline -> k3s preflight -> go/no-go decision
    - cluster events/storage -> stateful migration risk table
    - provider inventory -> runtime parity requirements
---

<objective>
Create the k3s migration contract and preflight gate for `router-ai-atius`.

Purpose: make the migration decision evidence-based before manifests or cutover touch production traffic.
Output: a runbook and non-secret preflight script that capture the current Podman baseline, k3s cluster blockers, routing edge decision, and migration go/no-go gates.
</objective>

## Artifacts this phase produces

- `docs/K3S-MIGRATION.md`
- `scripts/k3s-router-preflight.sh`
- Phase 22 state notes in `.planning/STATE.md`

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | phase_goal | Prepare and validate migration from Podman to k3s with rollback | 22-01, 22-02, 22-03, 22-04 | COVERED | Sequential waves keep cutover last. |
| CONTEXT | D-01 | Keep Podman rollback until validation passes | 22-01, 22-03, 22-04 | COVERED | Baseline and rollback gates. |
| CONTEXT | D-02 | Dedicated namespace `router-ai-atius` | 22-01, 22-02 | COVERED | Runbook and manifests. |
| CONTEXT | D-03 | Keep Apache/Cloudflare edge initially | 22-01, 22-04 | COVERED | No ingress controller in scope. |
| CONTEXT | D-11 | Disk/image pressure is a blocker | 22-01, 22-03 | COVERED | Preflight checks and go/no-go. |
| RESEARCH | Cluster storage local-path only | DB cutover is high risk | 22-01, 22-03 | COVERED | Data strategy must be explicit. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-CONTEXT.md
@.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-RESEARCH.md
@docs/PODMAN.md
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@scripts/podman-validate.sh
@podman-compose.yml
@/home/ubuntu/GitHub/embeddings/k8s/tei-gte.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the migration contract runbook</name>
  <files>docs/K3S-MIGRATION.md</files>
  <read_first>
    - `.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-CONTEXT.md`: decisions D-01 through D-12.
    - `.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-RESEARCH.md`: current runtime findings, k3s findings, and prerequisites.
    - `docs/PODMAN.md`: current production shape, restart path, and rollback validation.
    - `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`: provider, embeddings, governor, smoke, and recovery rules.
  </read_first>
  <action>
    Create `docs/K3S-MIGRATION.md` in PT-BR. Include sections named `Estado atual`, `Decisoes bloqueantes`, `Preflight`, `Arquitetura alvo inicial`, `Dados e secrets`, `Shadow deployment`, `Cutover manual`, `Rollback`, and `Go/no-go`. State exact current blockers: `atius-srv-1` DiskPressure, image filesystem pressure on `atius-srv-2`, local-path RWO/Delete/no expansion, and no IngressClass. State that Apache/Cloudflare remain the edge in this phase. State that Podman remains active until public k3s smokes pass. Do not include secret values.
  </action>
  <acceptance_criteria>
    - `docs/K3S-MIGRATION.md` contains the exact string `Podman continua sendo o rollback ate o cutover k3s passar nos smokes`.
    - The runbook names namespace `router-ai-atius`.
    - The runbook names the four blockers: `DiskPressure`, `image filesystem`, `local-path`, and `IngressClass`.
    - The runbook states that `model-detailed` must not return to the `/v1/` path.
    - The runbook includes a `Rollback` section with `systemctl --user restart container-router-ai-atius.service` and `bin/clianything status`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Podman continua sendo o rollback|router-ai-atius|DiskPressure|image filesystem|local-path|IngressClass|model-detailed|systemctl --user restart container-router-ai-atius.service|bin/clianything status" docs/K3S-MIGRATION.md</automated>
  </verify>
  <done>Operators have a migration contract before any k3s apply.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-secret k3s preflight script</name>
  <files>scripts/k3s-router-preflight.sh</files>
  <read_first>
    - `scripts/podman-validate.sh`: current shell style and validation pattern.
    - `docs/PODMAN.md`: baseline commands for Podman daily checks.
    - `/home/ubuntu/GitHub/embeddings/scripts/apply-tei.sh`: safe `sudo -n kubectl` usage pattern.
  </read_first>
  <action>
    Create `scripts/k3s-router-preflight.sh` with `set -euo pipefail`. The script must run only read-only checks: Graphify status, `bin/clianything status`, `bin/clianything providers --all`, `podman pod ps`, `podman ps --filter pod=atius-ai-router`, `sudo -n k3s kubectl get nodes -o wide`, `sudo -n k3s kubectl top nodes`, `sudo -n k3s kubectl get storageclass,pv,pvc -A -o wide`, `sudo -n k3s kubectl get ingressclass,ingress -A -o wide`, and recent events sorted by timestamp. Redact common secret tokens from any systemd/unit output if included. Do not call `kubectl apply`, `delete`, `rollout restart`, `podman restart`, `systemctl restart`, or any write command.
  </action>
  <acceptance_criteria>
    - Script starts with `#!/usr/bin/env bash` and `set -euo pipefail`.
    - Script contains `sudo -n k3s kubectl get nodes -o wide`.
    - Script contains `sudo -n k3s kubectl get ingressclass,ingress -A -o wide`.
    - Script does not contain `kubectl apply`, `kubectl delete`, `rollout restart`, `podman restart`, or `systemctl --user restart`.
    - `bash -n scripts/k3s-router-preflight.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/k3s-router-preflight.sh</automated>
    <automated>! rg -n "kubectl apply|kubectl delete|rollout restart|podman restart|systemctl --user restart" scripts/k3s-router-preflight.sh</automated>
  </verify>
  <done>Preflight can gather migration evidence without changing runtime state.</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase 22 state without overwriting Phase 21 context</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - `.planning/STATE.md`: current Phase 21 handoff, v1.8 Podman runtime, and next actions.
    - `.planning/ROADMAP.md`: Phase 22 entry.
  </read_first>
  <action>
    Add a concise `Roadmap Evolution` or pending-work note stating that Phase 22 was added for k3s migration preflight and that Phase 21 remains a separate PT-native upstream PR handoff. Do not delete or rewrite the existing Phase 21 context. Mention that production Podman remains the current source of truth until Phase 22 cutover gates pass.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `Phase 22 added: k3s migration preflight`.
    - `.planning/STATE.md` still contains `Phase 21` and `feat-pt-native-pr`.
    - `.planning/STATE.md` contains `Podman remains the current production source of truth`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 22 added: k3s migration preflight|Phase 21|feat-pt-native-pr|Podman remains the current production source of truth" .planning/STATE.md</automated>
  </verify>
  <done>Planning state captures the new migration phase without erasing active PT-native handoff context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator shell -> runtime | Preflight commands read live Podman and k3s state. |
| Docs/scripts -> secrets | Scripts may touch unit metadata but must not print real secrets. |
| Planning -> production | Phase 22 planning must not trigger cutover or restarts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-22-01-01 | Information Disclosure | preflight output | high | mitigate | Read-only checks only; redact secret-like env output; avoid `kubectl get secret -o yaml`. |
| T-22-01-02 | Denial of Service | preflight script | high | mitigate | Forbid restart/apply/delete commands in preflight. |
| T-22-01-03 | Tampering | STATE/ROADMAP | medium | mitigate | Add only Phase 22 notes; preserve Phase 21 context. |
</threat_model>

<verification>
<automated>bash -n scripts/k3s-router-preflight.sh</automated>
<automated>rg -n "Podman continua sendo o rollback|router-ai-atius|DiskPressure|image filesystem|local-path|IngressClass" docs/K3S-MIGRATION.md</automated>
<automated>rg -n "Phase 22 added: k3s migration preflight|Podman remains the current production source of truth" .planning/STATE.md</automated>
</verification>

<success_criteria>
- Migration has a written preflight/runbook before any manifest apply.
- Preflight script is read-only and shell-valid.
- State captures Phase 22 without losing Phase 21.
</success_criteria>

<output>
Create `.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-01-SUMMARY.md` when done.
</output>
