---
phase: 25-embedding-governor-auto-workload-inference
plan: "03"
type: execute
wave: 3
depends_on:
  - 25-01
  - 25-02
files_modified:
  - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
  - scripts/smoke-embeddings.py
  - tests/test_clianything.py
autonomous: true
requirements:
  - PHASE-25-GOVERNED-MODEL-SCOPE
  - PHASE-25-AUTO-WORKLOAD-INFERENCE
  - PHASE-25-HEADER-OVERRIDE-COMPATIBILITY
  - PHASE-25-TEI-BATCH-SAFETY
  - PHASE-25-CLIENT-SMOKE-VALIDATION
user_setup:
  - service: router-ai-atius
    why: "Authenticated live /v1/embeddings smoke for embedding-gte-v1 dimension 768"
    env_vars:
      - name: ATIUS_ROUTER_TOKEN
        source: "Secure runtime secret store or ephemeral shell export; never print, commit, or paste the value"
must_haves:
  truths:
    - "D-01: docs and smoke defaults use only public governed alias embedding-gte-v1."
    - "D-02: docs state X-Embedding-Workload is optional and override-only."
    - "D-03: smoke can exercise no-header single and array requests through metadata inference."
    - "D-04: docs state arrays over 4 fail closed for governed TEI safety."
    - "D-05: smoke validates dimension 768 without printing tokens."
  artifacts:
    - "scripts/smoke-embeddings.py defaults to local Go router, embedding-gte-v1, and dimension 768."
    - "tests/test_clianything.py covers smoke payload array support and redaction."
    - "docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md documents Phase 25 envs, threshold, header semantics, cap, and validation commands."
  key_links:
    - "Operator docs -> smoke script -> authenticated /v1/embeddings validation prove client-facing behavior."
    - "Smoke defaults align with Phase 24 runtime baseline and Phase 25 classifier behavior."
---

<objective>
Update operational documentation and smoke tooling for the Phase 25 client contract.

Purpose: Make the new default safe for Graphify, GBrain, and normal clients that omit `X-Embedding-Workload`, while preserving token hygiene for runtime validation.
Output: Smoke defaults for `embedding-gte-v1`, docs for automatic workload inference, and conditional authenticated smoke commands.
</objective>

## Artifacts this phase produces

- `service/embeddinggovernor.Config.AutoWorkload bool`
- `EMBEDDING_GOVERNOR_AUTO_WORKLOAD=true`
- `defaultBatchInputCountThreshold = 2`
- `EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD=2`
- `(*Governor).IsGovernedModel(model string) bool`
- `embeddinggovernor.IsGovernedModel(model string) bool`
- `(*Governor).ClassifyWorkload(req Request) string` returning `batch` or `interactive`
- `relay.maxGovernedTEIInputCount = 4`
- `scripts/smoke-embeddings.py` defaults for `embedding-gte-v1`, `http://127.0.0.1:3000/v1`, and dimension `768`
- `ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE=single|array`
- Focused tests in `service/embeddinggovernor/governor_test.go`, `relay/embedding_handler_test.go`, and `tests/test_clianything.py`
- Operator docs in `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-RESEARCH.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-01-SUMMARY.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-02-SUMMARY.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-04-SUMMARY.md
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@scripts/smoke-embeddings.py
@tests/test_clianything.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update embeddings smoke defaults and array-mode coverage</name>
  <files>scripts/smoke-embeddings.py, tests/test_clianything.py</files>
  <read_first>
    @scripts/smoke-embeddings.py
    @tests/test_clianything.py
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-RESEARCH.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
  </read_first>
  <behavior>
    - Missing `ATIUS_ROUTER_TOKEN` still exits `2` before network access and prints no token value per D-05.
    - Default base URL is `http://127.0.0.1:3000/v1`, default model is `embedding-gte-v1`, and default expected dimension is `768` per D-01 and D-05.
    - Default smoke request does not send `X-Embedding-Workload`; header override remains an operator option outside the default request per D-02 and D-03.
    - `ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE=array` sends a small two-item array to exercise automatic batch inference without a public `*-batch` alias per D-01 and D-03.
    - Successful array smoke validates response row count and vector dimension without printing secrets per D-05.
  </behavior>
  <action>Change `DEFAULT_BASE_URL` to `http://127.0.0.1:3000/v1`, `DEFAULT_MODEL` to `embedding-gte-v1`, and `DEFAULT_EXPECTED_DIM` to `768`. Extend `build_embedding_payload` with an optional `input_items: list[str] | None` argument; when provided, set payload `input` to that list, otherwise keep the existing `input_text` behavior. Add env handling for `ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE` with accepted values `single` and `array`; `array` must build exactly two small strings by default and must not add `X-Embedding-Workload`. Preserve `embo-01` `type` behavior only when `model == "embo-01"`. Validate all returned rows for array mode by checking row count and using `assert_embedding_vector_shape` on each row. Update `tests/test_clianything.py` method `Phase19ProviderRoutingTests.test_smoke_embeddings_helpers_cover_payload_shape_and_redaction` or add adjacent tests to cover `embedding-gte-v1` default payload shape, array payload shape, dimension helper behavior, and `_scrub` redaction.</action>
  <acceptance_criteria>Smoke tooling targets the governed local embedding by default, can exercise no-header array inference, and keeps redaction behavior covered by tests.</acceptance_criteria>
  <verify>
    <automated>python3 -m py_compile scripts/smoke-embeddings.py</automated>
    <automated>python3 -m unittest tests.test_clianything.Phase19ProviderRoutingTests.test_smoke_embeddings_helpers_cover_payload_shape_and_redaction -v</automated>
    <automated>env -u ATIUS_ROUTER_TOKEN python3 scripts/smoke-embeddings.py; test "$?" -eq 2</automated>
  </verify>
  <done>Embedding smoke defaults and helper tests match the Phase 25 client-facing contract.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 25 governor defaults, header override, cap, and smokes</name>
  <files>docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</files>
  <read_first>
    @docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-RESEARCH.md
    @.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-04-SUMMARY.md
  </read_first>
  <action>Update the `Governor de embeddings Go-native` section so it states `X-Embedding-Workload` is optional for normal clients and override-only for operators per D-02. Replace the documented count threshold with `InputCount >= 2` and add env lines `EMBEDDING_GOVERNOR_AUTO_WORKLOAD=true` and `EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD=2` near the existing governor env block per D-03. Document the Phase 25 fail-closed choice for governed local TEI arrays larger than `4`, including that this is independent of header value and exists because no safe transparent response recomposition pattern exists in the current relay per D-04. Update validation commands to use `/usr/local/go/bin/go`, `embedding-gte-v1`, dimension `768`, `ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE=array` for the no-header batch-classifier smoke, and `ATIUS_ROUTER_TOKEN` only as an ephemeral env var from a secure source per D-05. Preserve all fork guardrails: no Python/model-detailed owner, no sidecar/container, no public `*-batch` alias, no secrets in docs, and no protected identity or branding removal.</action>
  <acceptance_criteria>Manual docs match the implemented defaults, explain header compatibility, name the fail-closed TEI cap, and provide token-safe smoke commands.</acceptance_criteria>
  <verify>
    <automated>rg -n 'EMBEDDING_GOVERNOR_AUTO_WORKLOAD=true|EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD=2|InputCount >= 2|ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE=array|embedding-gte-v1|768' docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</automated>
    <automated>rg -n 'model-detailed|sidecar|\\*-batch|ATIUS_ROUTER_TOKEN' docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</automated>
  </verify>
  <done>Operator docs are aligned with Phase 25 behavior and token hygiene.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 25 | Govern `embedding-gte-v1`, infer batch vs interactive without required header, preserve override, avoid public batch alias, keep TEI safe | 25-01, 25-02, 25-03 | COVERED | Goal is split across service classifier, relay cap, and docs/smoke.
REQ | PHASE-25-GOVERNED-MODEL-SCOPE | Single default public governed local embedding alias and unknown no-op | 25-01, 25-02, 25-03 | COVERED | D-01 cited in actions and tests.
REQ | PHASE-25-AUTO-WORKLOAD-INFERENCE | Header priority, count threshold 2, char threshold, auto workload env, metadata-only classifier | 25-01, 25-02, 25-03 | COVERED | D-03 cited in service and smoke tasks.
REQ | PHASE-25-HEADER-OVERRIDE-COMPATIBILITY | batch/bulk and interactive/realtime override, invalid fallback, docs optional header | 25-01, 25-02, 25-03 | COVERED | D-02 cited in all plans.
REQ | PHASE-25-TEI-BATCH-SAFETY | Conservative concurrency, split accounting, max automatic 3, arrays over 4 respect TEI cap | 25-01, 25-02, 25-03 | COVERED | D-04 implemented with fail-closed relay cap.
REQ | PHASE-25-CLIENT-SMOKE-VALIDATION | Service/relay tests, docs/smoke no-header operation, authenticated dimension 768 smoke | 25-01, 25-02, 25-03 | COVERED | D-05 covered by automated tests and conditional smoke.
RESEARCH | R-01 | Current threshold is 4 and must become 2 | 25-01, 25-03 | COVERED | Code and docs update threshold.
RESEARCH | R-02 | `EMBEDDING_GOVERNOR_AUTO_WORKLOAD` is missing | 25-01, 25-03 | COVERED | Config and docs add env.
RESEARCH | R-03 | Smoke defaults are stale: 3001, embo-01, 1536 | 25-03 | COVERED | Smoke defaults changed to 3000, embedding-gte-v1, 768.
RESEARCH | R-04 | No safe transparent TEI sub-batching/recomposition analog exists | 25-02, 25-03 | COVERED | Plan chooses fail-closed cap of 4.
CONTEXT | D-01 | Governed model scope | 25-01, 25-02, 25-03 | COVERED | No public batch alias and unknown no-op preserved.
CONTEXT | D-02 | Header override priority | 25-01, 25-02, 25-03 | COVERED | Explicit overrides remain highest priority.
CONTEXT | D-03 | Automatic workload inference | 25-01, 25-02, 25-03 | COVERED | Metadata-only classifier and no-header smoke.
CONTEXT | D-04 | TEI batch safety | 25-01, 25-02, 25-03 | COVERED | Concurrency preserved and cap enforced.
CONTEXT | D-05 | Validation and smoke | 25-01, 25-02, 25-03 | COVERED | Service, relay, smoke helper, docs, conditional live smoke.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> smoke script | `ATIUS_ROUTER_TOKEN` and smoke env vars enter a local Python script. |
| docs -> operator actions | Documentation drives runtime validation commands and secret handling. |
| smoke script -> router API | Authenticated request crosses into `/v1/embeddings`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-07 | Information Disclosure | `scripts/smoke-embeddings.py` | high | mitigate | Preserve `_scrub`, keep missing-token exit before network, never print Authorization or token env values per D-05. |
| T-25-08 | Tampering | operator docs | medium | mitigate | Document header as optional override and document fail-closed cap so operators do not create public `*-batch` aliases or bypass governor per D-01, D-02, and D-04. |
| T-25-09 | Repudiation | smoke output | low | accept | Smoke output reports model, type, dimension, and row validation only; request identity remains governed by existing auth logs outside this phase. |
| T-25-SC | Tampering | package installs | low | accept | No npm, pip, or cargo install is part of this plan; Python smoke uses stdlib only. |
</threat_model>

<verification>
Plan-level automated checks:

<automated>/usr/local/go/bin/go test ./service/embeddinggovernor ./dto ./relay -count=1</automated>
<automated>python3 -m py_compile scripts/smoke-embeddings.py</automated>
<automated>python3 -m unittest tests.test_clianything.Phase19ProviderRoutingTests.test_smoke_embeddings_helpers_cover_payload_shape_and_redaction -v</automated>
<automated>env -u ATIUS_ROUTER_TOKEN python3 scripts/smoke-embeddings.py; test "$?" -eq 2</automated>

Manual/conditional live smoke, only when `ATIUS_ROUTER_TOKEN` is already exported in the shell from a secure source:

<manual-check>ATIUS_ROUTER_EMBEDDINGS_BASE_URL=http://127.0.0.1:3000/v1 ATIUS_ROUTER_EMBEDDINGS_MODEL=embedding-gte-v1 ATIUS_ROUTER_EXPECT_EMBEDDING_DIM=768 python3 scripts/smoke-embeddings.py</manual-check>
<manual-check>ATIUS_ROUTER_EMBEDDINGS_BASE_URL=http://127.0.0.1:3000/v1 ATIUS_ROUTER_EMBEDDINGS_MODEL=embedding-gte-v1 ATIUS_ROUTER_EXPECT_EMBEDDING_DIM=768 ATIUS_ROUTER_EMBEDDINGS_INPUT_MODE=array python3 scripts/smoke-embeddings.py</manual-check>

Do not print, echo, commit, or paste the token value. If the token is absent, record the authenticated smoke as blocked by missing setup and keep the no-token exit-2 check as the automated safety gate.
</verification>

<success_criteria>
- Smoke defaults and docs target `embedding-gte-v1` and dimension `768`.
- Docs and smoke prove clients can omit `X-Embedding-Workload` for normal operation.
- Header override remains documented for operational steering only.
- Governed TEI cap of 4 is documented as fail-closed.
- Full phase coverage audit has no missing GOAL, REQ, RESEARCH, or CONTEXT items.
</success_criteria>

<output>
Create `.planning/phases/25-embedding-governor-auto-workload-inference/25-03-SUMMARY.md` when done.
</output>
