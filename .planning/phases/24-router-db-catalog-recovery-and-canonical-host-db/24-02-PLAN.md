---
phase: 24-router-db-catalog-recovery-and-canonical-host-db
plan: "02"
type: execute
wave: 2
depends_on:
  - "24-01"
files_modified:
  - scripts/phase24-build-canonical-db.sh
  - scripts/phase24-catalog-transform.sql
  - docs/ROUTER-DB-RECOVERY.md
autonomous: true
requirements:
  - PHASE-24-CANONICAL-HOST-DB
  - PHASE-24-CATALOG-RESTORE
  - PHASE-24-CUTOVER-ROLLBACK
must_haves:
  truths:
    - D-04 The final DB name is canonical and host-based, not an accidental drift artifact.
    - D-05 Catalog rows are transformed before restore; they are not replayed blindly.
    - D-06 Old `newapi` remains available for rollback until cutover passes.
  artifacts:
    - scripts/phase24-build-canonical-db.sh
    - scripts/phase24-catalog-transform.sql
  key_links:
    - fresh newapi dump -> candidate DB -> transformed catalog restore
    - PgBouncer mapping -> router DSN target -> rollback holdback
---

<objective>
Create the candidate canonical DB and transform/restore the router catalog safely.

Purpose: build the final DB target without destroying `newapi`, and restore only the allowed router catalog rows.
</objective>

## Artifacts this plan produces

- `scripts/phase24-build-canonical-db.sh`
- `scripts/phase24-catalog-transform.sql`
- additions to `docs/ROUTER-DB-RECOVERY.md`

<context>
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-CONTEXT.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-RESEARCH.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-PATTERNS.md
@backups/clianything/20260701_184735_channels.sql
@backups/clianything/20260701_184735_models.sql
@backups/clianything/20260701_184735_abilities.sql
@/home/ubuntu/.backups/router-ai-atius-incident-20260703T231027-0300/newapi-before.fix.dump
@tools/clianything.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create candidate DB build script</name>
  <files>scripts/phase24-build-canonical-db.sh</files>
  <action>
    Create a controlled shell script that documents and automates the candidate DB build flow: fresh dump from live `newapi`, creation of `DBRouterAiAtius`, restore of the fresh dump into the candidate DB, PgBouncer mapping check, and verification queries. The script may execute writes in the future execution phase, but for planning it must be explicit, parameterized, and guarded with dry-run/confirm structure.
  </action>
  <acceptance_criteria>
    - Script references `newapi` as source and `DBRouterAiAtius` as target.
    - Script contains a preflight/confirm guard before DB mutation.
    - Script contains `pg_restore -l` verification before restore.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "newapi|DBRouterAiAtius|pg_restore -l|confirm|dry-run" scripts/phase24-build-canonical-db.sh</automated>
  </verify>
  <done>Candidate DB build path is explicit and reviewable.</done>
</task>

<task type="auto">
  <name>Task 2: Create transformed catalog SQL</name>
  <files>scripts/phase24-catalog-transform.sql</files>
  <action>
    Create SQL or SQL-generation output that restores the 2026-07-01 router catalog into the candidate DB with the required transforms:
    - include `OpenAI - Codex`
    - include DeepSeek flash/pro
    - include MiniMax consolidated channel/models but final disabled
    - preserve `embedding-gte-v1`
    - exclude `gpt-5.4-1m`, `gpt-5.5-1m`
    - exclude `text-embedding-3-small`, `text-embedding-3-large`
    - exclude `channel 5` `-1m` model_mapping entries
  </action>
  <acceptance_criteria>
    - SQL contains `OpenAI - Codex`.
    - SQL contains `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.3-codex-spark`.
    - SQL does not contain `gpt-5.4-1m` or `gpt-5.5-1m`.
    - SQL does not contain `text-embedding-3-small` or `text-embedding-3-large`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "OpenAI - Codex|gpt-5.5|gpt-5.4|gpt-5.4-mini|gpt-5.3-codex-spark" scripts/phase24-catalog-transform.sql</automated>
    <automated>! rg -n "gpt-5.4-1m|gpt-5.5-1m|text-embedding-3-small|text-embedding-3-large" scripts/phase24-catalog-transform.sql</automated>
  </verify>
  <done>Catalog restore source is transformed to the desired final state.</done>
</task>

<task type="auto">
  <name>Task 3: Document rollback holdback</name>
  <files>docs/ROUTER-DB-RECOVERY.md</files>
  <action>
    Extend the runbook with an explicit holdback rule: `newapi` stays untouched and still mapped in PgBouncer until all Phase 24 verification gates pass, and there is no drop/rename of `newapi` in this phase.
  </action>
  <acceptance_criteria>
    - The runbook contains the exact phrase `newapi permanece intacto para rollback`.
    - The runbook contains the exact phrase `não fazer DROP nem rename destrutivo de newapi nesta fase`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "newapi permanece intacto para rollback|não fazer DROP nem rename destrutivo de newapi nesta fase" docs/ROUTER-DB-RECOVERY.md</automated>
  </verify>
  <done>Rollback safety is explicit before cutover.</done>
</task>

</tasks>

<verification>
<automated>rg -n "newapi|DBRouterAiAtius|pg_restore -l" scripts/phase24-build-canonical-db.sh</automated>
<automated>! rg -n "gpt-5.4-1m|gpt-5.5-1m|text-embedding-3-small|text-embedding-3-large" scripts/phase24-catalog-transform.sql</automated>
</verification>

<success_criteria>
- Candidate DB creation path is explicit.
- Catalog transformation is explicit and matches the requested exclusions.
- Rollback holdback for `newapi` is documented.
</success_criteria>

<output>
Create `.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-02-SUMMARY.md` when done.
</output>
