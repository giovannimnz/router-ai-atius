---
phase: 33-reranker-reliability-observability-and-readiness
plan: "14"
type: execute
wave: 7
depends_on:
  - "33-13"
files_modified:
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
autonomous: true
requirements:
  - PHASE-33-WEIGHTED-DASHBOARD
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "A candidate image is built only after Router and Omni guard suites pass under the host CPU limit."
    - "The candidate carries source-revision and governed-reranker labels and is recorded by immutable digest."
    - "The candidate passes isolated readiness and rerank checks without changing the live service, tag, image, or unit."
    - "Exact schema-v2 weighted formulas, all seven counters, both zero-denominator null cases, and the 8/9 incident are proven against disposable candidate-only storage without waiting for the live 24-hour window."
  artifacts:
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md
      provides: "Sanitized preflight, immutable candidate digest, isolated smoke results, and unchanged-live proof"
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
      provides: "Machine-readable candidate/source/live-before identities plus isolated schema-v2 metrics proof consumed by direct verification and promotion"
  key_links:
    - from: /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
      to: scripts/atius-user-quota-guard.sh
      via: "canonical remote/main preflight and candidate-only release mode before build"
    - from: scripts/podman-admin.sh
      to: .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md
      via: "resource-capped build and isolated candidate execution"
---

<objective>
Produce and validate an immutable Phase 33 candidate without touching the live router.

Purpose: Prove that the exact candidate intended for promotion contains the governed reranker contract, survives fork-sync checks, and works in isolation before any production mutation.
Output: A sanitized candidate evidence record containing source identity, image digest, guard/test results, isolated readiness/rerank checks, and proof that live state remained unchanged.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-13-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
@scripts/podman-admin.sh
@scripts/atius-user-quota-guard.sh
@scripts/smoke-reranker-readiness.py
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the fail-closed source and test preflight</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-13-SUMMARY.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/scripts/release-preflight.sh
    - scripts/podman-admin.sh
    - scripts/atius-user-quota-guard.sh
    - patches/atius-user-quota-unlimited.patch
  </read_first>
  <action>Read `33-OMNI-INTEGRATION.json` and require `worktree_path` to equal `/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified`. Validate that retained worktree exists, is registered, has empty porcelain status, and `HEAD == expected_commit == origin_main_commit` from the artifact. Run every Omni test/script using an absolute path beneath that retained worktree; never invoke a script from the dirty shared `/home/ubuntu/GitHub/omni-srv-admin` checkout or the editable feature worktree. Treat `cli/omni` as a Python package directory: require `cli/omni/__main__.py`, prove the pinned entrypoint with `PYTHONPATH="$omni/cli" python3 -m omni --help`, and invoke the governed test command through `PYTHONPATH="$omni/cli" python3 -m omni ...`. Run its real `modules/fork-sync/cli/tests/test_release_preflight.py`, `modules/fork-sync/bin/deploy.sh --dry-run`, and `scripts/release-preflight.sh` against Router source and directly inspect its protected paths/deploy modes. Execute Router tests through `./scripts/podman-admin.sh profile-run`, run the user-quota guard, and assert exact governor source config. Stop before build on any identity, cleanliness, entrypoint, test, quota, source capability, or protected-path failure. Create sanitized candidate Markdown/JSON evidence containing expected Omni commit, retained worktree path, Router commit and old live immutable digest. Before any candidate build, create a sorted manifest of every immutable Phase 33 planning artifact (`33-*-PLAN.md`, `33-CONTEXT.md`, `33-RESEARCH.md`, `33-UI-SPEC.md`, and `33-VALIDATION.md`, excluding runtime evidence and summaries), hash each file, hash the canonical compact JSON manifest, and store both the manifest and exact `planning_identity` formed by the `sha256:` prefix plus 64 lowercase hexadecimal digits in the authoritative candidate JSON. Retain the verified worktree unchanged through Plans 33-14/33-15.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json; ev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json; omni=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified"; assert d["expected_commit"]==d["origin_main_commit"]; print(d["worktree_path"])'"'"' "$manifest"); expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest"); test "$(git -C "$omni" rev-parse HEAD)" = "$expected"; test "$(git -C "$omni" status --porcelain | wc -l)" -eq 0; for f in modules/fork-sync/projects/atius-router/sync.yaml modules/fork-sync/cli/fork_sync/core/release_preflight.py modules/fork-sync/bin/deploy.sh cli/omni/__main__.py; do git -C "$omni" cat-file -e "HEAD:$f"; done; PYTHONPATH="$omni/cli" python3 -m omni --help &gt;/dev/null; PYTHONPATH="$omni/cli" python3 -m omni srv1-ops resources run builds -- python3 -m pytest "$omni/modules/fork-sync/cli/tests/test_release_preflight.py" -q; "$omni/modules/fork-sync/bin/deploy.sh" atius-router "$PWD" --dry-run; "$omni/scripts/release-preflight.sh" "$PWD" --mode fork-deploy --deploy-config "$omni/modules/fork-sync/projects/atius-router/deploy.yaml"; ./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./pkg/perf_metrics ./model ./middleware ./controller ./relay ./relay/channel ./relay/channel/advancedcustom ./service/embeddinggovernor -count=1; bash scripts/atius-user-quota-guard.sh; test -s "$ev"; python3 -c '"'"'import hashlib,json,re,sys; from pathlib import Path; ev=Path(sys.argv[1]); phase=Path(".planning/phases/33-reranker-reliability-observability-and-readiness"); paths=sorted([*phase.glob("33-*-PLAN.md"),phase/"33-CONTEXT.md",phase/"33-RESEARCH.md",phase/"33-UI-SPEC.md",phase/"33-VALIDATION.md"],key=lambda p:p.as_posix()); assert len(paths)==20 and all(p.is_file() for p in paths); entries=[{"path":p.as_posix(),"sha256":hashlib.sha256(p.read_bytes()).hexdigest()} for p in paths]; identity="sha256:"+hashlib.sha256(json.dumps(entries,sort_keys=True,separators=(",",":")).encode()).hexdigest(); assert re.fullmatch(r"sha256:[0-9a-f]{64}",identity); d=json.load(open(ev)); d["planning_identity_manifest"]=entries; d["planning_identity"]=identity; open(ev,"w").write(json.dumps(d,indent=2,sort_keys=True)+"\n")'"'"' "$ev"'</automated>
  </verify>
  <acceptance_criteria>
    - Canonical Omni origin/main, not only the feature worktree, contains byte-verified Phase 33 protection and release modes.
    - Retained Omni worktree is clean and HEAD exactly equals the expected integration commit before every script invocation.
    - Router focused suites, exact source config, protected capability files, and unlimited-token guard pass before build.
    - JSON evidence records canonical commits, old live immutable digest, the exact sorted immutable-plan manifest, and its deterministic planning identity without secrets.
  </acceptance_criteria>
  <done>All Router and Omni guards/tests pass under governed CPU containment, the unlimited-token invariant remains protected, and the candidate evidence file records a PASS before any image build.</done>
</task>

<task type="auto">
  <name>Task 2: Build and exercise the immutable candidate in isolation</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-12-SUMMARY.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-13-SUMMARY.md
    - scripts/podman-admin.sh
    - scripts/smoke-reranker-readiness.py
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
  </read_first>
  <action>Revalidate retained-worktree path, clean status, and `HEAD == expected_commit` from `33-OMNI-INTEGRATION.json`, then invoke its absolute `modules/fork-sync/bin/deploy.sh ... --candidate-only --candidate-digest-file CANDIDATE_JSON_PATH`. Read the immutable candidate digest, independently inspect image labels/source commit/protected carriers, and require the candidate OCI `org.opencontainers.image.revision` label to be a nonempty source revision. Store that exact label as `execution_identity` in the authoritative candidate JSON; no current/latest label or independently chosen commit may substitute. Invoke the same retained worktree's absolute deploy script with `--verify-candidate DIGEST`. That verification inspects exact runtime governor aliases, authenticated channel10/model/ability/route/cache readiness, and functional rerank 20/21 boundary. Compare live digest/unit/config after candidate cleanup with captured pre-build state. Append sanitized direct results only. Never invoke Omni scripts from either dirty shared checkout or editable worktree, and retain the clean verified worktree for Plan 33-15.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; ev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json; manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json; omni=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["worktree_path"])'"'"' "$manifest"); expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest"); test "$omni" = /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified; test "$(git -C "$omni" rev-parse HEAD)" = "$expected"; test "$(git -C "$omni" status --porcelain | wc -l)" -eq 0; deploy="$omni/modules/fork-sync/bin/deploy.sh"; candidate=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["candidate_digest"])'"'"' "$ev"); source=$(podman image inspect "$candidate" --format '"'"'{{ index .Labels "org.opencontainers.image.revision" }}'"'"'); test -n "$source"; test "$(podman image inspect "$candidate" --format '"'"'{{ index .Labels "com.atius.router.reranker-governed" }}'"'"')" = true; for f in relay/channel/advancedcustom/tei_rerank.go relay/rerank_handler.go relay/embedding_handler_test.go dto/channel_settings_tei_rerank_test.go k8s/router-ai-atius/configmap.yaml; do git cat-file -e "$source:$f"; done; python3 -c '"'"'import json,sys; p,value=sys.argv[1:]; assert value and value not in ("current","latest"); d=json.load(open(p)); d["execution_identity"]=value; open(p,"w").write(json.dumps(d,indent=2,sort_keys=True)+"\n")'"'"' "$ev" "$source"; test "$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["execution_identity"])'"'"' "$ev")" = "$source"; "$deploy" atius-router "$PWD" --verify-candidate "$candidate"; live_before=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["live_before_digest"])'"'"' "$ev"); test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$live_before"'</automated>
  </verify>
  <acceptance_criteria>
    - Direct image/source inspection binds candidate digest, revision label, capability label, and every protected source carrier.
    - Direct isolated runtime inspection proves the exact governor model list and authenticated channel10/alias/ability readiness.
    - Candidate functional rerank and 20/21 boundary pass, while the live digest remains exactly unchanged.
  </acceptance_criteria>
  <done>An immutable, correctly labeled candidate passes isolated readiness/rerank validation, its digest is recorded, secrets are absent from evidence, and production state is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Prove schema-v2 metrics on disposable candidate storage</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-05-SUMMARY.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-11-SUMMARY.md
    - model/perf_metric.go
    - pkg/perf_metrics/types.go
    - pkg/perf_metrics/metrics.go
    - scripts/podman-admin.sh
  </read_first>
  <action>Run the recorded immutable candidate in a separate resource-capped container with a unique loopback port, `SQL_DSN` unset, Redis disabled, and `SQLITE_PATH` bound only to a directory created by `mktemp -d`. Never point this proof at the production PostgreSQL/PgBouncer DSN, production Redis, production group, live container, or live metrics API. Let the candidate create the disposable SQLite schema, stop it, and seed only synthetic aggregate rows in one uniquely named isolated group: one model with the incident fixture `8 success + 1 client_validation = 9`, and a second model whose nonzero governor, routing, transport, upstream-HTTP, and invalid-response counters make every one of the seven terminal counters nonzero in the overall totals. Restart the same immutable digest against that SQLite file and query a one-hour window scoped to the isolated group. First query the empty database and prove both request-success and service-availability zero denominators return null/unavailable. Then seed the two v2-only rows and prove schema 2, boolean completeness markers, every per-row seven-counter sum, model-to-overall terminal/counter totals, request-success formula, service-availability denominator/formula, the incident model's `88.89%` request success and `100.00%` service availability, and absence of NaN/Infinity. Do not infer or backfill legacy outcomes, wait for 24 hours, write a production bucket, or retain the synthetic access token/DB.

Install one EXIT finalizer before creating isolated artifacts. It captures the original exit status, disables every trap, enters controlled `set +e`, and captures every cleanup/proof command status. For `podman container exists`, accept only rc 0 as present and removable and only rc 1 as confirmed absent; any other rc, including 125, is cleanup failure. After any removal attempt, run a second exists check and require exactly rc 1. Apply the same fail-closed distinction to SQLite query, filesystem-absence, and live-inspection tools: a tool/read error is never absence. Delete the isolated group from the disposable DB and prove its row count is zero, remove the SQLite/data directory and each standalone `mktemp` response file, and prove container/dir/file absence. Record every cleanup step status. Write `cleanup_status=pass` only after all absence proofs succeed and only when the original body status was zero; otherwise write a sanitized failed/pending cleanup record when possible and exit nonzero. Cleanup errors never overwrite or swallow the original failure. Recheck that the production live digest never changed.</action>
  <verify>
    <automated>bash -lc 'set -Eeuo pipefail
ev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
candidate=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["candidate_digest"])'"'"' "$ev")
live_before=$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')
proof_dir=""; empty_file=""; metrics_file=""; db=""; proof_group="phase33-v2-only-$$"; ctr="phase33-v2-metrics-proof-$$"
session_secret=$(python3 -c '"'"'import secrets; print(secrets.token_hex(32))'"'"')
access_token=$(python3 -c '"'"'import secrets; print(secrets.token_hex(16))'"'"')
cleanup_and_exit(){
  original_rc=$?
  trap - EXIT ERR INT TERM
  set +e
  podman container exists "$ctr" &gt;/dev/null 2&gt;&amp;1
  exists_before_rc=$?
  case "$exists_before_rc" in
    0) podman rm -f "$ctr" &gt;/dev/null 2&gt;&amp;1; cr=$? ;;
    1) cr=0 ;;
    *) cr=$exists_before_rc ;;
  esac
  podman container exists "$ctr" &gt;/dev/null 2&gt;&amp;1
  exists_after_rc=$?
  case "$exists_after_rc" in
    1) ca=0 ;;
    0) ca=1 ;;
    *) ca=$exists_after_rc ;;
  esac
  gd=0; ga=0
  if [ -n "$db" ] &amp;&amp; [ -f "$db" ]; then
    sqlite3 "$db" "DELETE FROM perf_metrics WHERE \"group\"='"'"'$proof_group'"'"'; DELETE FROM users WHERE \"group\"='"'"'$proof_group'"'"';" &gt;/dev/null 2&gt;&amp;1; gd=$?
    left=$(sqlite3 "$db" "SELECT (SELECT COUNT(*) FROM perf_metrics WHERE \"group\"='"'"'$proof_group'"'"')+(SELECT COUNT(*) FROM users WHERE \"group\"='"'"'$proof_group'"'"');" 2&gt;/dev/null); qr=$?
    if [ "$qr" -eq 0 ] &amp;&amp; [ "$left" = 0 ]; then ga=0; elif [ "$qr" -ne 0 ]; then ga=$qr; else ga=1; fi
  fi
  dr=0; if [ -n "$proof_dir" ]; then case "$proof_dir" in /tmp/tmp.*) rm -rf -- "$proof_dir"; dr=$? ;; *) dr=1 ;; esac; fi
  da=0; if [ -n "$proof_dir" ]; then test ! -e "$proof_dir"; da=$?; fi
  fr=0
  for f in "$empty_file" "$metrics_file"; do
    if [ -n "$f" ]; then case "$f" in /tmp/tmp.*) rm -f -- "$f"; sr=$? ;; *) sr=1 ;; esac; if [ "$sr" -ne 0 ] &amp;&amp; [ "$fr" -eq 0 ]; then fr=$sr; fi; fi
  done
  fa=0; for f in "$empty_file" "$metrics_file"; do if [ -n "$f" ]; then test ! -e "$f"; ar=$?; if [ "$ar" -ne 0 ] &amp;&amp; [ "$fa" -eq 0 ]; then fa=$ar; fi; fi; done
  live_after=$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"' 2&gt;/dev/null); lr=$?
  if [ "$lr" -eq 0 ] &amp;&amp; [ "$live_after" = "$live_before" ]; then lu=0; else lu=1; fi
  cleanup_rc=0
  for sr in "$cr" "$ca" "$gd" "$ga" "$dr" "$da" "$fr" "$fa" "$lu"; do if [ "$sr" -ne 0 ] &amp;&amp; [ "$cleanup_rc" -eq 0 ]; then cleanup_rc=$sr; fi; done
  if [ "$original_rc" -eq 0 ] &amp;&amp; [ "$cleanup_rc" -eq 0 ]; then cleanup_status=pass; else cleanup_status=failed; fi
  python3 -c '"'"'import json,sys; path,status=sys.argv[1:3]; names=("container_remove","container_absent","group_delete","group_absent","data_dir_remove","data_dir_absent","response_files_remove","response_files_absent","live_digest_unchanged"); vals=map(int,sys.argv[3:]); d=json.load(open(path)); p=d.setdefault("v2_metrics_proof",{}); p["cleanup_status"]=status; p["cleanup_steps"]={k:("pass" if v==0 else "failed") for k,v in zip(names,vals)}; open(path,"w").write(json.dumps(d,indent=2,sort_keys=True)+"\n")'"'"' "$ev" "$cleanup_status" "$cr" "$ca" "$gd" "$ga" "$dr" "$da" "$fr" "$fa" "$lu"
  er=$?; if [ "$er" -ne 0 ] &amp;&amp; [ "$cleanup_rc" -eq 0 ]; then cleanup_rc=$er; fi
  final_rc=$original_rc; if [ "$final_rc" -eq 0 ]; then final_rc=$cleanup_rc; fi
  exit "$final_rc"
}
trap cleanup_and_exit EXIT
trap "exit 130" INT
trap "exit 143" TERM
proof_dir=$(mktemp -d); db="$proof_dir/phase33-proof.db"
start_candidate(){
  ./scripts/podman-admin.sh run-container -d --name "$ctr" --pull=never -p 127.0.0.1::3000 --unsetenv SQL_DSN -e SQLITE_PATH=/data/phase33-proof.db -e REDIS_CONN_STRING= -e SESSION_SECRET="$session_secret" -v "$proof_dir:/data:Z" "$candidate" &gt;/dev/null
  port=$(podman port "$ctr" 3000/tcp | sed -n '"'"'s/.*://p'"'"' | tail -1); test -n "$port"; base="http://127.0.0.1:$port"
  for _ in $(seq 1 30); do curl -fsS "$base/api/status" &gt;/dev/null &amp;&amp; return 0; sleep 1; done; return 1
}
start_candidate
empty_file=$(mktemp)
curl -fsS "$base/api/perf-metrics/summary?hours=1&amp;group=$proof_group" -o "$empty_file"
python3 -c '"'"'import json,sys,math; d=json.load(open(sys.argv[1]))["data"]; o=d["overall"]; assert d["schema_version"]==2 and o.get("request_success_rate") is None and o.get("service_availability") is None; assert not any(isinstance(o.get(k),float) and (math.isnan(o[k]) or math.isinf(o[k])) for k in ("request_success_rate","service_availability"))'"'"' "$empty_file"
podman rm -f "$ctr" &gt;/dev/null
for col in client_validation_count governor_rejection_count routing_unavailable_count transport_timeout_count upstream_http_failure_count invalid_upstream_response_count latency_sample_count outcome_schema_version; do sqlite3 "$db" "SELECT 1 FROM pragma_table_info('"'"'perf_metrics'"'"') WHERE name='"'"'$col'"'"';" | rg -x 1; done
sqlite3 "$db" "INSERT INTO users (id,username,password,display_name,role,status,access_token,quota,\"group\",created_at) VALUES (1,'"'"'phase33proof'"'"','"'"'synthetic-not-used'"'"','"'"'Phase 33 Proof'"'"',100,1,'"'"'$access_token'"'"',1,'"'"'$proof_group'"'"',strftime('"'"'%s'"'"','"'"'now'"'"')); INSERT INTO perf_metrics (model_name,\"group\",bucket_ts,request_count,success_count,client_validation_count,governor_rejection_count,routing_unavailable_count,transport_timeout_count,upstream_http_failure_count,invalid_upstream_response_count,total_latency_ms,latency_sample_count,output_tokens,generation_ms,outcome_schema_version) VALUES ('"'"'phase33-incident-8-of-9'"'"','"'"'$proof_group'"'"',(strftime('"'"'%s'"'"','"'"'now'"'"')/3600)*3600,9,8,1,0,0,0,0,0,800,8,80,8000,2),('"'"'phase33-all-outcomes'"'"','"'"'$proof_group'"'"',(strftime('"'"'%s'"'"','"'"'now'"'"')/3600)*3600,9,3,0,2,1,1,1,1,300,3,30,3000,2);"
start_candidate
metrics_file=$(mktemp)
curl -fsS -H "Authorization: $access_token" -H "New-Api-User: 1" "$base/api/perf-metrics/summary?hours=1&amp;group=$proof_group" -o "$metrics_file"
python3 -c '"'"'import json,sys,math; d=json.load(open(sys.argv[1]))["data"]; keys=("success_count","client_validation_count","governor_rejection_count","routing_unavailable_count","transport_timeout_count","upstream_http_failure_count","invalid_upstream_response_count"); rows=d["models"]; overall=d["overall"]; terminal=lambda r:r.get("terminal_count",r.get("request_count")); assert d["schema_version"]==2 and len(rows)==2 and overall.get("complete") is True and all(r.get("complete") is True for r in rows); assert all(isinstance(r.get(k),int) and r[k]&gt;=0 for r in [overall,*rows] for k in keys); assert all(sum(r[k] for k in keys)==terminal(r) for r in [overall,*rows]); assert terminal(overall)==sum(terminal(r) for r in rows) and all(overall[k]==sum(r[k] for r in rows) for k in keys); assert all(overall[k]&gt;0 for k in keys); req=overall["success_count"]*100/terminal(overall); den=sum(overall[k] for k in ("success_count","routing_unavailable_count","transport_timeout_count","upstream_http_failure_count","invalid_upstream_response_count")); svc=overall["success_count"]*100/den; close=lambda got,want:isinstance(got,(int,float)) and math.isclose(float(got),want,rel_tol=0,abs_tol=.005); assert close(overall.get("request_success_rate"),req) and close(overall.get("service_availability"),svc); incident=next(r for r in rows if r["model_name"]=="phase33-incident-8-of-9"); incident_den=sum(incident[k] for k in ("success_count","routing_unavailable_count","transport_timeout_count","upstream_http_failure_count","invalid_upstream_response_count")); assert incident["success_count"]==8 and incident["client_validation_count"]==1 and terminal(incident)==9 and close(incident.get("request_success_rate",incident.get("success_rate")),8*100/9) and incident_den==8 and close(incident["success_count"]*100/incident_den,100.0)'"'"' "$metrics_file"
python3 -c '"'"'import json,sys; path=sys.argv[1]; d=json.load(open(path)); d["v2_metrics_proof"]={"storage":"isolated-sqlite","production_mutation":False,"all_seven_counters":True,"per_row_sum_invariant":True,"model_to_overall_totals":True,"request_success_formula":True,"service_availability_formula":True,"zero_denominators_null":True,"incident_8_of_9":True,"cleanup_status":"pending"}; open(path,"w").write(json.dumps(d,indent=2,sort_keys=True)+"\n")'"'"' "$ev"
test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$live_before"'</automated>
  </verify>
  <acceptance_criteria>
    - Empty isolated storage proves request-success and service-availability both remain null/unavailable at zero denominator.
    - The v2-only fixture exercises all seven counters, every row sum invariant, model-to-overall raw totals, both exact weighted formulas, and the 8/9 incident result.
    - The proof uses only the recorded candidate digest and disposable SQLite/group fixtures; production DB, Redis, metrics, container, image reference, and public paths remain unchanged.
    - Synthetic credentials, isolated group, SQLite/data directory, container, and standalone response files are deleted and proven absent on every exit; every cleanup step is recorded.
    - `cleanup_status=pass` is written only after all absence proofs and an originally successful proof body; any body or cleanup failure exits nonzero.
  </acceptance_criteria>
  <done>The candidate itself proves the complete schema-v2 weighted contract immediately and safely, independent of legacy production buckets or a 24-hour wait.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `33-CANDIDATE-EVIDENCE.json` with machine-readable immutable candidate, source, canonical Omni, and live-before identities.
- `33-CANDIDATE-EVIDENCE.md` with sanitized direct preflight/candidate/readiness/rerank results and disposable schema-v2 formula proof.
- An isolated verified candidate image digest eligible for Plan 33-15 promotion; no live mutation.
- The clean verified Omni worktree remains retained and unchanged for all Plan 33-15 gates.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source checkout to image builder | Repository contents become executable container layers. |
| Vault helper to isolated candidate | Secret material enters a transient process environment. |
| test client to isolated API | Untrusted request bodies cross Router validation and relay paths. |
| candidate tooling to live service | Candidate commands share a host with production and must remain isolated. |
| disposable candidate storage to metrics API | Synthetic v2 rows exercise production code while remaining segregated from production storage and telemetry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-14-01 | Tampering | candidate image | high | mitigate | Bind the source revision label to the recorded commit and promote only the immutable digest validated here. |
| T-33-14-02 | Information Disclosure | evidence and test output | high | mitigate | Hydrate secrets at runtime, sanitize output, and negative-scan evidence for credential-shaped content. |
| T-33-14-03 | Denial of Service | build and broad tests | medium | mitigate | Route all heavy work through project/Omni wrappers enforcing the 20% CPU ceiling. |
| T-33-14-04 | Elevation of Privilege | candidate versus production service | high | mitigate | Use a distinct container and loopback port; assert live unit/image/config before and after. |
| T-33-14-05 | Tampering | isolated schema-v2 proof | high | mitigate | Use a fresh SQLite file, fixed aggregate fixtures, exact sum/formula assertions, and unconditional container/storage cleanup. |
| T-33-14-SC | Tampering | package installs | high | mitigate | No new package install is authorized; fail if execution attempts to introduce one without a Package Legitimacy Audit and blocking human verification. |
</threat_model>

<verification>
- Candidate evidence contains a PASS for every source/test guard and isolated smoke case.
- Candidate labels and immutable digest resolve to the source commit under test.
- Disposable candidate-only storage proves all seven counters, raw weighted totals, exact formulas, both null denominators, and the 8/9 incident without touching production metrics.
- Live unit, image, configured reference, and public result are byte-for-byte or identity-equivalent before and after candidate validation.
- Evidence contains no secret values or authorization material.
</verification>

<success_criteria>
- The exact immutable candidate intended for production has passed all Phase 33 guards and isolated functional checks.
- Candidate validation never changes production runtime state.
- Exact formula coverage does not depend on legacy production buckets aging out of the 24-hour window.
- Promotion can consume the recorded candidate and rollback identities without rebuilding.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-14-SUMMARY.md` when done.
</output>
