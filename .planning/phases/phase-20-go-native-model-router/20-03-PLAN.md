---
phase: phase-20-go-native-model-router
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - service/embeddinggovernor/governor.go
  - service/embeddinggovernor/governor_test.go
autonomous: true
requirements:
  - PHASE-20-PYTHON-MIDDLEWARE-REMOVAL
  - PHASE-20-UPSTREAM-SYNC-GUARD
  - PHASE-20-SDK-SMOKES
must_haves:
  truths:
    - Go-native embedding governor remains the only runtime owner for local TEI backpressure.
    - Request-derived classification uses metadata only and preserves explicit workload header priority.
    - Batch latency cannot poison interactive scale-up decisions.
    - Client validation/status errors do not close the adaptive circuit; pressure failures still reduce to min concurrency.
    - Automatic governor concurrency still caps at 3; 4 remains manual/turbo only.
  artifacts:
    - service/embeddinggovernor/governor.go
    - service/embeddinggovernor/governor_test.go
  key_links:
    - relay request metadata -> embeddinggovernor.Request fields
    - embeddinggovernor.Request -> isBatch classification
    - Lease.Finish -> status/latency classifier -> adaptive concurrency state
    - Snapshot -> metadata-only aggregate metrics
---

<objective>
Evolve the active Go-native embedding governor core with metadata-only workload classification, split interactive/batch feedback, and safer pressure-failure semantics.

Purpose: make the current production governor adapt to GBrain/Obsidian catch-up workloads without reviving any retired non-Go runtime owner or weakening TEI safety limits.
Output: tested governor state-machine behavior in `service/embeddinggovernor`.
</objective>

## Artifacts this phase produces

- Governor request metadata fields for input count/coarse size, with no raw embedding text.
- Configured thresholds for metadata-derived batch classification.
- Separate interactive and batch latency/success metrics in governor state/snapshot.
- Status/failure classification that distinguishes client errors from TEI/router pressure.
- Deterministic Go tests using `testify`.

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | phase_goal | Evolve the active Go-native embedding governor with proactive adaptive triggers and better workload classification while preserving the production Go path | 20-03, 20-04, 20-05 | COVERED | Core governor behavior in 20-03, relay wiring in 20-04, health/docs validation in 20-05. |
| REQ | PHASE-20-PYTHON-MIDDLEWARE-REMOVAL | Middleware must not be required for queue/retry behavior or embeddings conversion | 20-03, 20-04 | COVERED | All changes stay in Go `service/embeddinggovernor`, `relay`, and `dto`. |
| REQ | PHASE-20-UPSTREAM-SYNC-GUARD | Preserve fork-owned Go routing/governor paths | 20-03, 20-05 | COVERED | Tests and docs lock `service/embeddinggovernor` and `relay/embedding_handler.go`. |
| REQ | PHASE-20-CLI-DOCS-RUNTIME-PARITY | Operators need docs parity and runtime restart docs through user systemd | 20-05 | COVERED | Docs/validation plan updates operator manual and monitor gates. |
| REQ | PHASE-20-SDK-SMOKES | Runtime validation must include embeddings smoke and known failure classification | 20-03, 20-05 | COVERED | Unit status classifier in 20-03; smoke gate in 20-05. |
| REQ | PHASE-20-GRAPHIFY-GATE | Graphify must be checked when enabled | 20-05 | COVERED | Final validation plan includes freshness gate. |
| REQ | PHASE-20-GO-ONLY-V1-MODELS and related catalog/base-url/channel requirements | Existing Phase 20 catalog/provider contract | none | EXCLUDED | Already validated in `20-VALIDATION.md` / `20-UAT.md`; this follow-up does not alter catalog ownership or provider consolidation. |
| RESEARCH | Request-local signals first | InputCount/coarse size/workload header | 20-03, 20-04 | COVERED | Core fields/classification in 20-03; relay metadata extraction in 20-04. |
| RESEARCH | Split batch vs interactive EWMA | Prevent catch-up latency from blocking interactive scale | 20-03 | COVERED | Separate metrics and scale-up gates. |
| RESEARCH | Failure classification | `429`, `5xx`, timeout/connection failures affect backpressure; ordinary client `4xx` does not | 20-03, 20-04 | COVERED | Governor classifier plus relay status propagation. |
| RESEARCH | Optional TEI health signal | Disabled-by-default/read-only with hysteresis | 20-05 | COVERED | Health plan is env-gated and requires consecutive bad windows. |
| RESEARCH | Router-side sub-batching | Split arrays into upstream batches of 4 | none | EXCLUDED | Research explicitly says not to include splitting/recomposition unless the user expands scope; this prompt did not include it. |
| CONTEXT | runtime-path | GBrain/Obsidian -> router Go -> governor -> Local TEI -> TEI 10.1.1.4:3000 | 20-03, 20-04, 20-05 | COVERED | All plans preserve this path. |
| CONTEXT | daily-limits | `min=1`, `initial=2`, auto `max=3`; `4` manual/turbo only | 20-03, 20-05 | COVERED | Defaults are asserted in tests and docs. |
| CONTEXT | no-sensitive-capture | Do not capture input text or secrets in Request, Snapshot, logs, docs, or tests | 20-03, 20-04, 20-05 | COVERED | Metadata-only actions and acceptance gates. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-20-go-native-model-router/20-RESEARCH.md
@.planning/phases/phase-20-go-native-model-router/20-PATTERNS.md
@.planning/phases/phase-20-go-native-model-router/20-VALIDATION.md
@.planning/phases/phase-20-go-native-model-router/20-UAT.md
@service/embeddinggovernor/governor.go
@service/embeddinggovernor/governor_test.go
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-26-router-embedding-governor-go-native.md
@/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-26-gbrain-obsidian-tei-atius-router-service-management.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add metadata-only workload classification</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    - `service/embeddinggovernor/governor.go`: `Request`, `Config`, `LoadConfigFromEnv`, `normalizeConfig`, `Acquire`, `isBatch`.
    - `service/embeddinggovernor/governor_test.go`: existing `testConfig`, `testConfigWith`, `TestLoadConfigUsesDailySafeDefaults`, and batch/interactive tests.
    - `20-PATTERNS.md`: "No-input-text invariant", "Workload classification pattern", and "Env Config Normalization".
  </read_first>
  <behavior>
    - RED: unlabeled `embedding-pt-v1` requests with input count at or above the configured threshold are classified as batch.
    - RED: explicit `X-Embedding-Workload` values still win over derived metadata.
    - RED: invalid/empty threshold env vars normalize to documented safe defaults.
    - RED: `min=1`, `initial=2`, `max=3` remain the daily defaults.
  </behavior>
  <action>
    Extend `embeddinggovernor.Request` with metadata-only fields named `InputCount` and `InputChars` for input count and coarse input size. Extend `Config` and `LoadConfigFromEnv` with normalized env-backed thresholds for metadata-derived batch classification, using existing env helper patterns. Update `isBatch` in this priority order: explicit batch/bulk workload, explicit interactive/realtime workload, metadata threshold match, configured batch model. Do not add raw input strings, request bodies, tokens, provider keys, or Authorization values to any governor type, loggable struct, snapshot, or test fixture. Preserve the protected automatic cap of `MaxConcurrency=3`.
  </action>
  <acceptance_criteria>
    - Header `batch`/`bulk` forces batch and header `interactive`/`realtime` forces interactive regardless of metadata.
    - An unlabeled request whose input count or coarse size crosses the configured threshold uses batch queue/timeout behavior.
    - A small unlabeled `embedding-pt-v1` request remains interactive.
    - The existing `embedding-pt-v1-batch` model fallback still classifies as batch.
    - Config tests prove invalid/empty threshold env vars fall back safely and do not change `min=1`, `initial=2`, `max=3`.
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestWorkloadMetadataClassifiesUnlabeledLargeInputsAsBatch|TestWorkloadHeaderOverridesMetadataClassification|TestLoadConfigNormalizesWorkloadMetadataThresholds|TestLoadConfigUsesDailySafeDefaults)$' -count=1</automated>
  </verify>
  <done>Governor classification is deterministic, metadata-only, and covered by table tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split interactive and batch feedback</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    - `service/embeddinggovernor/governor.go`: `Snapshot`, `finish`, `maybeScaleForDemandLocked`, `canIncreaseLocked`, `blendDuration`.
    - `20-RESEARCH.md`: "Latency accounting", "Separate Interactive And Batch Feedback", and "Pitfall 2".
    - Obsidian governor log: "Gaps para a proxima iteracao do governor".
  </read_first>
  <behavior>
    - RED: a slow batch finish updates batch latency but does not block healthy interactive scale-up by itself.
    - RED: interactive scale-up reads interactive latency, not combined batch latency.
    - RED: snapshot exposes aggregate interactive/batch latency counters only, never request text.
  </behavior>
  <action>
    Replace the single adaptive latency feedback path with separate interactive and batch EWMA/counters while retaining the global concurrency cap. Keep existing aggregate snapshot fields and add snake_case snapshot fields for `interactive_average_latency_ms`, `batch_average_latency_ms`, `interactive_completed`, `batch_completed`, `interactive_slow`, and `batch_slow`. Use the current workload class recorded on `Lease` to update the matching metrics. Update scale-up gating so interactive demand is not blocked solely by slow batch/catch-up latency; batch slow thresholds still protect TEI through the batch-specific slow duration. Keep all state mutation under `g.mu` and keep the existing condition-variable wakeup pattern.
  </action>
  <acceptance_criteria>
    - Batch EWMA and interactive EWMA can differ in tests.
    - Long batch latency increments batch slow/latency metrics without changing interactive EWMA.
    - Healthy interactive requests can scale from 1 toward 2/3 when no cooldown or pressure failure exists, even after a prior slow batch sample.
    - Snapshot JSON contains only aggregate numbers/timestamps and no raw input/body/token fields.
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestSplitLatencyMetricsTrackInteractiveAndBatchSeparately|TestBatchLatencyDoesNotBlockInteractiveScaleUp|TestSnapshotContainsOnlyAggregateEmbeddingGovernorMetadata)$' -count=1</automated>
  </verify>
  <done>Batch catch-up latency no longer poisons interactive adaptive decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify pressure failures without punishing client errors</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    - `service/embeddinggovernor/governor.go`: `Lease.Finish`, `finish`, `rejectFromContext`, status handling.
    - `20-RESEARCH.md`: "Failure classification" and "Pitfall 3".
    - `relay/embedding_handler.go`: current `finishGovernor(false, statusCode)` call sites for context only; do not edit relay in this plan.
  </read_first>
  <behavior>
    - RED: ordinary client/validation statuses such as 400, 401, 403, 404, and 422 do not reduce concurrency or start cooldown.
    - RED: 429, 5xx, transport-equivalent status, and sustained slow request thresholds still reduce concurrency to min and start cooldown.
    - RED: successful 200 responses still count as completed and can reopen through success windows.
  </behavior>
  <action>
    Add a small governor-local status classifier used by `finish` before changing adaptive state. Treat pressure failures as signals that reduce to `MinConcurrency` and start cooldown: upstream rate pressure, server failures, transport failure markers, and slow-threshold breaches. Treat ordinary client/validation statuses as non-pressure outcomes for adaptive concurrency so bad input cannot close the circuit for other clients. Keep external response semantics out of this package; the relay still owns client-facing errors. Add focused table tests for each status class and maintain existing failure behavior for 5xx.
  </action>
  <acceptance_criteria>
    - `Finish(false, 400/401/403/404/422, latency)` does not increment pressure failure state, lower concurrency, or set cooldown.
    - `Finish(false, 429, latency)`, `Finish(false, 500+, latency)`, and transport-equivalent failure do reduce to `min=1` and set cooldown.
    - Slow request thresholds still count as pressure based on workload-specific thresholds.
    - Existing tests for cooldown/reopening still pass.
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestStatusClassificationIgnoresClientErrors|TestStatusClassificationReducesOnPressureFailures|TestStatusClassificationKeepsSlowRequestsAsPressure)$' -count=1</automated>
  </verify>
  <done>Governor pressure semantics protect TEI without letting client mistakes throttle healthy traffic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client -> Go router | Untrusted embedding requests and workload headers enter the relay/governor path. |
| Relay -> governor | Relay passes derived metadata and upstream status into adaptive state. |
| Governor -> operator snapshot/logs | Aggregate state may be logged or inspected for operations. |
| Go router -> TEI | Governor decisions control request pressure before local TEI dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-03-01 | Information Disclosure | `embeddinggovernor.Request`, `Snapshot`, tests | high | mitigate | Store and expose only counts, coarse sizes, durations, statuses, and workload class; tests must not introduce raw payload or secret fixtures. |
| T-20-03-02 | Denial of Service | Workload classification | medium | mitigate | Header is honored but bounded by queue limits, batch concurrency, and global cap; request-derived fallback prevents unlabeled catch-up from consuming interactive capacity. |
| T-20-03-03 | Denial of Service | Failure classification | medium | mitigate | Pressure statuses reduce/cool down; ordinary client statuses do not close the circuit. |
| T-20-03-04 | Tampering | Adaptive defaults | medium | mitigate | Config normalization preserves min/initial/max safety and prevents automatic `4` from becoming the default. |
| T-20-03-SC | Tampering | Package supply chain | low | accept | No package install is planned; implementation uses existing Go stdlib and in-repo dependencies. |
</threat_model>

<verification>
Overall plan gate:
<automated>/usr/local/go/bin/go test ./service/embeddinggovernor -count=1</automated>
<automated>/usr/local/go/bin/go test ./service/embeddinggovernor ./relay -count=1</automated>
</verification>

<success_criteria>
- The governor core passes focused and package-level tests.
- No new runtime owner, sidecar, middleware dependency, or container path is introduced.
- Automatic concurrency remains bounded by `max=3`.
- Governor state and tests remain metadata-only.
</success_criteria>

<output>
Create `.planning/phases/phase-20-go-native-model-router/20-03-SUMMARY.md` when done.
</output>
