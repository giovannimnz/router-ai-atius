---
phase: 30-k3s-public-cutover-and-rollback-soak
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/k3s-router-cutover-preflight.sh
  - docs/K3S-MIGRATION.md
autonomous: true
requirements:
  - PHASE-22-CUTOVER-ROLLBACK
must_haves:
  truths:
    - "A tentativa de cutover só avança quando existe um artefato GO íntegro da Phase 29 e todos os gates live continuam válidos."
    - "Apache, PgBouncer, banco, k3s e Podman possuem backups identificados por checksum antes de qualquer mutação."
    - "Segredos ausentes interrompem a execução e valores sensíveis nunca aparecem em stdout, logs ou evidências."
  artifacts:
    - path: "scripts/k3s-router-cutover-preflight.sh"
      provides: "Preflight fail-closed, inventário e envelope de backup do cutover"
    - path: "docs/K3S-MIGRATION.md"
      provides: "Contrato operacional do preflight, evidências e restauração"
  key_links:
    - from: "scripts/k3s-router-cutover-preflight.sh"
      to: ".planning/phases/29-k3s-shadow-restore-and-go-no-go/"
      via: "validação do GO, hashes, ClusterIPs e procedimentos de rollback da Phase 29"
      pattern: "GO|sha256|ClusterIP"
    - from: "scripts/k3s-router-cutover-preflight.sh"
      to: "backups/k3s-router-cutover-*"
      via: "manifesto sanitizado de backups e checksums"
      pattern: "sha256sum"
---

<objective>
Criar o envelope de segurança e o gate GO que tornam o cutover reversível antes da primeira alteração live, implementando D-02 e D-03.

Purpose: Impedir uma promoção baseada em evidência incompleta, segredo ausente ou rollback não comprovado.
Output: Script fail-closed de preflight/backup e contrato operacional em português.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-CONTEXT.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-RESEARCH.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-VALIDATION.md
@scripts/k3s-router-backup.sh
@scripts/k3s-router-preflight.sh
@scripts/k3s-router-cutover-checklist.sh
@docs/K3S-MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar preflight GO fail-closed e inventário sanitizado</name>
  <files>scripts/k3s-router-cutover-preflight.sh</files>
  <behavior>
    - Caso 1: GO ausente, checksum divergente, ClusterIP vazio, EndpointSlice sem endpoint ready, Pod/PVC/PV inválido, node sem Ready ou com DiskPressure/taint resulta em exit não zero antes de backup ou mutação.
    - Caso 2: token/credencial operacional ausente resulta em exit não zero sem imprimir nome expandido, valor, header ou DSN.
    - Caso 3: GO íntegro e estado live coerente produz inventário sanitizado com timestamps, UIDs, digests, ClusterIPs e checksums.
  </behavior>
  <action>Criar script Bash com `set -euo pipefail`, um modo fixture/read-only para testes e opt-in explícito para a coleta live. Descobrir o artefato GO da Phase 29 sem assumir nome arbitrário; exigir decisão GO, checksum válido, imagem imutável, restore aprovado, backend k3s do CLIAnything aprovado, PVs Retain, Pods no label dedicado de atius-srv-1, node Ready com DiskPressure=False e sem taint, Services ClusterIP e EndpointSlices ready. Registrar apenas metadados não sensíveis. Aplicar D-02 ao capturar os ClusterIPs exatos e D-03 ao resolver o vhost efetivamente carregado, PgBouncer, units k3s/Podman e banco. Não implementar Headroom, conforme D-08. Toda operação pesada deve usar a contenção de CPU do repo e permanecer em até 20%.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-cutover-preflight.sh &amp;&amp; scripts/k3s-router-cutover-preflight.sh --self-test</automated>
  </verify>
  <done>Fixtures provam falha fechada para GO/secret/endpoint inválidos e sucesso somente com o conjunto completo, sem material sensível na saída.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar envelope atômico de backups e checksums</name>
  <files>scripts/k3s-router-cutover-preflight.sh</files>
  <behavior>
    - Caso 1: diretório timestamped é criado com modo 0700 e manifesto de checksums verificável.
    - Caso 2: falha em qualquer backup obrigatório invalida o envelope inteiro e impede o cutover.
    - Caso 3: evidência não contém Secret YAML, token, cookie, senha, DSN com credencial ou conteúdo de arquivo de autenticação.
  </behavior>
  <action>Adicionar modo `--prepare` idempotente que, após o preflight, cria um envelope único contendo cópia preservando owner/mode do vhost enabled e configuração PgBouncer, dump fresco consistente de DBRouterAiAtius via caminho operacional aprovado, checksums do dump e configs, inventário sanitizado de Apache/PgBouncer/k3s/Podman, manifests k3s sem Secret, imagens/digests, volumes, units e estado de serviços. Não persistir valores do Vault nem executar com tracing. Exigir espaço livre, dump não vazio e verificação de checksum; gravar ponteiros exatos para rollback independente de Apache e PgBouncer. Não parar serviços nem alterar configurações nesta task.</action>
  <verify>
    <automated>scripts/k3s-router-cutover-preflight.sh --self-test-backup</automated>
  </verify>
  <done>O envelope só recebe status READY quando todos os backups e checksums são verificáveis e o scanner de segredos da fixture não encontra dados sensíveis.</done>
</task>

<task type="auto">
  <name>Task 3: Documentar gate, CPU, segredos e restauração</name>
  <files>docs/K3S-MIGRATION.md</files>
  <action>Atualizar em português o runbook com comando exato do preflight, fonte Vault em memória, regra fail-closed, artefatos gerados, interpretação de READY/NO-GO, limite CPU de 20%, ordem de restauração e proibição de Headroom per D-08. Documentar que a execução não pode tocar Apache/PgBouncer sem o envelope READY e que qualquer discrepância temporal exige novo backup.</action>
  <verify>
    <automated>scripts/smoke-docs-links.sh &amp;&amp; rg -n "cutover-preflight|READY|NO-GO|20%|Headroom|checksum" docs/K3S-MIGRATION.md</automated>
  </verify>
  <done>O runbook permite reproduzir o preflight e restaurar cada superfície sem depender de informação secreta ou implícita.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault/processo→scripts | Segredos entram somente no processo e não podem alcançar logs/evidências. |
| Host→k3s API/Services | Estado live e endpoints precisam corresponder ao GO assinado da Phase 29. |
| Runtime→backup | Configuração e dados de produção cruzam para armazenamento de rollback protegido. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Spoofing | artefato GO/ClusterIPs | high | mitigate | Verificar checksum, identidade do namespace/Services, UIDs e EndpointSlices antes de aceitar GO. |
| T-30-02 | Information Disclosure | preflight/evidence | high | mitigate | Sem tracing, redaction, allowlist de campos e scanner de segredos em fixtures. |
| T-30-03 | Tampering | backups/configuração | high | mitigate | Diretório 0700, cópias com owner/mode, sha256 e manifesto READY somente após verificação. |
| T-30-04 | Denial of Service | dump/inventário | medium | mitigate | Espaço livre, comandos limitados e CPU até 20% antes da coleta. |
</threat_model>

<verification>
Executar os self-tests sem mutação live; em execução real, exigir `--prepare`, envelope READY e checksums válidos antes de liberar o Plan 30-02.
</verification>

<success_criteria>
- GO da Phase 29 é requisito mecanicamente verificado, não afirmação textual.
- Backups de Apache, PgBouncer, k3s, Podman e DB formam um envelope único e íntegro.
- Nenhum segredo é impresso ou persistido.
</success_criteria>

<output>
Create `.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-01-SUMMARY.md` when done
</output>
