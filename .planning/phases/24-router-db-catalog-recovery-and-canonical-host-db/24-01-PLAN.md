---
phase: 24-router-db-catalog-recovery-and-canonical-host-db
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ROUTER-DB-RECOVERY.md
  - scripts/phase24-db-preflight.sh
  - .planning/STATE.md
autonomous: true
requirements:
  - PHASE-24-CANONICAL-HOST-DB
  - PHASE-24-CUTOVER-ROLLBACK
must_haves:
  truths:
    - D-01 The live runtime currently uses host PgBouncer to `newapi`; no mutation starts before that baseline is frozen.
    - D-02 Fresh full and catalog-only backups are mandatory before any DB/catalog change.
    - D-03 Recovery source ranking must preserve live operational data and use 2026-07-01 snapshots only for missing catalog rows.
  artifacts:
    - docs/ROUTER-DB-RECOVERY.md
    - scripts/phase24-db-preflight.sh
  key_links:
    - runtime baseline -> backup inventory -> restore source selection
    - current newapi state -> 2026-07-01 catalog snapshot -> transformed target catalog
---

<objective>
Create the preflight, freeze, and backup contract for the full router DB/catalog recovery.

Purpose: establish exactly what is live, what must be preserved, what is missing, and which artifacts are authoritative before any restore or rename action happens.
</objective>

## Artifacts this plan produces

- `docs/ROUTER-DB-RECOVERY.md`
- `scripts/phase24-db-preflight.sh`
- Phase 24 state notes in `.planning/STATE.md`

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-01 | Host PgBouncer path is current truth | 24-01 | COVERED | Freeze runtime before mutation. |
| CONTEXT | D-02 | Fresh full and catalog-only backups | 24-01 | COVERED | Backup contract and verification. |
| RESEARCH | Source ranking | Preserve live non-catalog state | 24-01 | COVERED | Use current `newapi` for operational data. |
| RESEARCH | 2026-07-01 snapshots | Best catalog source | 24-01 | COVERED | Explicit source selection table. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-CONTEXT.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-RESEARCH.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-PATTERNS.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-VALIDATION.md
@/home/ubuntu/.config/systemd/user/container-router-ai-atius.service
@/home/ubuntu/.config/systemd/user/container-postgres.service
@backups/clianything/20260701_184735_channels.sql
@backups/clianything/20260701_184735_models.sql
@backups/clianything/20260701_184735_abilities.sql
@/home/ubuntu/.backups/router-ai-atius-incident-20260703T231027-0300/newapi-before.fix.dump
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the DB recovery contract runbook</name>
  <files>docs/ROUTER-DB-RECOVERY.md</files>
  <action>
    Create a PT-BR runbook describing `Estado atual`, `Fontes de restauracao`, `Transformacoes obrigatorias`, `Banco final canonico`, `Backups obrigatorios`, `Mutacao segura`, `Rollback`, and `Validacao final`. It must explicitly state that the 2026-07-01 catalog snapshots are the best source for `OpenAI - Codex` and GPT/Codex rows, while current `newapi` remains the source of truth for `users`, `tokens`, and `logs`.
  </action>
  <acceptance_criteria>
    - The runbook contains the exact phrase `catalogo 2026-07-01`.
    - The runbook contains the exact phrase `users, tokens e logs permanecem vindo do banco live`.
    - The runbook contains a `Rollback` section that references the fresh Phase 24 backup.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "catalogo 2026-07-01|users, tokens e logs permanecem vindo do banco live|Rollback" docs/ROUTER-DB-RECOVERY.md</automated>
  </verify>
  <done>Operators have a written recovery contract before any mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Add read-only DB/catalog preflight script</name>
  <files>scripts/phase24-db-preflight.sh</files>
  <action>
    Create a read-only shell script with `set -euo pipefail` that captures: Graphify status, `bin/clianything status --strict`, current `channels/models/abilities/tokens` counts, host DB list, current router unit DB target, and `pg_restore -l` checks for the chosen backup sources. The script must not run `psql` writes, `pg_restore`, `systemctl restart`, or any SQL mutation.
  </action>
  <acceptance_criteria>
    - Script contains `set -euo pipefail`.
    - Script contains `pg_restore -l`.
    - Script does not contain `INSERT`, `UPDATE`, `DELETE`, `ALTER DATABASE`, `systemctl --user restart`, or `pg_restore -d`.
    - `bash -n scripts/phase24-db-preflight.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/phase24-db-preflight.sh</automated>
    <automated>! rg -n "INSERT |UPDATE |DELETE |ALTER DATABASE|systemctl --user restart|pg_restore -d" scripts/phase24-db-preflight.sh</automated>
  </verify>
  <done>Preflight can be rerun safely without mutating DB or runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Update state to record the active recovery phase</name>
  <files>.planning/STATE.md</files>
  <action>
    Add/adjust the Phase 24 state note so the repo clearly records that router DB/catalog recovery is the active planning focus and that Phase 21 remains parked, not deleted.
  </action>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `Phase 24`.
    - `.planning/STATE.md` contains `Phase 21 remains parked`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 24|Phase 21 remains parked" .planning/STATE.md</automated>
  </verify>
  <done>State reflects the active recovery initiative.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Wrong backup selected | high | Require source ranking and `pg_restore -l` validation before mutation. |
| Fresh live data overwritten by old dump | high | Separate live data preservation from catalog restoration. |
| Secrets leak into planning/docs | high | Redact DSNs and never commit live values. |
</threat_model>

<verification>
<automated>bash -n scripts/phase24-db-preflight.sh</automated>
<automated>rg -n "catalogo 2026-07-01|users, tokens e logs permanecem vindo do banco live|Rollback" docs/ROUTER-DB-RECOVERY.md</automated>
</verification>

<success_criteria>
- Fresh recovery contract exists.
- Read-only preflight exists.
- State clearly names Phase 24 as active recovery work.
</success_criteria>

<output>
Create `.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-01-SUMMARY.md` when done.
</output>
