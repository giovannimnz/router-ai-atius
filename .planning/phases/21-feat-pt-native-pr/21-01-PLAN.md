---
phase: 21-feat-pt-native-pr
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md
  - .planning/phases/21-feat-pt-native-pr/21-SAME-AS-ENGLISH-LITERALS.json
autonomous: true
requirements:
  - PHASE-21-UPSTREAM-NATIVE-I18N
  - PHASE-21-REUSE-EXISTING-TRANSLATIONS
  - PHASE-21-LOCAL-FIRST-VALIDATION
  - PHASE-21-UPSTREAM-PR-HYGIENE
must_haves:
  truths:
    - "D-01: Current upstream baseline must be re-fetched and recorded before implementation."
    - "D-02: Implementation must revalidate `upstream/main` before edits and must not rely on stale clean commit `cd8cb89bb72b1f5551a9f7536f104498ddfb4d75`."
    - "D-03: The older clean branch is translation source material only, not the final scope contract."
    - "D-13: Existing PT-BR translation sources must be inventoried and classified before translating gaps."
    - "D-15: Reuse translation values through the inventory; do not cherry-pick or copy historical branches wholesale."
    - "D-16: Same-as-English values must be classified as brand/code literals or unresolved gaps before claiming coverage."
    - Planning artifacts stay outside the future upstream PR branch.
  artifacts:
    - `.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md`
    - `.planning/phases/21-feat-pt-native-pr/21-SAME-AS-ENGLISH-LITERALS.json`
  key_links:
    - upstream baseline -> clean worktree -> upstream-ready diff
    - translation source inventory -> reuse mapping -> gap-only translation
---

<objective>
Create the clean implementation lane and a verified PT-BR translation inventory so later code plans reuse existing translations first and do not inherit fork/runtime contamination.
</objective>

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-01/D-02 | Use current upstream baseline | 21-01 | COVERED | Re-fetch and record exact commit. |
| CONTEXT | D-03/D-13/D-14/D-15/D-16 | Old clean lanes are source material only | 21-01 | COVERED | Inventory maps usable strings, not branch diffs. |
| REQUIREMENTS | PHASE-21-REUSE-EXISTING-TRANSLATIONS | Reuse all existing PT work | 21-01 | COVERED | Produces explicit inventory artifact. |
| REQUIREMENTS | PHASE-21-UPSTREAM-PR-HYGIENE | Keep upstream branch clean | 21-01 | COVERED | Worktree contract excludes `.planning/` from PR branch. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-feat-pt-native-pr/21-CONTEXT.md
@.planning/phases/21-feat-pt-native-pr/21-RESEARCH.md
@AGENTS.md
@.github/PULL_REQUEST_TEMPLATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfirm upstream baseline and create a clean lane</name>
  <files>git worktree/branch only</files>
  <action>
    Fetch `upstream/main`, record the exact commit, and create a clean implementation worktree or branch from that commit. Do not implement in the dirty fork checkout. The implementation lane starts with no diffs against `upstream/main`. Prefer `/home/ubuntu/GitHub/containers/router-ai-atius-phase21-upstream` as the dedicated Codex execution lane unless it already exists for a different active purpose.
  </action>
  <acceptance_criteria>
    - `git rev-parse upstream/main` is recorded in the summary and inventory.
    - `implementation_worktree` is recorded in `21-TRANSLATION-INVENTORY.md` as the authoritative cwd for plans `21-02` through `21-05`.
    - `git status --short --branch` is clean in the implementation lane before edits.
    - `git diff --name-only upstream/main...HEAD` is empty before edits.
    - `git worktree list` includes the recorded implementation lane path.
  </acceptance_criteria>
  <verify>
    <automated>git fetch upstream main --prune</automated>
    <automated>git rev-parse upstream/main</automated>
    <automated>git worktree list</automated>
    <automated>git status --short --branch</automated>
    <automated>git diff --name-only upstream/main...HEAD</automated>
  </verify>
  <done>Implementation is anchored to upstream truth instead of fork history.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the PT-BR translation inventory artifact</name>
  <files>.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</files>
  <action>
    Inventory every available `pt`, `pt-BR`, and Portuguese translation source before editing locale files. Include the current repo, `origin/feat/pt-native-i18n-clean`, `/home/ubuntu/GitHub/containers/router-ai-atius-pt-native-clean`, historical commits `728bb2e2`, `3f9209e0`, `05accaf9`, and `5f0453fb` when available. Record which sources are primary, fallback, stale, or unsafe.
  </action>
  <acceptance_criteria>
    - Inventory names source paths/commits and whether each source is reusable.
    - Inventory includes backend, default frontend, classic frontend, and glossary/style-guide sources.
    - Inventory records known key-count fit, placeholder mismatch status, stale/missing counts, and fork/brand contamination risks.
    - Inventory states the reuse order used by later plans.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
    <automated>rg -n "feat/pt-native-i18n-clean|router-ai-atius-pt-native-clean|728bb2e2|3f9209e0|05accaf9|5f0453fb|same_as_key|fork/brand" .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
  </verify>
  <done>Translation work has a documented source-of-truth order and does not start from scratch.</done>
</task>

<task type="auto">
  <name>Task 3: Classify unsafe reuse cases before implementation</name>
  <files>.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md, .planning/phases/21-feat-pt-native-pr/21-SAME-AS-ENGLISH-LITERALS.json</files>
  <action>
    Mark strings that must not be blindly reused: fork-only branding, `Atius` text, `/home/ubuntu` paths, docs/runtime language, accidental translation of protected `New API`/`QuantumNous` identity, placeholder mismatches, stale historical keys, and English fallbacks that are not brand/code literals. Create `21-SAME-AS-ENGLISH-LITERALS.json` with reviewed key allowlists for `web/default` and `web/classic`; only keys listed there may remain exactly equal to English because they are brand names, API/model names, code literals, or protected identity text.
  </action>
  <acceptance_criteria>
    - Inventory explicitly says not to cherry-pick whole old branches.
    - Protected project identity stays literal where upstream uses it.
    - Known same-as-key values are classified for later review instead of accepted as translated coverage.
    - `21-SAME-AS-ENGLISH-LITERALS.json` exists and contains only reviewed key arrays under `web/default` and `web/classic`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Do not cherry-pick|Protected identity|same_as_key|Atius|New API|QuantumNous" .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const data=JSON.parse(fs.readFileSync('.planning/phases/21-feat-pt-native-pr/21-SAME-AS-ENGLISH-LITERALS.json','utf8')); for (const k of ['web/default','web/classic']) { if (!Array.isArray(data[k])) throw new Error(`${k} must be an array of reviewed locale keys`); }"</automated>
  </verify>
  <done>Reusable translation content is separated from unsafe fork contamination.</done>
</task>

<task type="auto">
  <name>Task 4: Record the implementation branch contract</name>
  <files>.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</files>
  <action>
    State that all `.planning/` summaries, inventory notes, Graphify data, Obsidian notes, PR draft text, and review notes stay outside the final upstream PR branch. The future upstream branch may contain only native language files, wiring, and narrowly justified validation tests/checks.
  </action>
  <acceptance_criteria>
    - Inventory/handoff notes cannot be accidentally included in `git diff upstream/main...HEAD`.
    - Future PR branch scope is limited before code plans start.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "outside the final upstream PR branch|native language files|Graphify|Obsidian" .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
  </verify>
  <done>Planning artifacts are useful locally but excluded from upstream review.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Implementing from dirty fork state | high | Clean worktree/branch from freshly fetched `upstream/main`. |
| Reusing old branch diffs instead of translation values | high | Inventory treats old lanes as source material only. |
| Leaking fork branding or runtime paths through translation reuse | high | Unsafe reuse cases are classified before editing locale files. |
| Losing the user's existing translation work | high | Inventory is mandatory before any new translation is created. |
</threat_model>

<verification>
<automated>git fetch upstream main --prune</automated>
<automated>git rev-parse upstream/main</automated>
<automated>test -f .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
<automated>rg -n "Reuse order|Do not cherry-pick|outside the final upstream PR branch" .planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md</automated>
</verification>

<success_criteria>
- Upstream baseline is current and recorded.
- Implementation lane is clean before code edits.
- PT-BR source inventory exists and names all reuse sources.
- Unsafe fork-specific strings are called out before implementation.
- `.planning/` artifacts are explicitly excluded from the future upstream PR branch.
</success_criteria>

<output>
Create or update `.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md` and summarize exact baseline/reuse decisions in `21-01-SUMMARY.md`.
</output>
