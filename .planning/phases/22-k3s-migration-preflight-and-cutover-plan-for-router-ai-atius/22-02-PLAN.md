---
phase: 22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius
plan: "02"
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - k8s/router-ai-atius/namespace.yaml
  - k8s/router-ai-atius/configmap.yaml
  - k8s/router-ai-atius/secret.example.env
  - k8s/router-ai-atius/postgres.yaml
  - k8s/router-ai-atius/redis.yaml
  - k8s/router-ai-atius/router.yaml
  - k8s/router-ai-atius/README.md
  - scripts/k3s-router-validate-manifests.sh
autonomous: true
requirements:
  - PHASE-22-K3S-PREFLIGHT
  - PHASE-22-RUNTIME-PARITY
  - PHASE-22-STATEFUL-DATA
must_haves:
  truths:
    - D-02 All router resources live in namespace `router-ai-atius`.
    - D-07 Real secrets are not committed; only templates or creation commands are committed.
    - D-09 Governor env is explicit and keeps `min=1`, `initial=2`, `max=3`.
    - D-12 TEI in `ai-search` is read as a dependency and is not changed by this plan.
  artifacts:
    - k8s/router-ai-atius/
    - scripts/k3s-router-validate-manifests.sh
  key_links:
    - Podman env -> ConfigMap/Secret/PVC manifest mapping
    - local-path storage limitation -> Postgres StatefulSet guardrails
    - TEI dependency -> router env and smoke gate
---

<objective>
Create the Kubernetes manifest set and server-side dry-run gate for the router stack.

Purpose: make the desired k3s runtime auditable before any real apply or data migration.
Output: non-secret manifests/templates and a validation script.
</objective>

## Artifacts this phase produces

- `k8s/router-ai-atius/namespace.yaml`
- `k8s/router-ai-atius/configmap.yaml`
- `k8s/router-ai-atius/secret.example.env`
- `k8s/router-ai-atius/postgres.yaml`
- `k8s/router-ai-atius/redis.yaml`
- `k8s/router-ai-atius/router.yaml`
- `k8s/router-ai-atius/README.md`
- `scripts/k3s-router-validate-manifests.sh`

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-02 | Dedicated namespace | 22-02 | COVERED | Namespace manifest. |
| CONTEXT | D-05 | DB migration high-risk on local-path | 22-02, 22-03 | COVERED | StatefulSet plus warnings and backup gates. |
| CONTEXT | D-07 | Secrets apply-time only | 22-02 | COVERED | `secret.example.env`, no Secret values. |
| CONTEXT | D-09 | Explicit governor env | 22-02 | COVERED | ConfigMap values. |
| CONTEXT | D-12 | Do not mutate TEI | 22-02 | COVERED | Router references TEI only. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-CONTEXT.md
@.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-RESEARCH.md
@docs/K3S-MIGRATION.md
@Dockerfile
@.dockerignore
@podman-compose.yml
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@/home/ubuntu/GitHub/embeddings/k8s/tei-gte.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add namespace and non-secret ConfigMap</name>
  <files>k8s/router-ai-atius/namespace.yaml, k8s/router-ai-atius/configmap.yaml</files>
  <read_first>
    - `22-CONTEXT.md`: decisions D-02, D-03, D-09, and D-12.
    - `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`: governor env names and safe defaults.
    - `/home/ubuntu/GitHub/embeddings/k8s/tei-gte.yaml`: labels, namespace style, and resource annotation style.
  </read_first>
  <action>
    Create namespace manifest for `router-ai-atius` with labels `app.kubernetes.io/part-of: atius-router` and `app.kubernetes.io/name: router-ai-atius`. Create a ConfigMap named `router-ai-atius-config` containing non-secret env values: `TZ=America/Sao_Paulo`, `LANG=pt_BR.UTF-8`, `TRUST_PROXY=true`, `ERROR_LOG_ENABLED=true`, `BATCH_UPDATE_ENABLED=true`, `NODE_NAME=router-ai-atius-k3s`, `EMBEDDING_GOVERNOR_ENABLED=true`, `EMBEDDING_GOVERNOR_MODELS=embedding-gte-v1`, `EMBEDDING_GOVERNOR_BATCH_MODELS=`, `EMBEDDING_GOVERNOR_INITIAL_CONCURRENCY=2`, `EMBEDDING_GOVERNOR_MIN_CONCURRENCY=1`, `EMBEDDING_GOVERNOR_MAX_CONCURRENCY=3`, `EMBEDDING_GOVERNOR_BATCH_CONCURRENCY=1`, `EMBEDDING_GOVERNOR_HEALTH_PROBE_ENABLED=false`, and blank/commented guidance for optional TEI health probe URL in README only, not a secret.
  </action>
  <acceptance_criteria>
    - `namespace.yaml` defines namespace `router-ai-atius`.
    - `configmap.yaml` defines `EMBEDDING_GOVERNOR_MAX_CONCURRENCY: "3"`.
    - `configmap.yaml` does not contain `SQL_DSN`, `REDIS_CONN_STRING`, `SESSION_SECRET`, `TOKEN`, `KEY`, `PASSWORD`, or `SECRET`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "kind: Namespace|name: router-ai-atius|EMBEDDING_GOVERNOR_MAX_CONCURRENCY" k8s/router-ai-atius/namespace.yaml k8s/router-ai-atius/configmap.yaml</automated>
    <automated>! rg -n "SQL_DSN|REDIS_CONN_STRING|SESSION_SECRET|TOKEN|KEY|PASSWORD|SECRET" k8s/router-ai-atius/configmap.yaml</automated>
  </verify>
  <done>Non-secret runtime config is explicit and reviewable.</done>
</task>

<task type="auto">
  <name>Task 2: Add secret template and README creation commands</name>
  <files>k8s/router-ai-atius/secret.example.env, k8s/router-ai-atius/README.md</files>
  <read_first>
    - `22-CONTEXT.md`: decision D-07.
    - `docs/PODMAN.md`: current runtime source of truth.
    - `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`: secret hygiene and recovery rules.
  </read_first>
  <action>
    Create `secret.example.env` with placeholder names only: `POSTGRES_PASSWORD=REPLACE_ME`, `REDIS_PASSWORD=REPLACE_ME`, `SESSION_SECRET=REPLACE_ME`, and any additional router secret placeholders needed by the manifests. Create `k8s/router-ai-atius/README.md` with commands that use `kubectl -n router-ai-atius create secret generic router-ai-atius-secrets --from-env-file=... --dry-run=client -o yaml | sudo -n k3s kubectl apply -f -`, plus a warning never to commit a filled env file. Do not include real values.
  </action>
  <acceptance_criteria>
    - `secret.example.env` contains only placeholder values with `REPLACE_ME`.
    - `README.md` contains `create secret generic router-ai-atius-secrets`.
    - No real-looking bearer/token/OAuth/provider key values are present.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "REPLACE_ME|create secret generic router-ai-atius-secrets|from-env-file" k8s/router-ai-atius/secret.example.env k8s/router-ai-atius/README.md</automated>
  </verify>
  <done>Secret creation is documented without leaking values.</done>
</task>

<task type="auto">
  <name>Task 3: Add Postgres, Redis, and router manifests for dry-run review</name>
  <files>k8s/router-ai-atius/postgres.yaml, k8s/router-ai-atius/redis.yaml, k8s/router-ai-atius/router.yaml</files>
  <read_first>
    - `podman-compose.yml`: service names, ports, DB/Redis relationships.
    - `Dockerfile`: image entrypoint, exposed port, workdir `/data`.
    - `.dockerignore`: protected runtime dirs out of build context.
    - `22-RESEARCH.md`: current k3s storage and ingress findings.
  </read_first>
  <action>
    Create manifests for a single-replica initial stack. Use `apps/v1` StatefulSet or Deployment for Postgres with a PVC and labels under namespace `router-ai-atius`; Redis can be Deployment with service unless persistence is explicitly required by the README. Create router Deployment with `replicas: 1`, image `ghcr.io/giovannimnz/router-ai-atius:latest`, container port `3000`, env from ConfigMap and Secret, `SQL_DSN` assembled from secret-backed password pointing to the Postgres service, `REDIS_CONN_STRING` assembled from secret-backed password pointing to Redis service, `/data` PVC or documented emptyDir decision, and `/app/logs` PVC/emptyDir decision. Add readiness/liveness probes against `/api/status` or `/health` with conservative initial delays. Add a Service named `router-ai-atius` that exposes port `3000` internally; do not expose public traffic yet unless README marks it shadow-only.
  </action>
  <acceptance_criteria>
    - Manifests use namespace `router-ai-atius`.
    - Router Deployment has `replicas: 1`.
    - Router image is `ghcr.io/giovannimnz/router-ai-atius:latest`.
    - Router env references `router-ai-atius-config` and `router-ai-atius-secrets`.
    - No manifest contains literal production secret values.
    - Postgres PVC includes a warning annotation or README note that local-path is not HA.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "namespace: router-ai-atius|replicas: 1|ghcr.io/giovannimnz/router-ai-atius:latest|router-ai-atius-config|router-ai-atius-secrets" k8s/router-ai-atius/*.yaml</automated>
    <automated>! rg -n "Bearer |access_token|refresh_token|sk-|ghp_|AIza|password=[^\\$]|SESSION_SECRET: [^\\$]" k8s/router-ai-atius/*.yaml</automated>
  </verify>
  <done>Manifest target state is reviewable and secret-free.</done>
</task>

<task type="auto">
  <name>Task 4: Add server-side manifest validation script</name>
  <files>scripts/k3s-router-validate-manifests.sh</files>
  <read_first>
    - `scripts/k3s-router-preflight.sh`: shell style and read-only behavior from Plan 22-01.
    - `k8s/router-ai-atius/README.md`: manifest apply/dry-run instructions.
  </read_first>
  <action>
    Create `scripts/k3s-router-validate-manifests.sh` with `set -euo pipefail`. The script must verify that the manifest directory exists, run `sudo -n k3s kubectl apply --dry-run=server -f k8s/router-ai-atius/`, and print a reminder that dry-run does not create resources or validate secret values. It must not apply real resources unless a future explicit flag is added in another plan.
  </action>
  <acceptance_criteria>
    - `bash -n scripts/k3s-router-validate-manifests.sh` exits 0.
    - Script contains `--dry-run=server`.
    - Script does not contain `kubectl apply -f` without `--dry-run=server`.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/k3s-router-validate-manifests.sh</automated>
    <automated>rg -n -- "--dry-run=server" scripts/k3s-router-validate-manifests.sh</automated>
  </verify>
  <done>Manifest validation is reproducible before any real apply.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git repo -> Kubernetes | Committed manifests must not contain real secrets. |
| Router pod -> DB/Redis | DSNs connect to internal services and use Secret values. |
| Router pod -> TEI | Embedding requests leave namespace to `ai-search` TEI dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-22-02-01 | Information Disclosure | manifests/secrets | high | mitigate | Commit only placeholders; use Kubernetes Secret creation commands at apply time. |
| T-22-02-02 | Denial of Service | resource scheduling | high | mitigate | Single replica, explicit requests/limits, avoid tainted DiskPressure node in later apply plan. |
| T-22-02-03 | Spoofing/Tampering | public service exposure | medium | mitigate | Keep Service internal/shadow until cutover plan. |
| T-22-02-04 | Information Disclosure | `/v1/models` | high | mitigate | Preserve Go-owned code path and include smoke tests in later plans. |
</threat_model>

<verification>
<automated>bash -n scripts/k3s-router-validate-manifests.sh</automated>
<automated>sudo -n k3s kubectl apply --dry-run=server -f k8s/router-ai-atius/</automated>
<automated>! rg -n "Bearer |access_token|refresh_token|sk-|ghp_|AIza|SESSION_SECRET: [^\\$]" k8s/router-ai-atius</automated>
</verification>

<success_criteria>
- k3s manifest set exists, is secret-free, and passes server-side dry-run.
- Router config explicitly preserves governor and full-Go runtime assumptions.
- No public traffic exposure is introduced by this plan.
</success_criteria>

<output>
Create `.planning/phases/22-k3s-migration-preflight-and-cutover-plan-for-router-ai-atius/22-02-SUMMARY.md` when done.
</output>
