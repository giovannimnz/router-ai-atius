---
phase: 30-k3s-public-cutover-and-rollback-soak
plan: "03"
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - scripts/k3s-router-public-smoke.sh
  - scripts/k3s-router-soak.sh
  - docs/K3S-MIGRATION.md
autonomous: true
requirements:
  - PHASE-22-CUTOVER-ROLLBACK
  - PHASE-20-GO-ONLY-V1-MODELS
  - PHASE-25-CLIENT-SMOKE-VALIDATION
must_haves:
  truths:
    - "O endpoint público passa health, models autenticado/não autenticado, chat non-stream, chat stream SSE e Responses com contratos válidos."
    - "Falhas locais/edge/DB são críticas; erros comprovadamente upstream são classificados separadamente e não mascaram regressão local."
    - "O soak autônomo de 30 minutos coleta 30 amostras de 60s e 6 matrizes completas de 5min, revertendo automaticamente em gate crítico."
  artifacts:
    - path: "scripts/k3s-router-public-smoke.sh"
      provides: "Matriz pública autenticada e fail-closed"
    - path: "scripts/k3s-router-soak.sh"
      provides: "Baseline, sampling, critérios objetivos e rollback automático"
    - path: "docs/K3S-MIGRATION.md"
      provides: "Contrato de soak acelerado e monitoramento prolongado"
  key_links:
    - from: "scripts/k3s-router-soak.sh"
      to: "scripts/k3s-router-public-smoke.sh"
      via: "smoke completo a cada 5 minutos"
      pattern: "public-smoke"
    - from: "scripts/k3s-router-soak.sh"
      to: "scripts/k3s-router-rollback.sh"
      via: "rollback automático em gate crítico"
      pattern: "rollback"
---

<objective>
Provar o contrato público e estabilizar o k3s por um soak acelerado, denso, objetivo e totalmente autônomo, implementando D-04, D-05 e D-06.

Purpose: Detectar regressões de edge, auth, catálogo, streaming, Responses, Pods, node e storage antes de retirar Podman.
Output: Smoke contratual, sampler de soak, evidência e runbook.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-CONTEXT.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-RESEARCH.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-02-SUMMARY.md
@scripts/k3s-router-smoke.sh
@scripts/smoke-openai-sdk.py
@scripts/smoke-provider-consolidation.py
@scripts/k3s-router-rollback.sh
@docs/K3S-MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar smoke público autenticado completo</name>
  <files>scripts/k3s-router-public-smoke.sh</files>
  <behavior>
    - Health retorna contrato esperado; models sem token retorna 401 e com token retorna 200 com root data somente e sem campos internos protegidos.
    - Non-stream retorna JSON válido; stream retorna SSE com deltas/eventos e terminal válido; Responses retorna contrato válido.
    - Token ausente, HTML inesperado, timeout, 5xx local, 401/403 inesperado ou payload inválido falha; erro upstream comprovado recebe classificação separada sem virar sucesso silencioso.
  </behavior>
  <action>Criar wrapper parametrizável por base URL e modelos aprovados no GO da Phase 29, sem hard-code temporal. Exigir token em memória e desligar tracing. Reusar os scripts Python existentes quando cobrem o contrato e adicionar assertions focadas onde não cobrem `/v1/responses` ou SSE. Aplicar D-04: health, models auth/unauth, non-stream, stream e Responses; validar propriedade Go-owned de `/v1/models`, root `data`, ausência de top-level extras e campos internos. Classificar 502/503/504, conexão, DB, 5xx local e contrato inválido como crítico; somente classificar quota/auth/provider como upstream quando houver evidência de dispatch e saúde local. A saída persistida deve conter status, latência, modelo e classe, nunca bodies completos, prompts, token ou headers.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-public-smoke.sh &amp;&amp; scripts/k3s-router-public-smoke.sh --self-test</automated>
  </verify>
  <done>Fixtures cobrem sucesso e cada classe de falha; execução sem token falha e nenhum segredo aparece na evidência.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar soak acelerado com rollback automático</name>
  <files>scripts/k3s-router-soak.sh</files>
  <behavior>
    - Execução padrão dura 30 minutos, coleta uma amostra por minuto e roda matriz completa no início e a cada 5 minutos, totalizando no mínimo 30 amostras e 6 matrizes.
    - Zero restarts novos, Pods Ready, node Ready/DiskPressure=False/sem taint, PVCs Bound/PVs Retain, storage com pelo menos 20% livre e ausência de eventos Warning críticos aprovam o eixo k3s.
    - Duas falhas consecutivas de health/model sampling ou uma falha crítica de smoke/Pod/node/storage/DB aciona imediatamente rollback Apache→PgBouncer e smoke público Podman.
  </behavior>
  <action>Criar sampler com baseline T-0 de Pod UIDs/restartCount, node conditions/taints, PVC/PV, uso de storage, eventos, PgBouncer backend e latência pública. Fixar padrão autônomo pragmático em 30 minutos, intervalo 60s e matriz completa 300s; permitir duração maior somente por flag, sem reduzir os mínimos padrão. Aplicar D-05 com critérios mensuráveis e D-06 chamando rollback completo no primeiro gate crítico; health sampling tolera somente uma falha transitória isolada, nunca duas consecutivas. Registrar cada amostra em formato sanitizado e produzir resumo PASS/ROLLED_BACK/FAILED com contagens exatas. Não usar loops CPU-heavy; comandos ficam muito abaixo de 20% e qualquer verificação pesada usa wrapper.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-soak.sh &amp;&amp; scripts/k3s-router-soak.sh --self-test</automated>
  </verify>
  <done>Relógio/smokes fake provam 30/6 no caminho PASS e rollback imediato em cada gate crítico, incluindo smoke de rollback.</done>
</task>

<task type="auto">
  <name>Task 3: Executar smoke e soak e documentar observação prolongada</name>
  <files>docs/K3S-MIGRATION.md</files>
  <action>Executar primeiro uma matriz pública completa e então o soak padrão de 30 minutos sem checkpoint humano. Se PASS, registrar baseline, 30+ amostras, 6+ matrizes, ausência de restarts/eventos críticos e backend DB k3s. Se houver gate crítico, exigir rollback e smoke Podman aprovados e encerrar NO-GO. Documentar o mesmo sampler em modo prolongado de 24 horas como acompanhamento pós-operação recomendado, com amostra de 5 minutos e smoke de 15 minutos, sem convertê-lo em requisito bloqueante desta execução autônoma. Manter D-08: sem Headroom.</action>
  <verify>
    <automated>scripts/smoke-docs-links.sh &amp;&amp; rg -n "30 minutos|60 segundos|5 minutos|24 horas|rollback automatico|Responses" docs/K3S-MIGRATION.md</automated>
  </verify>
  <done>Uma execução PASS tem contagens e critérios completos; qualquer falha tem rollback público Podman comprovado; monitoramento de 24h fica operacionalmente documentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cliente sintético→Apache público | Requests autenticados atravessam TLS e edge de produção. |
| Apache→router→providers | Falhas locais precisam ser distinguidas de respostas upstream. |
| Sampler→rollback privilegiado | Detecção automática pode restaurar configurações live. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-09 | Information Disclosure | smoke/evidence | high | mitigate | Token obrigatório em memória, sem tracing/bodies/headers e evidência allowlisted. |
| T-30-10 | Repudiation | soak | medium | mitigate | Timestamps, baseline, amostras, classes e checksums encadeados no diretório de evidência. |
| T-30-11 | Denial of Service | smoke/soak | medium | mitigate | Requests pequenos, intervalos densos porém limitados, timeouts e CPU abaixo de 20%. |
| T-30-12 | Spoofing | classificação upstream | high | mitigate | Exigir saúde local e evidência de dispatch antes de classificar como upstream. |
</threat_model>

<verification>
Rodar self-tests; depois executar a matriz pública e o soak padrão somente se Plan 30-02 concluiu com ambos os ponteiros em k3s.
</verification>

<success_criteria>
- Todos os contratos públicos passam ou a tentativa reverte automaticamente.
- O soak produz no mínimo 30 amostras e 6 matrizes em 30 minutos.
- Nenhum restart, evento crítico, pressão de node/storage ou falha DB fica sem rollback.
</success_criteria>

<output>
Create `.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-03-SUMMARY.md` when done
</output>
