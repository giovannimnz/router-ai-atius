---
phase: 29-k3s-shadow-restore-and-go-no-go
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - k8s/router-ai-atius/postgres.yaml
  - scripts/k3s-router-backup.sh
  - scripts/k3s-router-preflight.sh
  - scripts/k3s-router-restore-rehearsal.sh
  - scripts/k3s-router-validate-manifests.sh
  - tests/phase29-k3s-router-selftest.sh
  - tests/phase29-k3s-router-restore-selftest.sh
autonomous: true
requirements:
  - PHASE-22-STATEFUL-DATA
must_haves:
  truths:
    - "Um dump fresco e criado com pg_dump 17 diretamente do PostgreSQL 17 canonico do host em 127.0.0.1:8745, cluster /var/lib/postgresql/17/main e unit postgresql@17-main, com identidade cruzada pelo PgBouncer 10.11.1.11:6432, checksum e validacao estrutural."
    - "O PostgreSQL Podman contem DBRouterAiAtius vazio e nunca participa como fonte de backup ou restore."
    - "Somente namespace/config/Secret/PostgreSQL sobem antes do restore; Redis e router permanecem ausentes."
    - "Todo PV criado ate este estagio e descoberto pelos PVC UIDs, muda para Retain e tem readback confirmado antes do restore."
    - "No backup, cliente pg_dump e PostgreSQL host ficam em no maximo 400m cada, com agregado no maximo 800m; restore executa por profile-run em no maximo 800m, e todas as quotas sao registradas."
    - "O restore ocorre atomicamente em PostgreSQL 17 k3s integralmente limpo, falha no primeiro erro SQL, prova identidade/schema/invariantes nao sensiveis e so permite retry de um no-go explicito arquivado."
  artifacts:
    - path: "scripts/k3s-router-backup.sh"
      provides: "backup.json, dump fresco do PostgreSQL 17 canonico, checksum e metadados sanitizados"
    - path: "scripts/k3s-router-restore-rehearsal.sh"
      provides: "restore.json, apply PostgreSQL-only, Retain, restore atomico fail-closed e verificacao"
  key_links:
    - from: "scripts/k3s-router-backup.sh"
      to: "scripts/k3s-router-restore-rehearsal.sh"
      via: "diretorio explicito, checksum e identidade da origem"
      pattern: "K3S_BACKUP_DIR"
    - from: "PVC PostgreSQL"
      to: "PV local-path"
      via: "bind no node dedicado seguido de patch reclaimPolicy=Retain"
      pattern: "persistentVolumeReclaimPolicy.*Retain"
---

<objective>
Produzir um backup fresco confiavel e restaurar o PostgreSQL de forma estagiada no k3s, protegendo o volume antes de importar dados.

Purpose: Provar que dados reais podem ser recuperados no target sem permitir que o router inicialize contra banco vazio.
Output: Scripts de backup/restore e evidencia sanitizada de dump, PV Retain e invariantes restaurados.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-VALIDATION.md
@.planning/debug/phase29-diskpressure-audit.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-01-SUMMARY.md
@scripts/k3s-router-backup.sh
@scripts/k3s-router-apply-shadow.sh
@k8s/router-ai-atius/postgres.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar e validar backup fresco da origem live</name>
  <files>scripts/k3s-router-backup.sh</files>
  <behavior>
    - Teste 1: a unit postgresql@17-main inativa, o data_directory diferente de /var/lib/postgresql/17/main, a porta 127.0.0.1:8745 ausente/ambigua ou a identidade diferente de DBRouterAiAtius falham antes do pg_dump; PgBouncer 10.11.1.11:6432 deve reportar a mesma identidade sem ser origem do dump.
    - Teste 2: pg_dump fora da major 17, dump vazio, erro do pg_dump, checksum ausente ou validacao estrutural falha encerra nao zero.
    - Teste 3: evidencia contem timestamp UTC, identidade nao sensivel, versoes, tamanho, checksum e invariantes, nunca DSN, password ou payload de Secret.
    - Teste 4: a existencia do PostgreSQL Podman vazio nao altera a selecao; nenhuma consulta ou dump usa o container como fonte.
  </behavior>
  <action>Usar como origem autoritativa exclusivamente o PostgreSQL 17 nativo do host em 127.0.0.1:8745. Antes do dump, exigir que systemctl confirme postgresql@17-main ativo, que SHOW data_directory retorne /var/lib/postgresql/17/main, que server_version_num seja major 17 e que database/user/invariantes correspondam a DBRouterAiAtius/admin. Cruzar os mesmos campos nao sensiveis pelo PgBouncer em 10.11.1.11:6432, sem fazer dump pelo pool.

Tratar o PostgreSQL Podman vazio apenas como runtime legado explicitamente excluido da selecao. Exigir opt-in e pg_dump major 17; criar dump plain SQL fresco com exit status preservado, checksum SHA-256, validacao estrutural e backup.json sanitizado. Nao aceitar o backup antigo de 643 bytes. Reexecutar o cliente por ./scripts/podman-admin.sh profile-run com no maximo 400m e aplicar quota runtime temporaria de no maximo 400m a `postgresql@17-main`, restaurando a quota original em sucesso, erro ou sinal. Exigir agregado no maximo 800m e registrar ambos os `cpu.max` sem imprimir credenciais.</action>
  <verify>
    <automated>PHASE29_EXECUTE=1 PHASE29_BACKUP_CONFIRM=BACKUP_CANONICAL_HOST_POSTGRES17 ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-backup.sh --live --source-host 127.0.0.1 --source-port 8745 --pgbouncer-host 10.11.1.11 --pgbouncer-port 6432 --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}"</automated>
  </verify>
  <done>Casos negativos falham e uma execucao live autorizada produz backup.json, dump fresco e checksum valido do PostgreSQL 17 do host; unit, data_directory, PgBouncer e exclusao do PostgreSQL Podman estao comprovados sem segredos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restaurar PostgreSQL estagiado com PV Retain</name>
  <files>scripts/k3s-router-restore-rehearsal.sh</files>
  <behavior>
    - Teste 1: cleanup.json de outro cluster, preflight atual vermelho, bootstrap.json stale/de outro cluster/desvinculado dos manifests, backup invalido, target nao integralmente limpo, Redis/router presentes ou PV fora do srv1 bloqueiam o restore.
    - Teste 2: o script aplica apenas namespace/config/Secret/PostgreSQL, espera bind, descobre todo PV criado pelos PVC UIDs e altera/readback cada reclaim policy para Retain antes de importar.
    - Teste 3: o target PostgreSQL 17 prova schema public integralmente vazio antes da importacao; qualquer erro SQL reverte toda a importacao via psql -X, ON_ERROR_STOP e single transaction.
    - Teste 4: identidade, schema, tabelas e contagens/invariantes esperados precisam corresponder para emitir restore_passed em restore.json.
    - Teste 5: o estado canonico global do target bloqueia rerun entre diretorios distintos; retry explicito aceita somente restore.json no-go com checksum/caminho/cluster validos, arquiva a evidencia anterior e jamais sobrescreve um restore go.
  </behavior>
  <action>Criar o rehearsal com opt-in explicito e entradas K3S_BACKUP_DIR, cleanup.json e bootstrap.json. Tratar cleanup.json como evidencia historica cluster-bound, mas executar preflight atual por cinco minutos antes do restore; exigir bootstrap.json do cluster atual, com no maximo uma hora e manifest_sha256 igual aos manifests correntes. Validar backup.json/checksum/origem, aplicar somente namespace/config/Secret/PostgreSQL sob profile-run e limite de 20% da CPU do host, exigir que o manifest e o Pod PostgreSQL 17 declarem CPU request/limit exatamente 500m, esperar Ready e provar PVC/PV/node. Enumerar PVCs da namespace, resolver PV por spec.volumeName e conferir claimRef UID; aplicar Retain e fazer readback antes de importar. Provar que o target esta integralmente limpo, restaurar atomicamente por psql -X --set ON_ERROR_STOP=on --single-transaction e validar invariantes do backup. Criar restore.json por rename atomico e manter estado canonico atomico em ~/.local/state/router-ai-atius/phase29/restore-target-state.json, ligado a target, cluster UID, caminho absoluto e SHA-256 da evidencia. Em falha registrar no-go, manter Redis/router ausentes e preservar PVC/PV. Bloquear toda repeticao, inclusive entre diretorios distintos, exceto --retry-no-go explicito sobre a evidencia no-go canonica previamente arquivada; estado go e irrepetivel. Excluir Secret YAML, DSN e dados de usuarios da evidencia.</action>
  <verify>
    <automated>PHASE29_EXECUTE=1 PHASE29_RESTORE_CONFIRM=RESTORE_CANONICAL_BACKUP_TO_CLEAN_K3S_POSTGRES17 ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-restore-rehearsal.sh --live --backup-dir "${K3S_BACKUP_DIR:?required}" --cleanup-evidence "${PHASE29_CLEANUP_EVIDENCE:?required}" --bootstrap-evidence "${PHASE29_BOOTSTRAP_EVIDENCE:?required}" --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}" &amp;&amp; jq -e '.status == "go" and .restore_passed == true and .backup.source == "host-postgresql-17" and .target.clean_before_restore == true and (.target.server_version_num | test("^17[0-9]{4}$")) and .runtime_stage.redis_applied == false and .runtime_stage.router_applied == false and all(.pvs[]; .reclaim_policy == "Retain" and .claim_uid_matched == true)' "${PHASE29_EVIDENCE_DIR:?required}/restore.json"</automated>
  </verify>
  <done>O PostgreSQL 17 k3s restaurado atomicamente corresponde ao backup fresco, o target limpo e o retry policy estao provados em restore.json, os PVs estao Retain e o router nunca iniciou contra banco vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL live → arquivo de backup | Dados de producao saem do runtime para artefato protegido fora do Git. |
| backup → PostgreSQL k3s | SQL nao confiavel para o target atravessa psql. |
| PVC → PV local-path | Lifecycle do claim pode apagar dados se reclaim policy permanecer Delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Spoofing | origem PostgreSQL | high | mitigate | confirmar unit, data_directory, major 17, identidade do DB e cross-check PgBouncer antes do pg_dump 17 |
| T-29-06 | Tampering | dump/restore | high | mitigate | SHA-256, target integralmente limpo, transacao unica, ON_ERROR_STOP, retry apenas de no-go arquivado e invariantes pos-restore |
| T-29-07 | Information Disclosure | backup/evidencia | high | mitigate | mode restritivo, fora do Git e evidencia sanitizada sem DSN/Secret |
| T-29-08 | Denial of Service | PV local-path | high | mitigate | patch e read-back de Retain antes do restore; proibir delete no rollback |
| T-29-SC | Tampering | imagens Postgres | high | mitigate | usar imagem previamente auditada e identidade imutavel; sem installs |
</threat_model>

<verification>
Executar backup e restore live somente se 29-01 estiver verde. Confirmar PostgreSQL 17 host em 127.0.0.1:8745, cluster /var/lib/postgresql/17/main, unit postgresql@17-main, PgBouncer 10.11.1.11:6432, pg_dump 17, checksum, cpu.max dentro de 20%, Pods em 500m, cleanup cluster-bound com preflight atual de cinco minutos, bootstrap fresco/manifest-bound, target integralmente limpo, restore atomico, retry apenas de no-go, Retain e invariantes; verificar que Redis e router ainda nao existem.
</verification>

<success_criteria>
Backup fresco validado do PostgreSQL 17 canonico do host e restore atomico completo no PostgreSQL 17 k3s, com target limpo, retry fail-closed, PV Retain, evidencia sem segredos e nenhum router iniciado antes da validacao dos dados.
</success_criteria>

<output>
Create `.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-02-SUMMARY.md` when done
</output>
