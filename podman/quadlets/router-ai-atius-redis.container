# systemd quadlet for Redis

[Unit]
Description=router-ai-atius — Redis 7 (cache + rate limiting)
After=network-online.target

[Container]
Image=docker.io/library/redis:7-alpine
ContainerName=redis-newapi
# Secrets are injected by scripts/podman-quadlets-install.sh.
EnvironmentFile=%h/.config/containers/systemd/router-ai-atius.env
Command=redis-server --requirepass ${REDIS_PASSWORD}
Timezone=local
NoNewPrivileges=true
ReadOnly=true
Tmpfs=/data
HealthCmd=redis-cli -a ${REDIS_PASSWORD} ping
HealthIntervalSec=30s
HealthTimeoutSec=5s
HealthRetries=3

[Service]
Restart=always
RestartSec=10
User=%U
Group=%U

[Install]
WantedBy=default.target
