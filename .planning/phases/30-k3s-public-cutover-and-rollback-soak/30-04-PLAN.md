---
phase: 30-k3s-public-cutover-and-rollback-soak
plan: "04"
type: execute
wave: 4
depends_on:
  - 30-03
files_modified:
  - scripts/k3s-router-podman-retire.sh
  - scripts/k3s-router-host-pg-preserve.sh
  - docs/K3S-MIGRATION.md
  - docs/PODMAN.md
  - .planning/phases/30-k3s-public-cutover-and-rollback-soak/30-OPERATION.md
autonomous: true
requirements:
  - PHASE-22-CUTOVER-ROLLBACK
  - PHASE-20-GO-ONLY-V1-MODELS
  - PHASE-25-CLIENT-SMOKE-VALIDATION
must_haves:
  truths:
    - "Units e containers Podman de router/Redis deixam de executar e iniciar automaticamente somente após evidência PASS do soak."
    - "O PostgreSQL 17 host permanece active/enabled: a auditoria live confirmou dependências de ATS, Horistic, GBrain, Omni Fleet e outras databases."
    - "Data dir, databases, imagens, volumes, dumps, checksums e unit files permanecem preservados por no mínimo 7 dias."
    - "Operação, monitoramento, backup/restore, CLIAnything e recuperação do k3s estão documentados e validados sem dependência de podman exec."
  artifacts:
    - path: "scripts/k3s-router-podman-retire.sh"
      provides: "Aposentadoria allowlisted, guarded e não destrutiva de router/Redis Podman"
    - path: "scripts/k3s-router-host-pg-preserve.sh"
      provides: "Inventário e prova fail-closed de preservação do PostgreSQL 17 host compartilhado"
    - path: ".planning/phases/30-k3s-public-cutover-and-rollback-soak/30-OPERATION.md"
      provides: "Registro final sanitizado da operação e retenção"
    - path: "docs/K3S-MIGRATION.md"
      provides: "Runbook k3s final"
    - path: "docs/PODMAN.md"
      provides: "Status aposentado e recuperação preservada"
  key_links:
    - from: "scripts/k3s-router-podman-retire.sh"
      to: ".planning/phases/30-k3s-public-cutover-and-rollback-soak/30-03-SUMMARY.md"
      via: "gate de soak PASS e checksums"
      pattern: "PASS"
    - from: "scripts/k3s-router-host-pg-preserve.sh"
      to: "/etc/pgbouncer/pgbouncer.ini"
      via: "inventário de todas as databases, mappings e workloads antes de qualquer lifecycle de postgresql@17-main"
      pattern: "postgresql@17-main|DBRouterAiAtius"
    - from: "docs/K3S-MIGRATION.md"
      to: "tools/clianything.py"
      via: "comandos k3s validados para operação do DB"
      pattern: "CLIANYTHING_DB_BACKEND"
---

<objective>
Aposentar router/Redis Podman após o soak aprovado, retirar `DBRouterAiAtius` como source antigo, preservar obrigatoriamente o PostgreSQL 17 host compartilhado e finalizar operação/documentação, implementando D-07, D-08, D-09 e D-10.

Purpose: Tornar k3s o único runtime ativo sem destruir a capacidade de recuperação.
Output: Script de retirement Podman, verificador de preservação PG17, inventário final e runbooks em português.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-CONTEXT.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-RESEARCH.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-VALIDATION.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-01-SUMMARY.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-03-SUMMARY.md
@scripts/podman-admin.sh
@scripts/podman-down.sh
@docs/K3S-MIGRATION.md
@docs/PODMAN.md
@tools/clianything.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar retirement Podman e preservação obrigatória do PostgreSQL 17 host</name>
  <files>scripts/k3s-router-podman-retire.sh, scripts/k3s-router-host-pg-preserve.sh</files>
  <behavior>
    - Soak sem PASS íntegro, checksums divergentes, backend público/DB fora do k3s ou inventário incompleto aborta sem parar qualquer unit/container.
    - Allowlist exata para router/Redis Podman impede wildcard, prune e remoção de imagem, volume, dump ou unit file.
    - PostgreSQL 17 host permanece active/enabled e somente DBRouterAiAtius deixa de ser source; qualquer tentativa de stop/disable falha.
  </behavior>
  <action>Criar dois scripts com self-tests e opt-in live explícito, consumindo evidência PASS checksummed do Plan 30-03 e envelope do Plan 30-01. No script Podman, redescobrir router/Redis e exigir correspondência com allowlist antes de parar/desabilitar user units e remover somente containers/pod aprovados; proibir prune e preservar unit files, imagens, volumes, binds, dumps e checksums. No verificador PG17 host, aplicar D-07 e D-10: provar que PgBouncer já aponta `DBRouterAiAtius` ao k3s, inventariar todas as databases do cluster `/var/lib/postgresql/17/main`, mappings PgBouncer, conexões e workloads; comparar com o baseline live e produzir somente decisão estruturada `KEEP_SERVICE`. Exigir `postgresql@17-main` active/enabled e falhar se qualquer comando/caminho puder parar, desabilitar, dropar ou remover o cluster. Registrar que apenas `DBRouterAiAtius` deixou de ser source; preservar data dir, databases, dumps e unit files por no mínimo 7 dias. Produzir artifacts separados e checksummed, com `--verify-live` validando preserve-set, edge/DB k3s, smoke e CLIAnything. Aplicar D-09 para Vault, CPU <=20% e prova dos Pods em 500m; manter Headroom fora do escopo per D-08.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-podman-retire.sh scripts/k3s-router-host-pg-preserve.sh &amp;&amp; scripts/k3s-router-podman-retire.sh --self-test &amp;&amp; scripts/k3s-router-host-pg-preserve.sh --self-test &amp;&amp; scripts/k3s-router-podman-retire.sh --verify-live "$(scripts/k3s-router-podman-retire.sh --latest-evidence)" &amp;&amp; scripts/k3s-router-host-pg-preserve.sh --verify-live "$(scripts/k3s-router-host-pg-preserve.sh --latest-evidence)"</automated>
  </verify>
  <done>Verifiers provam router/Redis Podman retirados, artifacts preservados e PostgreSQL 17 host active/enabled com decisão KEEP_SERVICE.</done>
</task>

<task type="auto">
  <name>Task 2: Validar operação k3s e fechar evidência</name>
  <files>scripts/k3s-router-podman-retire.sh, scripts/k3s-router-host-pg-preserve.sh, .planning/phases/30-k3s-public-cutover-and-rollback-soak/30-OPERATION.md</files>
  <action>Gerar `30-OPERATION.md` exclusivamente via `--render-operation` a partir dos manifests JSON e SHA256SUMS verificados dos Plans 30-01 a 30-04; o Markdown é uma projeção, nunca a fonte da verdade. O renderer deve incluir IDs/checksums do GO, envelope, configs pré/pós, identidade e contagens 34/0 da topologia original, imagem k3s, PV/PVC, timestamps/duração/contagens do soak, retirement Podman, inventário/decisão `KEEP_SERVICE` PG17, preserve-set e status final, rejeitando artifact ausente, checksum divergente, schema/status inválido ou segredo detectado. Implementar `--verify-operation` para reabrir as fontes estruturadas, executar `sha256sum -c`, recalcular valores exibidos e comparar a projeção. Executar após retirement health/models/non-stream/stream/Responses, query DB via PgBouncer, CLIAnything backend k3s, backup lógico k3s checksummed, Pods/restarts/events/storage, ausência de router/Redis Podman e `postgresql@17-main` active/enabled; registrar nos artifacts, não por texto manual. Fixar retenção mínima de 7 dias, sem descarte automático.</action>
  <verify>
    <automated>PODMAN_EVIDENCE="$(scripts/k3s-router-podman-retire.sh --latest-evidence)" &amp;&amp; PG_EVIDENCE="$(scripts/k3s-router-host-pg-preserve.sh --latest-evidence)" &amp;&amp; scripts/k3s-router-podman-retire.sh --render-operation "$PODMAN_EVIDENCE" --pg-evidence "$PG_EVIDENCE" --output .planning/phases/30-k3s-public-cutover-and-rollback-soak/30-OPERATION.md &amp;&amp; scripts/k3s-router-podman-retire.sh --verify-operation "$PODMAN_EVIDENCE" --pg-evidence "$PG_EVIDENCE" .planning/phases/30-k3s-public-cutover-and-rollback-soak/30-OPERATION.md</automated>
  </verify>
  <done>30-OPERATION é reprodução verificável de artifacts estruturados íntegros; checksums, statuses, edge/DB k3s, smoke, CLIAnything, retirement e retenção são validados mecanicamente.</done>
</task>

<task type="auto">
  <name>Task 3: Finalizar runbooks de produção e recuperação</name>
  <files>docs/K3S-MIGRATION.md, docs/PODMAN.md</files>
  <action>Atualizar docs em português declarando k3s como runtime ativo somente após evidência final; cobrir status, restart/rollout, logs, monitoramento prolongado de 24h, backup, restore, PgBouncer, Apache, CLIAnything k3s e rollback correto: router/Redis via Podman e DB via PostgreSQL 17 host em `127.0.0.1:8745`. Documentar `KEEP_SERVICE` obrigatório para `postgresql@17-main`, a proibição de apagar data dir/databases/dumps/imagens/volumes/units e retenção mínima de 7 dias. Marcar instruções Podman como recuperação de router/Redis, não como fonte de DB. Remover qualquer dependência operacional de `podman exec` para DB. Registrar explicitamente que Headroom não foi implementado e permanece fora do escopo, per D-08.</action>
  <verify>
    <automated>scripts/smoke-docs-links.sh &amp;&amp; rg -n "runtime ativo|24h|backup|restore|PgBouncer|Apache|CLIAnything|7 dias|recuperacao|Headroom" docs/K3S-MIGRATION.md docs/PODMAN.md</automated>
  </verify>
  <done>Os runbooks permitem operar e recuperar k3s, descrevem Podman somente como rollback de router/Redis e PostgreSQL 17 host como rollback de DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| systemd user→Podman | Retirement altera lifecycle e autostart de router/Redis antigos. |
| systemd/PgBouncer→PostgreSQL 17 host | Retirement de DB pode afetar outras databases e workloads compartilhadas. |
| Operador→k3s/CLIAnything | Operação futura precisa funcionar sem acesso aos containers Podman de router/Redis. |
| Backups→retenção | Artefatos críticos ficam armazenados após remover containers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-13 | Denial of Service | retirement | critical | mitigate | Gate PASS, allowlist, pós-smoke e auto-recuperação pelas units preservadas. |
| T-30-14 | Tampering | artefatos rollback | high | mitigate | Inventário de digest/checksum antes/depois e proibição de prune/remoção. |
| T-30-15 | Elevation of Privilege | lifecycle systemd | medium | mitigate | Comandos exatos e limitados às units allowlisted; sem glob/wildcard. |
| T-30-16 | Information Disclosure | operation docs | high | mitigate | Evidência sanitizada e negative scan de token/DSN/senha. |
| T-30-17 | Denial of Service | postgresql@17-main | critical | mitigate | Inventariar databases, mappings, conexões e workloads; manter service se existir qualquer dependência. |
</threat_model>

<verification>
Executar self-tests, retirement live somente com soak PASS, depois matriz pública, DB/CLIAnything, backup, prova de ausência do router/Redis Podman ativo e decisão checksummed sobre o PG17 host.
</verification>

<success_criteria>
- k3s é o único runtime ativo do router, DBRouterAiAtius e cache no escopo aprovado.
- Router/Redis Podman não executam nem reiniciam automaticamente, mas seus artefatos recuperáveis permanecem íntegros.
- PostgreSQL 17 host permanece active/enabled por ser compartilhado; nenhum dado é apagado.
- Runbooks e registro final cobrem operação, monitoramento, backup/restore, rollback e retenção.
</success_criteria>

<output>
Create `.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-04-SUMMARY.md` when done
</output>
