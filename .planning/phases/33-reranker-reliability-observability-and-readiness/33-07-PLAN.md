---
phase: 33-reranker-reliability-observability-and-readiness
plan: "07"
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - docs/ATIUS-RERANKER-RELIABILITY.md
autonomous: true
requirements:
  - PHASE-33-TEI-TIMEOUT-RETRY
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "The rerank fallback timeout is selected from sanitized live latency and outer-deadline evidence before code locks a number."
    - "No fixed numeric assumption is privileged; the selected fallback exactly matches the deterministic recorded-input formula."
    - "Missing or contradictory evidence stops before TEI implementation and promotion."
  artifacts:
    - path: "docs/ATIUS-RERANKER-RELIABILITY.md"
      provides: "Numeric timeout decision, evidence window, formula, and rollback note"
  key_links:
    - from: "docs/ATIUS-RERANKER-RELIABILITY.md"
      to: "dto.ChannelSettings.RerankTimeoutSeconds"
      via: "Plan 33-08 reads the exact selected seconds value"
      pattern: "Rerank timeout default: [0-9]+ seconds"
---

<objective>
Resolve the rerank inference timeout from SLO/runtime evidence before implementation.

Purpose: Prevent any presumed numeric fallback from entering code without a reproducible bounded operational calculation.
Output: A sanitized, deterministic timeout decision consumed by Plan 33-08.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/debug/reranker-errors-last-24h.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-02-SUMMARY.md
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@/home/ubuntu/.config/systemd/user/container-router-ai-atius.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect sanitized SLO, latency, and outer-deadline evidence</name>
  <files>docs/ATIUS-RERANKER-RELIABILITY.md</files>
  <read_first>.planning/debug/reranker-errors-last-24h.md, docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md, /home/ubuntu/.config/systemd/user/container-router-ai-atius.service, relay/rerank_handler.go, relay/channel/api_request.go</read_first>
  <action>Read the effective Router/Apache/TEI deadline configuration and the latest available successful rerank latency samples from aggregate DB/log evidence without selecting or printing request bodies. Record machine-parseable lines for evidence window, successful sample count, observed p99 seconds, observed maximum seconds, documented SLO seconds, outer deadline seconds, and `Formula margin seconds: 5`. Restore the resolved calculation exactly: `required_seconds = ceil_to_5(max(15, 2*observed_p99_seconds+5, observed_max_seconds+5, documented_slo_seconds+5))`; accept it only when it is at most 120 and at most `outer_deadline_seconds-5`. Do not test or privilege 60. If no successful sample exists, an input is missing/contradictory, or the computed value exceeds the outer bound, stop fail-closed and surface the missing facts before Plan 33-08.</action>
  <verify>
    <automated>python3 -c 'import math,re; from pathlib import Path; s=Path("docs/ATIUS-RERANKER-RELIABILITY.md").read_text(); val=lambda k: float(re.search(rf"^{re.escape(k)}: ([0-9.]+)$",s,re.M).group(1)); samples=int(val("Successful samples")); p99=val("Observed p99 seconds"); observed_max=val("Observed max seconds"); slo=val("Documented SLO seconds"); outer=val("Outer deadline seconds"); margin=val("Formula margin seconds"); chosen=int(val("Computed rerank timeout seconds")); expected=int(math.ceil(max(15,2*p99+5,observed_max+5,slo+5)/5)*5); assert samples&gt;0 and margin==5 and chosen==expected and chosen&lt;=120 and chosen&lt;=outer-5 and "No payload content retained: yes" in s'</automated>
  </verify>
  <acceptance_criteria>
    - Evidence includes at least one successful sample or execution stops before writing a default.
    - The numeric calculation is reproducible from sanitized counts/latencies and documented deadlines.
    - No token, query, document, prompt, provider body, or credential appears in the file/output.
  </acceptance_criteria>
  <done>Sufficient sanitized evidence exists to choose one bounded numeric default.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the exact timeout decision and implementation contract</name>
  <files>docs/ATIUS-RERANKER-RELIABILITY.md</files>
  <read_first>docs/ATIUS-RERANKER-RELIABILITY.md, dto/channel_settings.go, .planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md</read_first>
  <action>Write exactly one line `Rerank timeout default: N seconds`, with N equal to the recomputed Task 1 value. Document that positive per-channel `rerank_timeout_seconds` overrides N, absent/zero uses the evidence-selected N even when `RELAY_TIMEOUT=0`, parent cancellation always wins, and rollback removes only the new setting/code while preserving the existing governed reranker. Record all sanitized formula inputs and the selected N in the Plan 33-07 summary so Plan 33-08 can implement the result without reopening the decision or introducing a presumed 60-second literal.</action>
  <verify>
    <automated>python3 -c 'import math,re; from pathlib import Path; s=Path("docs/ATIUS-RERANKER-RELIABILITY.md").read_text(); val=lambda k: float(re.search(rf"^{re.escape(k)}: ([0-9.]+)$",s,re.M).group(1)); p99=val("Observed p99 seconds"); observed_max=val("Observed max seconds"); slo=val("Documented SLO seconds"); outer=val("Outer deadline seconds"); margin=val("Formula margin seconds"); computed=int(val("Computed rerank timeout seconds")); m=re.findall(r"^Rerank timeout default: ([0-9]+) seconds$",s,re.M); expected=int(math.ceil(max(15,2*p99+5,observed_max+5,slo+5)/5)*5); assert len(m)==1 and margin==5 and computed==expected and int(m[0])==expected and expected&lt;=120 and expected&lt;=outer-5'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one selected integer between 15 and 120 is documented.
    - Both `Computed rerank timeout seconds` and `Rerank timeout default` exactly equal `ceil_to_5(max(15, 2*p99+5, observed_max+5, slo+5))` from the sanitized evidence.
    - The selected value is at most `outer_deadline_seconds-5`.
    - Plan 33-08 can read one exact value from this file/summary.
  </acceptance_criteria>
  <done>The timeout is evidence-backed and locked before relay code changes.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `docs/ATIUS-RERANKER-RELIABILITY.md` with the exact `Rerank timeout default: N seconds` decision and sanitized evidence.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Live telemetry/config to documentation | Operational evidence must be sanitized and not become secret storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-19 | Information Disclosure | evidence capture | high | mitigate | Store counts/latencies/deadlines only and run explicit content review. |
| T-33-20 | Denial of Service | timeout choice | high | mitigate | Deterministic lower/upper/outer-bound formula and fail-closed missing-data gate. |
</threat_model>

<verification>
Verify the evidence and exact decision lines with the two Python assertions; do not proceed if Task 1 cannot establish a safe value.
</verification>

<success_criteria>
- One exact timeout is selected before Plan 33-08.
- No fixed 60-second assumption enters the implementation contract.
- Evidence is sanitized and reproducible.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-07-SUMMARY.md` when done, including the selected seconds value.
</output>
