---
phase: phase-20-go-native-model-router
plan: "05"
type: execute
wave: 3
depends_on:
  - "20-03"
  - "20-04"
files_modified:
  - service/embeddinggovernor/governor.go
  - service/embeddinggovernor/governor_test.go
  - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
autonomous: true
requirements:
  - PHASE-20-GRAPHIFY-GATE
  - PHASE-20-CLI-DOCS-RUNTIME-PARITY
  - PHASE-20-UPSTREAM-SYNC-GUARD
  - PHASE-20-SDK-SMOKES
user_setup:
  - service: atius-router
    why: "Runtime smoke and production monitor validation use the existing operational router token and environment."
    env_vars:
      - name: ATIUS_ROUTER_TOKEN
        source: "Existing local/operator environment; do not write it into files."
must_haves:
  truths:
    - TEI health influence is disabled by default and read-only when enabled.
    - A single health timeout never reduces concurrency by itself.
    - Health-derived reductions require consecutive bad windows and still respect `min=1`, auto `max=3`.
    - Operator docs describe the final governor behavior, env vars, smoke commands, Graphify freshness, and production monitor gates.
    - Production restart/deploy remains a validation action, not an automatic plan action.
  artifacts:
    - service/embeddinggovernor/governor.go
    - service/embeddinggovernor/governor_test.go
    - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
  key_links:
    - optional health sampler -> governor hysteresis state
    - governor snapshot -> operator docs
    - local tests -> runtime smoke -> Graphify status
---

<objective>
Add the allowed read-only TEI health guardrail with hysteresis and update the operational manual/validation gates for the enhanced governor.

Purpose: let operators enable advisory TEI health influence safely while documenting the production rules that prevent noisy health probes from causing harmful downscale.
Output: disabled-by-default health signal, tests, and updated manual validation steps.
</objective>

## Artifacts this phase produces

- Env-gated TEI health signal with consecutive-window hysteresis.
- Tests proving one health timeout is insufficient to reduce concurrency.
- Updated operational manual for classification, split metrics, failure semantics, health guardrail, smoke tests, Graphify freshness, and production monitor gates.

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/phase-20-go-native-model-router/20-RESEARCH.md
@.planning/phases/phase-20-go-native-model-router/20-PATTERNS.md
@.planning/phases/phase-20-go-native-model-router/20-VALIDATION.md
@.planning/phases/phase-20-go-native-model-router/20-UAT.md
@.planning/phases/phase-20-go-native-model-router/20-03-SUMMARY.md
@.planning/phases/phase-20-go-native-model-router/20-04-SUMMARY.md
@service/embeddinggovernor/governor.go
@service/embeddinggovernor/governor_test.go
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
@/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-26-router-embedding-governor-go-native.md
@/home/ubuntu/GitHub/obsidian-vault/ideaverse/60-LOGS/2026-06-26-gbrain-obsidian-tei-atius-router-service-management.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add disabled-by-default TEI health hysteresis</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    - `service/embeddinggovernor/governor.go`: `Config`, `LoadConfigFromEnv`, `normalizeConfig`, `Governor`, `Snapshot`, `canIncreaseLocked`, `finish`.
    - `20-RESEARCH.md`: "Optional TEI health probing", "Security Domain", and "Known Threat Patterns".
    - Obsidian GBrain/TEI log: "Gatilhos proativos candidatos" and "Não confiar em `/health` curto".
  </read_first>
  <behavior>
    - RED: default config leaves TEI health probing disabled and performs no network probe.
    - RED: one failed/slow health sample does not reduce concurrency and does not start cooldown.
    - RED: the configured number of consecutive bad health windows can block scale-up and reduce toward min.
    - RED: healthy samples reset the bad-window counter.
  </behavior>
  <action>
    Add an env-gated, read-only TEI health signal inside `service/embeddinggovernor` using only Go stdlib HTTP/context primitives. Add config/env fields for enable flag, health URL, timeout, probe interval, bad-window threshold, and slow-health duration. Keep it disabled unless both an enable flag and health URL are configured. Do not add a Kubernetes client, new process, sidecar, container, or persistent storage. Use a timeout default of at least 30 seconds and a consecutive bad-window threshold default of 3. Treat bad health as advisory hysteresis: one timeout or one slow sample must not reduce concurrency, while sustained bad samples can block scale-up and reduce current concurrency no lower than `MinConcurrency`. Do not include credentials in the health URL logs/snapshot, do not follow any path that needs secrets, and add only aggregate snapshot fields: `health_probe_enabled`, `health_bad_windows`, `last_health_status`, `last_health_latency_ms`, and `last_health_at`.
  </action>
  <acceptance_criteria>
    - `LoadConfigFromEnv` defaults keep health probing disabled.
    - Config normalization prevents bad thresholds/timeouts from becoming aggressive.
    - Tests prove one health timeout under inference cannot reduce concurrency by itself.
    - Tests prove consecutive bad windows trigger the documented guardrail and healthy windows reset it.
    - Existing governor tests remain deterministic and do not require live TEI/Kubernetes access.
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestHealthProbeDisabledByDefault|TestHealthHysteresisIgnoresSingleBadSample|TestHealthHysteresisReducesAfterConsecutiveBadWindows|TestHealthHysteresisHealthySampleResetsBadWindows)$' -count=1</automated>
  </verify>
  <done>Health influence is available as a safe, disabled-by-default guardrail with tested hysteresis.</done>
</task>

<task type="auto">
  <name>Task 2: Update operator docs and validation gates</name>
  <files>docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md</files>
  <read_first>
    - `docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md`: "Governor de embeddings Go-native", "GBrain", "Operacao diaria", "Protecao contra sync upstream".
    - `20-UAT.md` and `20-VALIDATION.md`: current full-Go runtime validation evidence.
    - Obsidian logs listed in this plan context for the current TEI/GBrain operating envelope.
  </read_first>
  <action>
    Update the governor section of the manual to document metadata-derived workload classification, split interactive/batch metrics, pressure failure classification, and the disabled-by-default TEI health guardrail. Add any new env vars with defaults and operational meaning. Preserve the explicit production constraints: the Go router owns the path, no retired middleware owner is in the runtime path, `min=1`, `initial=2`, automatic `max=3`, and `4` only for manual/turbo windows. Add validation steps for focused Go tests, embeddings smoke with dimension 768, Graphify freshness after code/docs changes, and production monitor gates: TEI ready, restarts zero, sustained health behavior, TEI CPU/memory, GBrain progress, and zero embed errors. State that production restart/deploy is not automatic in plan execution; it is a controlled validation step through the existing user systemd service.
  </action>
  <acceptance_criteria>
    - Manual describes all new governor env vars and safe defaults.
    - Manual says health signal is disabled by default and one timeout is insufficient to downscale.
    - Manual keeps tokens/secrets redacted and uses env placeholders only.
    - Manual keeps runtime restart guidance on `systemctl --user restart container-router-ai-atius.service`, not direct container restart.
    - Manual keeps protected Go-native routing/governor paths listed for upstream sync protection.
  </acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./common ./controller ./service/modelcatalog ./relay/common ./service/embeddinggovernor ./relay -count=1</automated>
    <automated>test -n "$ATIUS_ROUTER_TOKEN" && ATIUS_ROUTER_EMBEDDINGS_BASE_URL=http://127.0.0.1:3000/v1 ATIUS_ROUTER_EMBEDDINGS_MODEL=embedding-pt-v1 ATIUS_ROUTER_EXPECT_EMBEDDING_DIM=768 python3 scripts/smoke-embeddings.py</automated>
    <automated>node /home/ubuntu/.codex/gsd-core/bin/gsd-tools.cjs graphify status</automated>
  </verify>
  <done>Operator manual and validation gates match the enhanced Go-native governor and current production baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Env config -> governor | Operator-provided health URL and thresholds influence optional monitoring. |
| Governor -> TEI health endpoint | Router may perform read-only health checks when explicitly enabled. |
| Governor -> docs/operators | Snapshot/manual fields communicate operational state. |
| Production validation -> router | Smoke tests use existing token environment without writing secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-05-01 | Denial of Service | TEI health signal | high | mitigate | Disabled by default, timeout at least 30s, consecutive bad-window hysteresis, one bad sample cannot reduce concurrency. |
| T-20-05-02 | Information Disclosure | Health URL/docs/smoke validation | high | mitigate | Do not log credentials, do not write tokens, document env placeholders only, and keep smoke token in process env. |
| T-20-05-03 | Tampering | Health URL configuration | medium | mitigate | URL is operator env only; no client request can set it; no Kubernetes write/client behavior is introduced. |
| T-20-05-04 | Denial of Service | Production deployment validation | medium | mitigate | Restart/deploy remains controlled through user systemd service and is not an automatic task action. |
| T-20-05-SC | Tampering | Package supply chain | low | accept | No package install is planned; implementation uses Go stdlib and existing scripts. |
</threat_model>

<verification>
Overall phase follow-up gate:
<automated>/usr/local/go/bin/go test ./dto ./service/embeddinggovernor ./relay -count=1</automated>
<automated>/usr/local/go/bin/go test ./common ./controller ./service/modelcatalog ./relay/common ./service/embeddinggovernor ./relay -count=1</automated>
<automated>test -n "$ATIUS_ROUTER_TOKEN" && ATIUS_ROUTER_EMBEDDINGS_BASE_URL=http://127.0.0.1:3000/v1 ATIUS_ROUTER_EMBEDDINGS_MODEL=embedding-pt-v1 ATIUS_ROUTER_EXPECT_EMBEDDING_DIM=768 python3 scripts/smoke-embeddings.py</automated>
<automated>node /home/ubuntu/.codex/gsd-core/bin/gsd-tools.cjs graphify status</automated>
Production monitor gate after controlled deploy/restart: `bin/clianything status --strict`; TEI pod ready=true; restarts=0; `/health` behavior interpreted over consecutive windows; GBrain embed errors=0; `embedding-pt-v1` smoke returns dimension 768. Do not write operational tokens to files or logs.
</verification>

<success_criteria>
- Health signal is safe by default and cannot react to a single noisy `/health` timeout.
- Docs reflect the new governor behavior and current TEI/GBrain baseline.
- Final verification covers Go tests, embeddings smoke, Graphify freshness, and production monitor gates.
- No retired middleware owner, Python relay, sidecar, or extra container is introduced.
</success_criteria>

<output>
Create `.planning/phases/phase-20-go-native-model-router/20-05-SUMMARY.md` when done.
</output>
