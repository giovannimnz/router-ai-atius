---
phase: 30-k3s-public-cutover-and-rollback-soak
plan: "02"
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - scripts/k3s-router-cutover.sh
  - scripts/k3s-router-rollback.sh
  - docs/K3S-MIGRATION.md
autonomous: true
requirements:
  - PHASE-22-CUTOVER-ROLLBACK
must_haves:
  truths:
    - "Somente DBRouterAiAtius muda no PgBouncer, de Postgres Podman para ClusterIP:5432, e todas as outras databases permanecem idênticas."
    - "Somente upstreams Apache do router em 127.0.0.1:3000 mudam para ClusterIP:3000; rotas 127.0.0.1:3003 permanecem byte-for-byte intactas."
    - "PgBouncer e Apache possuem rollback independente, testado e acionável sem depender da outra troca."
  artifacts:
    - path: "scripts/k3s-router-cutover.sh"
      provides: "Cutover atômico e ordenado PgBouncer→Apache"
    - path: "scripts/k3s-router-rollback.sh"
      provides: "Rollback independente por componente e rollback completo"
    - path: "docs/K3S-MIGRATION.md"
      provides: "Comandos e invariantes do cutover/rollback"
  key_links:
    - from: "scripts/k3s-router-cutover.sh"
      to: "/etc/pgbouncer/pgbouncer.ini"
      via: "candidato atômico limitado à entrada DBRouterAiAtius"
      pattern: "DBRouterAiAtius"
    - from: "scripts/k3s-router-cutover.sh"
      to: "/etc/apache2/sites-enabled/router.atius.com.br-le-ssl.conf"
      via: "candidato atômico limitado ao target do router"
      pattern: "127\\.0\\.0\\.1:3000"
---

<objective>
Executar as duas trocas live ordenadas e reversíveis: primeiro DBRouterAiAtius no PgBouncer, depois somente os upstreams router do Apache, implementando D-01, D-02, D-03 e D-06.

Purpose: Mover banco e tráfego público para k3s sem configuração mista e com rollback independente.
Output: Scripts guarded de cutover/rollback e runbook operacional.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-CONTEXT.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-RESEARCH.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-VALIDATION.md
@.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-01-SUMMARY.md
@scripts/k3s-router-cutover-checklist.sh
@scripts/k3s-router-rollback-check.sh
@docs/K3S-MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repoint atômico exclusivo de DBRouterAiAtius</name>
  <files>scripts/k3s-router-cutover.sh, scripts/k3s-router-rollback.sh</files>
  <behavior>
    - Caso 1: fixture com exatamente uma entrada DBRouterAiAtius no backend Podman gera candidato mudando somente host/port para ClusterIP:5432.
    - Caso 2: entrada ausente, duplicada, já divergente ou qualquer alteração colateral aborta antes do install/reload.
    - Caso 3: rollback `--pgbouncer-only` restaura checksum original e confirma query pelo backend Podman sem tocar Apache.
  </behavior>
  <action>Criar scripts com modos fixture e live, exigindo envelope READY do Plan 30-01. Em `--stage pgbouncer`, validar endpoint PostgreSQL k3s e conexão antes da mutação; gerar candidato no mesmo filesystem, preservar owner/mode, comparar representação sanitizada de todas as databases e permitir mudança apenas em host/port de DBRouterAiAtius, per requisito explícito e D-03. Validar candidato com mecanismo compatível com a instalação, instalar atomicamente, executar RELOAD, WAIT_CLOSE e SHOW DATABASES sanitizado, abrir nova conexão via 127.0.0.1:6432 e provar backend k3s. Em qualquer falha, restaurar apenas PgBouncer, recarregar, drenar e provar DB Podman. Nunca registrar credenciais.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-cutover.sh scripts/k3s-router-rollback.sh &amp;&amp; scripts/k3s-router-cutover.sh --self-test pgbouncer &amp;&amp; scripts/k3s-router-rollback.sh --self-test pgbouncer</automated>
  </verify>
  <done>A fixture prova alteração exclusiva, e a etapa live só conclui com pool drenado e query nova no Postgres k3s; rollback PgBouncer funciona isoladamente.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget atômico exclusivo dos upstreams router Apache</name>
  <files>scripts/k3s-router-cutover.sh, scripts/k3s-router-rollback.sh</files>
  <behavior>
    - Caso 1: fixture válida troca todas e somente as ocorrências do target router aprovado para ClusterIP:3000.
    - Caso 2: bloco de docs/assets em 127.0.0.1:3003 mantém checksum de região idêntico antes/depois.
    - Caso 3: configtest/reload/health falho restaura automaticamente o vhost original sem reverter PgBouncer; `--apache-only` também pode ser acionado explicitamente.
  </behavior>
  <action>Em `--stage apache`, exigir conclusão comprovada da etapa PgBouncer e revalidar Service/EndpointSlice router. Resolver o arquivo enabled realmente carregado, inventariar os targets atuais e gerar candidato que substitui somente o antigo target 127.0.0.1:3000 pelo ClusterIP:3000 aprovado, incluindo pares ProxyPass/ProxyPassReverse e regras equivalentes. Não confiar para sempre nas contagens históricas 16/38: exigir baseline do envelope e igualdade estrutural de tudo fora do target autorizado. Validar candidato e configuração instalada com `apachectl configtest`, fazer graceful reload, checar active e health público. Se qualquer gate falhar, executar rollback Apache per D-06, configtest/reload e smoke Podman, mantendo PgBouncer no estado anterior à falha para permitir diagnóstico e rollback independente. D-01 exige preservar 3003 byte-for-byte; D-02 exige registrar ClusterIP e checksums.</action>
  <verify>
    <automated>scripts/k3s-router-cutover.sh --self-test apache &amp;&amp; scripts/k3s-router-rollback.sh --self-test apache</automated>
  </verify>
  <done>Somente os upstreams router mudam, docs/assets ficam intactos, e rollback Apache é comprovado sem depender do rollback PgBouncer.</done>
</task>

<task type="auto">
  <name>Task 3: Executar cutover serial e registrar procedimento independente</name>
  <files>scripts/k3s-router-cutover.sh, scripts/k3s-router-rollback.sh, docs/K3S-MIGRATION.md</files>
  <action>Documentar e executar autonomamente a ordem `preflight READY → pgbouncer → prova DB → apache → health`, incluindo comandos exatos para `--pgbouncer-only`, `--apache-only` e rollback completo inverso Apache→PgBouncer. Fazer os scripts produzirem `manifest.json`, JSON sanitizado e `SHA256SUMS`; `--latest-evidence` deve resolver univocamente o artifact completo ligado ao GO atual e falhar em ausência/ambiguidade. Implementar `--verify-live` que parseia o artifact, executa `sha256sum -c` e falha salvo se: o diff/checksums PgBouncer permitem somente host/port de DBRouterAiAtius e provam todas as demais databases idênticas; SHOW DATABASES está sanitizado; uma conexão nova via 127.0.0.1:6432 prova o ClusterIP PostgreSQL; Apache configtest/reload passaram; o target router instalado é o ClusterIP aprovado; a região `:3003` preserva checksum; e health/smoke pós-cutover passou. Persistir apenas hashes, targets não sensíveis, status e timestamps. Não parar Podman e não implementar Headroom per D-08. Se um gate falhar, finalizar com rollback comprovado e status NO-GO, sem prosseguir ao Plan 30-03.</action>
  <verify>
    <automated>EVIDENCE="$(scripts/k3s-router-cutover.sh --latest-evidence)" &amp;&amp; scripts/k3s-router-cutover.sh --verify-live "$EVIDENCE" &amp;&amp; scripts/smoke-docs-links.sh</automated>
  </verify>
  <done>O artifact live checksummed prova diff allowlisted, demais databases idênticas, SHOW DATABASES/query 6432, configtest, ClusterIPs, preservação de :3003 e smoke; ou a execução retorna ao estado seguro com rollback comprovado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PgBouncer→PostgreSQL k3s | Novas conexões de produção passam ao banco restaurado. |
| Apache→Service router k3s | Tráfego TLS público cruza do host para ClusterIP. |
| Scripts→config host | Automação privilegiada substitui arquivos live e recarrega serviços. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-05 | Tampering | pgbouncer.ini | critical | mitigate | Diff allowlisted para uma entrada, candidato atômico, checksum, reload/drain e rollback isolado. |
| T-30-06 | Tampering | vhost Apache | critical | mitigate | Diff limitado ao target router, checksum regional de 3003, configtest e restore automático. |
| T-30-07 | Denial of Service | reloads | high | mitigate | Endpoint preflight, validação antes/depois, graceful reload e health imediato. |
| T-30-08 | Information Disclosure | consoles/evidence | high | mitigate | SHOW DATABASES sanitizado e nenhuma persistência de DSN, senha, token ou header. |
</threat_model>

<verification>
Executar self-tests com fixtures antes do live; no live, comprovar checksums, PgBouncer drain/query e Apache configtest/health, mantendo Podman ativo.
</verification>

<success_criteria>
- PgBouncer aponta DBRouterAiAtius ao Postgres ClusterIP sem alterar outras databases.
- Apache envia apenas rotas router ao Service ClusterIP; 3003 permanece intacto.
- Rollbacks Apache e PgBouncer funcionam separadamente e em conjunto.
</success_criteria>

<output>
Create `.planning/phases/30-k3s-public-cutover-and-rollback-soak/30-02-SUMMARY.md` when done
</output>
