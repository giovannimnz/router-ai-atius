---
phase: 24-router-db-catalog-recovery-and-canonical-host-db
plan: "04"
type: execute
wave: 4
depends_on:
  - "24-03"
files_modified:
  - tools/clianything.py
  - tests/test_clianything.py
  - docs/PODMAN.md
  - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PHASE-24-CANONICAL-HOST-DB
  - PHASE-24-CUTOVER-ROLLBACK
  - PHASE-24-EMBEDDING-GOVERNOR-PRESERVE
must_haves:
  truths:
    - D-12 Runtime cutover is manual and reversible.
    - D-13 CLI/documentation defaults must match the final canonical DB name.
    - D-14 End-to-end validation must cover Codex, DeepSeek, embeddings, and provider enabled/disabled state.
  artifacts:
    - tools/clianything.py
    - tests/test_clianything.py
    - docs/PODMAN.md
    - docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
  key_links:
    - PgBouncer mapping -> router unit -> clianything default DB name
    - restored catalog -> public API validation -> rollback gate
---

<objective>
Perform the final runtime cutover and reconcile CLI/docs/validation with the canonical host DB.

Purpose: switch the live router to the final DB name via PgBouncer, validate the recovered catalog, and leave a precise rollback path.
</objective>

## Artifacts this plan produces

- Final `clianything` host DB default alignment
- Final runtime/docs parity for DB path and provider state
- Final validation and rollback record in `.planning/STATE.md`

<context>
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-CONTEXT.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-RESEARCH.md
@.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-VALIDATION.md
@/home/ubuntu/.config/systemd/user/container-router-ai-atius.service
@tools/clianything.py
@tests/test_clianything.py
@docs/PODMAN.md
@docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md
</context>

<tasks>

<task type="checkpoint">
  <name>Task 1: Apply the manual cutover to the canonical DB name</name>
  <action>
    Update PgBouncer mapping and the live router unit so the runtime points to the final canonical DB name via host PgBouncer. Restart only through user systemd. This task is a checkpoint because it mutates the active runtime DB target.
  </action>
  <acceptance_criteria>
    - `container-router-ai-atius.service` points to the canonical DB name via `10.1.1.1:6432`.
    - `newapi` remains intact for rollback.
    - Router restarts cleanly.
  </acceptance_criteria>
  <verify>
    <manual>Confirm backup paths and operator go/no-go before runtime mutation.</manual>
    <automated>systemctl --user status container-router-ai-atius.service --no-pager</automated>
  </verify>
  <done>Live router points to the canonical host DB name.</done>
</task>

<task type="auto">
  <name>Task 2: Align CLI defaults and tests to the final DB name</name>
  <files>tools/clianything.py, tests/test_clianything.py</files>
  <action>
    Update `clianything` host-mode defaults and its tests so the canonical DB name/path used after cutover is the default operational assumption, with explicit legacy fallback only where required.
  </action>
  <acceptance_criteria>
    - `clianything` defaults reference the final canonical DB name for host mode.
    - tests cover host default plus legacy fallback.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m unittest tests.test_clianything</automated>
  </verify>
  <done>CLIAnything matches the final runtime DB target.</done>
</task>

<task type="auto">
  <name>Task 3: Run end-to-end validation and document rollback</name>
  <files>docs/PODMAN.md, docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md, .planning/STATE.md</files>
  <action>
    Validate and document:
    - `bin/clianything status --strict`
    - authenticated `/v1/models`
    - authenticated GPT/Codex chat checks
    - authenticated DeepSeek checks
    - authenticated `/v1/embeddings` for `embedding-gte-v1`
    - MiniMax restored but disabled
    - exact rollback steps back to the preserved `newapi` target if needed
  </action>
  <acceptance_criteria>
    - Docs/state include the final canonical DB name and rollback instructions.
    - Validation explicitly states that GPT/Codex are back and `-1m` aliases are absent.
    - Validation explicitly states that DeepSeek is active and MiniMax is restored but disabled.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "rollback|DBRouterAiAtius|newapi|OpenAI - Codex|DeepSeek|MiniMax|embedding-gte-v1" docs/PODMAN.md docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md .planning/STATE.md</automated>
  </verify>
  <done>Recovery is verifiably complete and reversible.</done>
</task>

</tasks>

<verification>
<automated>python3 -m unittest tests.test_clianything</automated>
<automated>rg -n "DBRouterAiAtius|newapi|rollback|OpenAI - Codex|DeepSeek|MiniMax|embedding-gte-v1" docs/PODMAN.md docs/MANUAL-OPERACAO-ROUTER-AI-ATIUS.md .planning/STATE.md</automated>
</verification>

<success_criteria>
- Live runtime points to the canonical host DB name via PgBouncer.
- CLI/docs/test defaults match the final runtime.
- Public/API validation proves the restored catalog and provider state.
</success_criteria>

<output>
Create `.planning/phases/24-router-db-catalog-recovery-and-canonical-host-db/24-04-SUMMARY.md` when done.
</output>
