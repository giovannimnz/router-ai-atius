---
phase: 33-reranker-reliability-observability-and-readiness
plan: "15"
type: execute
wave: 8
depends_on:
  - "33-14"
files_modified:
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE/
autonomous: true
requirements:
  - PHASE-33-WEIGHTED-DASHBOARD
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Only the already validated immutable candidate digest is promoted to the production user service."
    - "Any failed post-promotion gate automatically restores and verifies the captured immutable rollback digest, then exits nonzero."
    - "Local, public, readiness, rerank-boundary, quota, API, and two independent headless dashboard gates pass on the live runtime."
    - "A mixed legacy/schema-v2 24-hour window passes only as consistent partial data; an all-v2 window passes only with exact weighted formulas."
  artifacts:
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
      provides: "Sanitized promotion/rollback identities and direct live gate matrix"
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
      provides: "Machine-readable transaction, runtime, metrics-state, promotion, and rollback results"
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE/
      provides: "Separate sanitized DOM, screenshot, and trace evidence for overview and models routes"
  key_links:
    - from: .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
      to: container-router-ai-atius.service
      via: "validated candidate digest and immutable old-live digest"
    - from: /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
      to: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
      via: "single promotion transaction with rollback trap and ordered direct gates"
    - from: /api/perf-metrics/summary?hours=24
      to: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE/
      via: "state-aware complete-or-partial API values verified on both real dashboard routes"
---

<objective>
Promote the validated Phase 33 candidate transactionally and capture direct live evidence.

Purpose: Make reranker reliability, readiness, bounded TEI behavior, the 20-document contract, weighted observability, and fork-sync protection true in production without misclassifying legitimate legacy history.
Output: Either a verified candidate digest live in production or a verified rollback with nonzero plan status, plus sanitized API/runtime/headless-UI evidence.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-14-SUMMARY.md
@scripts/smoke-reranker-readiness.py
@scripts/atius-user-quota-guard.sh
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
@/home/ubuntu/.config/systemd/user/container-router-ai-atius.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze validated promotion and rollback identities</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-14-SUMMARY.md
    - /home/ubuntu/.config/systemd/user/container-router-ai-atius.service
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
  </read_first>
  <action>Require the retained Omni worktree path to be exactly `/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified`, its porcelain status empty, and its HEAD equal to both expected/origin-main commit fields. Invoke Omni only through absolute paths under that worktree. Recompute the deterministic planning manifest from the sorted immutable Phase 33 plan set (`33-*-PLAN.md`, CONTEXT, RESEARCH, UI-SPEC, VALIDATION), require exact equality with the candidate manifest/hash, and copy both exact values to live evidence before mutation. Re-inspect candidate source/capability labels, require `execution_identity` to equal exactly the candidate OCI `org.opencontainers.image.revision`, copy it to live evidence, and verify protected carriers, exact governed aliases/channel 10/`tei_rerank.go`, the user-quota guard, and `deploy.sh --verify-candidate`. Require `33-CANDIDATE-EVIDENCE.json.v2_metrics_proof` to prove disposable isolated SQLite storage, no production mutation, all seven counters, per-row sums, model-to-overall raw totals, exact request-success and service-availability formulas, both null zero denominators, the 8/9 incident, and cleanup PASS. Freeze direct old-live immutable digest, configured image reference, unit hash/status, runtime aliases, readiness and sanitized DB/config identity. Do not promote when any field differs. Retain the clean worktree for Task 2 and Plan 33-16.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail
cev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
lev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
omni=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified" and d["expected_commit"]==d["origin_main_commit"]; print(d["worktree_path"])'"'"' "$manifest")
expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest")
test "$(git -C "$omni" rev-parse HEAD)" = "$expected"
test -z "$(git -C "$omni" status --porcelain)"
planning=$(python3 -c '"'"'import hashlib,json,re,sys; from pathlib import Path; cev,lev=map(Path,sys.argv[1:]); phase=Path(".planning/phases/33-reranker-reliability-observability-and-readiness"); paths=sorted([*phase.glob("33-*-PLAN.md"),phase/"33-CONTEXT.md",phase/"33-RESEARCH.md",phase/"33-UI-SPEC.md",phase/"33-VALIDATION.md"],key=lambda p:p.as_posix()); assert len(paths)==20 and all(p.is_file() for p in paths); entries=[{"path":p.as_posix(),"sha256":hashlib.sha256(p.read_bytes()).hexdigest()} for p in paths]; identity="sha256:"+hashlib.sha256(json.dumps(entries,sort_keys=True,separators=(",",":")).encode()).hexdigest(); assert re.fullmatch(r"sha256:[0-9a-f]{64}",identity); c=json.load(open(cev)); l=json.load(open(lev)); assert c["planning_identity_manifest"]==entries and l["planning_identity_manifest"]==entries and c["planning_identity"]==identity and l["planning_identity"]==identity; print(identity)'"'"' "$cev" "$lev")
candidate=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); p=d["v2_metrics_proof"]; expected={"storage":"isolated-sqlite","production_mutation":False,"all_seven_counters":True,"per_row_sum_invariant":True,"model_to_overall_totals":True,"request_success_formula":True,"service_availability_formula":True,"zero_denominators_null":True,"incident_8_of_9":True,"cleanup_status":"pass"}; assert all(p.get(k)==v for k,v in expected.items()); steps=p.get("cleanup_steps"); required=("container_remove","container_absent","group_delete","group_absent","data_dir_remove","data_dir_absent","response_files_remove","response_files_absent","live_digest_unchanged"); assert isinstance(steps,dict) and all(steps.get(k)=="pass" for k in required); print(d["candidate_digest"])'"'"' "$cev")
recorded=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["candidate_digest"])'"'"' "$lev")
execution=$(python3 -c '"'"'import json,sys; c=json.load(open(sys.argv[1])); l=json.load(open(sys.argv[2])); assert c["execution_identity"] and c["execution_identity"] not in ("current","latest") and l["execution_identity"]==c["execution_identity"]; print(c["execution_identity"])'"'"' "$cev" "$lev")
old=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["old_live_digest"])'"'"' "$lev")
test "$candidate" = "$recorded"
test "$(podman image inspect "$candidate" --format '"'"'{{ index .Labels "org.opencontainers.image.revision" }}'"'"')" = "$execution"
test "$(podman image inspect "$candidate" --format '"'"'{{ index .Labels "com.atius.router.reranker-governed" }}'"'"')" = true
test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$old"
"$omni/modules/fork-sync/bin/deploy.sh" atius-router "$PWD" --verify-candidate "$candidate"
bash scripts/atius-user-quota-guard.sh'</automated>
  </verify>
  <acceptance_criteria>
    - Candidate and rollback identities are immutable direct-inspection digests, not mutable tags.
    - The pinned clean Omni HEAD and candidate capability are revalidated before mutation.
    - Exact formula/zero-denominator proof comes from the disposable candidate dataset, not the legacy production window.
    - No secret value enters Markdown/JSON evidence.
  </acceptance_criteria>
  <done>The exact promotion and rollback digests are frozen, candidate-only metrics proof is verified, and production remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Promote transactionally and enforce the state-aware live gate</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json, .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE/</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - scripts/smoke-reranker-readiness.py
    - scripts/atius-user-quota-guard.sh
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
  </read_first>
  <action>Hydrate API/browser credentials only from approved Vault/bridge commands. In one `set -Eeuo pipefail` shell scope, define `rollback_on_failure`, register it for `ERR`, `INT`, and `TERM` before mutation, and define a single ordered `run_post_promotion_gates`. Promote only with the retained worktree's absolute `deploy.sh atius-router "$PWD" --promote-digest "$candidate" --rollback-digest "$old"`. Gates, all inside that function, are: running digest; exact runtime governor aliases; authenticated channel10/alias/ability/route/cache readiness; local and public functional rerank with 20 accepted and 21 deterministically rejected; state-aware 24-hour metrics API; two independent authenticated headless Chrome routes with separate DOM/PNG/trace; user-quota guard; artifact sanitization. Browser automation remains headless.

The metrics gate never infers or backfills legacy classes. Validate every model row before inspecting the overall branch. Every row must expose boolean `complete`, seven nonnegative integer counters, an integer terminal count, and marker/partial metadata. A row marked complete must carry its explicit v2 outcome marker and satisfy its own seven-counter terminal sum. A row marked incomplete must carry nonempty valid partial-reason metadata and leave its additive request-success, service-availability, latency, and throughput KPIs null/unavailable; it may not hide an invalid complete row. Treat each validated model row's completeness as the server-side aggregate of every contributing bucket marker/invariant established by Plans 33-01/33-05, derive expected overall completeness from all validated row contracts, require the overall marker to equal that result, and only then branch. Always require schema 2 and exact model-to-overall raw totals. When overall is partial, require nonempty valid overall partial metadata and null/unavailable derived overall KPIs; record `metrics_state=partial`; and require both UIs to show `Dados parciais` plus `—` without request/service `0,00%` or `100,00%`. When overall is complete, require its v2 marker and sum invariant, recompute both weighted formulas with null on their respective zero denominators, compare within absolute tolerance `0.005`, record `metrics_state=complete`, and require both UIs to show the API-derived values. Either consistent state passes. Inconsistent child markers, sums, partial metadata, totals, derived KPIs, overall marker, or UI state fail immediately; do not wait for 24 hours.

The rollback function disables recursive traps, enters controlled `set +e`, and attempts restore, running-digest equality, `--verify-live OLD`, and functional local rerank even if an earlier rollback step fails. It always records original failed gate/exit code, every rollback step status, deterministic first failed step, `promotion_status=failed`, and `rollback_status=verified|failed`, then exits with the original nonzero status. Retain the Omni worktree on every failure and on success; Plan 33-16 alone performs final knowledge synchronization and retained-worktree cleanup. Populate separately created `mktemp` files named `metrics` and `state`: write `metrics` only from an authenticated live 24-hour curl, then derive `state` only after the complete child-before-overall parser passes. For every wrong-route DOM marker and every negative secret scan, explicitly accept only `rg` rc 1 as absence, fail on rc 0, and fail on rc greater than 1 as a tool/read error. Scan the Router runbook, exact live JSON and Markdown, and the complete headless UI evidence directory including DOM and traces. Only after all gates pass, write exact machine-readable `planning_identity`, `planning_identity_manifest`, `execution_identity`, `candidate_digest`, directly inspected `running_digest`, `metrics_state`, `metrics_row_contracts=pass`, validated/partial row counts, `final_verdict=PASS`, `promotion_status=pass`, `rollback_status=not_required`, and the direct evidence matrix to `33-LIVE-EVIDENCE.json`, then disarm the trap. Recompute the planning identity from the frozen sorted artifact set and require exact manifest/hash equality. The execution identity is exactly the candidate OCI source-revision label and the current running image must carry the same label; generic labels cannot pass.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail
cev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-CANDIDATE-EVIDENCE.json
lev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
evidence=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
ui=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE
runbook=docs/ATIUS-RERANKER-RELIABILITY.md
metrics=$(mktemp)
state=$(mktemp)
cleanup_tmp(){ rm -f -- "$metrics" "$state"; }
trap cleanup_tmp EXIT
require_absent_fixed(){
  local needle=$1 target=$2 rc
  set +e
  rg -F -- "$needle" "$target" &gt;/dev/null
  rc=$?
  set -e
  case "$rc" in
    1) return 0 ;;
    0) printf "unexpected marker in %s: %s\n" "$target" "$needle" &gt;&amp;2; return 1 ;;
    *) printf "marker scan failed rc=%s target=%s\n" "$rc" "$target" &gt;&amp;2; return "$rc" ;;
  esac
}
require_secret_absent(){
  local rc
  set +e
  rg -n -e '"'"'(Authorization: Bearer [A-Za-z0-9]|sk-[A-Za-z0-9]{8}|api[_-]?key[=:][[:space:]]*[^$&lt;{])'"'"' -- "$@" &gt;/dev/null
  rc=$?
  set -e
  case "$rc" in
    1) return 0 ;;
    0) printf "secret-shaped material found\n" &gt;&amp;2; return 1 ;;
    *) printf "secret scan failed rc=%s\n" "$rc" &gt;&amp;2; return "$rc" ;;
  esac
}
omni=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified" and d["expected_commit"]==d["origin_main_commit"]; print(d["worktree_path"])'"'"' "$manifest")
expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest")
test "$(git -C "$omni" rev-parse HEAD)" = "$expected"
test -z "$(git -C "$omni" status --porcelain)"
planning=$(python3 -c '"'"'import hashlib,json,re,sys; from pathlib import Path; cev,lev=map(Path,sys.argv[1:]); phase=Path(".planning/phases/33-reranker-reliability-observability-and-readiness"); paths=sorted([*phase.glob("33-*-PLAN.md"),phase/"33-CONTEXT.md",phase/"33-RESEARCH.md",phase/"33-UI-SPEC.md",phase/"33-VALIDATION.md"],key=lambda p:p.as_posix()); assert len(paths)==20 and all(p.is_file() for p in paths); entries=[{"path":p.as_posix(),"sha256":hashlib.sha256(p.read_bytes()).hexdigest()} for p in paths]; identity="sha256:"+hashlib.sha256(json.dumps(entries,sort_keys=True,separators=(",",":")).encode()).hexdigest(); assert re.fullmatch(r"sha256:[0-9a-f]{64}",identity); c=json.load(open(cev)); l=json.load(open(lev)); assert c["planning_identity_manifest"]==entries and l["planning_identity_manifest"]==entries and c["planning_identity"]==identity and l["planning_identity"]==identity; print(identity)'"'"' "$cev" "$lev")
candidate=$(python3 -c '"'"'import json,re,sys; c=json.load(open(sys.argv[1])); l=json.load(open(sys.argv[2])); x=c["candidate_digest"]; assert re.fullmatch(r"sha256:[0-9a-f]{64}",x) and l["candidate_digest"]==x; print(x)'"'"' "$cev" "$lev")
execution=$(python3 -c '"'"'import json,sys; c=json.load(open(sys.argv[1])); l=json.load(open(sys.argv[2])); x=c["execution_identity"]; assert x and x not in ("current","latest") and l["execution_identity"]==x; print(x)'"'"' "$cev" "$lev")
running=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["running_digest"]==d["candidate_digest"]; print(d["running_digest"])'"'"' "$lev")
test "$running" = "$candidate"
test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$running"
test "$(podman image inspect "$candidate" --format '"'"'{{ index .Labels "org.opencontainers.image.revision" }}'"'"')" = "$execution"
"$omni/modules/fork-sync/bin/deploy.sh" atius-router "$PWD" --verify-live "$candidate"
python3 scripts/smoke-reranker-readiness.py --target local
python3 scripts/smoke-reranker-readiness.py --target public
: "${ATIUS_ROUTER_ADMIN_TOKEN:?ATIUS_ROUTER_ADMIN_TOKEN must be hydrated from Vault}"
router_base="${ATIUS_ROUTER_LOCAL_BASE:-http://127.0.0.1:3000}"
curl -fsS -H "Authorization: Bearer $ATIUS_ROUTER_ADMIN_TOKEN" "$router_base/api/perf-metrics/summary?hours=24" -o "$metrics"
test -s "$metrics"
python3 -c '"'"'import json,sys,math
src,out,lev=sys.argv[1:]
payload=json.load(open(src))
d=payload["data"]
keys=("success_count","client_validation_count","governor_rejection_count","routing_unavailable_count","transport_timeout_count","upstream_http_failure_count","invalid_upstream_response_count")
derived=("request_success_rate","service_availability","avg_latency_ms","avg_tps")
rows=d["models"]
o=d["overall"]
terminal=lambda r:r.get("terminal_count",r.get("request_count"))
marker=lambda r:r.get("outcome_schema_version",r.get("schema_version"))
def reasons(r):
 raw=r.get("partial_reasons",r.get("partial_reason"))
 vals=[raw] if isinstance(raw,str) else raw
 assert isinstance(vals,list) and vals and all(isinstance(x,str) and 0&lt;len(x)&lt;=64 for x in vals)
 allowed=("legacy","mixed","missing_v2","outcome_sum","bucket_contract")
 assert all(any(x==a or x.startswith(a+"_") for a in allowed) for x in vals)
 return vals
def raw_valid(r):
 return isinstance(terminal(r),int) and terminal(r)&gt;=0 and all(isinstance(r.get(k),int) and r[k]&gt;=0 for k in keys)
assert d["schema_version"]==2 and isinstance(rows,list) and rows and isinstance(o.get("complete"),bool) and raw_valid(o)
row_contracts=[]
for r in rows:
 assert isinstance(r.get("complete"),bool) and raw_valid(r)
 sum_ok=sum(r[k] for k in keys)==terminal(r)
 if r["complete"]:
  assert marker(r)==2 and sum_ok
  row_contracts.append(True)
 else:
  reasons(r)
  assert all(r.get(k) is None for k in derived)
  row_contracts.append(False)
assert terminal(o)==sum(terminal(r) for r in rows)
assert all(o[k]==sum(r[k] for r in rows) for k in keys)
expected_complete=all(row_contracts)
assert o["complete"] is expected_complete
disp=lambda x:"—" if x is None else (f"{x:.2f}%").replace(".",",")
if not expected_complete:
 reasons(o)
 assert all(o.get(k) is None for k in derived)
 state="partial"
 req=svc=None
else:
 assert marker(o)==2 and sum(o[k] for k in keys)==terminal(o)
 req=None if terminal(o)==0 else o["success_count"]*100/terminal(o)
 den=sum(o[k] for k in ("success_count","routing_unavailable_count","transport_timeout_count","upstream_http_failure_count","invalid_upstream_response_count"))
 svc=None if den==0 else o["success_count"]*100/den
 same=lambda got,want: got is None if want is None else isinstance(got,(int,float)) and math.isclose(float(got),want,rel_tol=0,abs_tol=.005)
 assert same(o.get("request_success_rate"),req) and same(o.get("service_availability"),svc)
 state="complete"
record=json.load(open(lev))
assert record["metrics_state"]==state
assert record["metrics_row_contracts"]=="pass"
assert record["metrics_validated_rows"]==len(rows)
assert record["metrics_partial_rows"]==sum(not x for x in row_contracts)
open(out,"w").write(json.dumps({"state":state,"request":disp(req),"service":disp(svc),"validated_rows":len(rows),"partial_rows":sum(not x for x in row_contracts)})+"\n")'"'"' "$metrics" "$state" "$lev"
for f in overview.dom.html overview.png overview.trace.json models.dom.html models.png models.trace.json; do test -s "$ui/$f"; done
for marker in "Visão geral" "Saúde de desempenho" "Resultados por classe" "Sucesso" "Validação do cliente" "Rejeição do governor" "Roteamento indisponível" "Transporte ou timeout" "Falha HTTP upstream" "Resposta upstream inválida"; do rg -F -- "$marker" "$ui/overview.dom.html" &gt;/dev/null; done
require_absent_fixed "Analytics de chamadas de modelo" "$ui/overview.dom.html"
for marker in "Analytics de chamadas de modelo" "Sucesso das solicitações" "Disponibilidade do serviço"; do rg -F -- "$marker" "$ui/models.dom.html" &gt;/dev/null; done
require_absent_fixed "Visão geral" "$ui/models.dom.html"
require_absent_fixed "Resultados por classe" "$ui/models.dom.html"
python3 -c '"'"'import json,sys; from html.parser import HTMLParser
class P(HTMLParser):
 def __init__(self): super().__init__(); self.parts=[]
 def handle_data(self,d):
  if d.strip(): self.parts.append(d.strip())
def text(path):
 p=P(); p.feed(open(path,encoding="utf-8").read()); return " ".join(p.parts)
s=json.load(open(sys.argv[1]))
docs=[text(sys.argv[2]),text(sys.argv[3])]
for doc in docs:
 assert "Sucesso das solicitações" in doc and "Disponibilidade do serviço" in doc
 if s["state"]=="partial":
  assert "Dados parciais" in doc
  a=doc.index("Sucesso das solicitações"); b=doc.index("Disponibilidade do serviço",a); c=doc.index("Latência",b); d=doc.index("Throughput",c)
  req=doc[a:b]; svc=doc[b:c]; latency=doc[c:d]; throughput=doc[d:d+200]
  assert all("—" in part for part in (req,svc,latency,throughput))
  assert all(x not in req and x not in svc for x in ("0,00%","100,00%"))
 else:
  assert s["request"] in doc and s["service"] in doc'"'"' "$state" "$ui/overview.dom.html" "$ui/models.dom.html"
bash scripts/atius-user-quota-guard.sh
require_secret_absent "$runbook" "$lev" "$evidence" "$ui"'</automated>
  </verify>
  <acceptance_criteria>
    - Exit zero is possible only while the running digest equals the validated candidate and all ordered direct gates pass.
    - The live 24-hour API validates every child row before branching overall, including explicit v2 marker/sum for complete rows and valid partial metadata plus null additive KPIs for incomplete rows.
    - Partial passes only after deriving overall incompleteness from all validated child contracts, with null derived KPIs, `Dados parciais`, and `—` without fabricated request/service 0/100; complete passes only with every child contract valid and exact overall formulas.
    - Overview and models routes each retain distinct sanitized DOM, PNG, and trace evidence and cannot satisfy each other's route markers.
    - Any failed promotion/live gate attempts and records every rollback step, restores the old digest when possible, and exits nonzero even after verified rollback.
    - The pinned clean Omni worktree remains available for Plan 33-16 or failure diagnosis.
  </acceptance_criteria>
  <done>The validated digest is live with a consistent complete-or-partial 24-hour state and direct gates PASS; otherwise rollback evidence exists and the plan fails nonzero.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `33-LIVE-EVIDENCE.json` with immutable candidate/old/running identities, ordered gate results, metrics state, and rollback status.
- `33-LIVE-EVIDENCE.md` with sanitized direct runtime, readiness, rerank, API/UI, quota, promotion, and rollback evidence.
- `33-LIVE-UI-EVIDENCE/` with separate sanitized DOM, screenshot, and trace artifacts for `/dashboard/overview` and `/dashboard/models`.
- A retained clean pinned Omni worktree for Plan 33-16 final synchronization and cleanup.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| candidate artifact to production unit | A tested immutable image becomes the public runtime. |
| public/authenticated clients to Router | Untrusted requests reach validation, routing, billing, and relay paths. |
| production metrics to API/UI | Mixed legacy/schema-v2 buckets must not be reinterpreted as complete health. |
| rollback controller to user service | Failure handling mutates the production image reference and process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-15-01 | Tampering | promotion image selection | critical | mitigate | Promote only the already validated immutable digest and independently recheck labels/running identity. |
| T-33-15-02 | Denial of Service | production restart/rollback | high | mitigate | Arm one rollback trap before mutation, attempt all rollback proofs, and preserve the old digest. |
| T-33-15-03 | Repudiation | legacy/schema-v2 metrics | high | mitigate | Enforce boolean completeness-marker consistency and retain explicit partial/complete verdict evidence. |
| T-33-15-04 | Information Disclosure | API, DOM, trace, and runtime evidence | high | mitigate | Use synthetic requests, Vault hydration, sanitized artifacts, and credential-shaped negative scans. |
| T-33-15-05 | Elevation of Privilege | authenticated readiness route | high | mitigate | Recheck ChannelOperate authorization through the governed live verifier and expose no secret-bearing payloads. |
| T-33-15-SC | Tampering | package installs | high | mitigate | No package install is authorized during promotion; abort if one is requested without audited legitimacy and blocking human verification. |
</threat_model>

<verification>
- Production runs the exact candidate digest and readiness/rerank 20/21/quota gates pass locally and publicly.
- The live 24-hour state is accepted as either internally consistent partial or formula-verified complete, never forced complete.
- Both real dashboard routes agree with the API state and retain distinct headless evidence.
- Any failed gate leaves a non-success plan result with explicit rollback evidence.
</verification>

<success_criteria>
- Phase 33 behavior is directly verified in production without fabricating historical outcome classes.
- Promotion and rollback identities are immutable and auditable.
- Plan 33-16 can synchronize final operational truth from a successful live evidence record and retained clean Omni worktree.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-15-SUMMARY.md` when done.
</output>
