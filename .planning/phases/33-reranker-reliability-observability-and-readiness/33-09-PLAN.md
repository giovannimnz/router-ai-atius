---
phase: 33-reranker-reliability-observability-and-readiness
plan: "09"
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - service/channel_readiness.go
  - controller/channel_readiness.go
  - controller/channel.go
  - router/channel-router.go
autonomous: true
requirements:
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Readiness synchronously rebuilds cache and polls the same normal channel-selection path under a bounded context."
    - "Channel 10 is ready only when enabled, alias/ability/Advanced Custom route/cache/selection all agree."
    - "A failed reranker re-enable restores the disabled state and rebuilds cache; smoke cannot proceed."
  artifacts:
    - path: "service/channel_readiness.go"
      provides: "ChannelReadinessResult and bounded selection probe"
    - path: "controller/channel_readiness.go"
      provides: "Sanitized admin readiness endpoint"
    - path: "router/channel-router.go"
      provides: "POST /:id/readiness with ChannelOperate"
    - path: "controller/channel.go"
      provides: "Fail-closed create/re-enable readiness wiring"
  key_links:
    - from: "controller/channel_readiness.go"
      to: "service/channel_readiness.go"
      via: "bounded request context"
      pattern: "ProbeChannelReadiness"
    - from: "service/channel_readiness.go"
      to: "model/channel_cache.go"
      via: "InitChannelCache then CacheGetRandomSatisfiedChannel"
      pattern: "InitChannelCache|CacheGetRandomSatisfiedChannel"
---

<objective>
Implement deterministic authenticated readiness after cache sync and make reranker create/re-enable fail closed.

Purpose: Eliminate the provisioning window that produced pre-relay 503 responses while preserving rollback state.
Output: Service probe, admin route, and mutation wiring for channel 10/alias/ability/route consistency.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-02-SUMMARY.md
@model/channel_cache.go
@model/channel_satisfy.go
@controller/channel.go
@controller/codex_oauth.go
@router/channel-router.go
@router/channel_router_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the bounded normal-selection readiness service</name>
  <files>service/channel_readiness.go</files>
  <read_first>model/channel_cache.go, model/channel_satisfy.go, model/channel.go, service/channel.go, controller/channel_readiness_test.go, dto/channel_settings.go</read_first>
  <behavior>
    - Channel/alias/ability/route mismatches return fixed reason codes.
    - Cache rebuild completes synchronously before polling normal selection.
    - Context cancellation/timeout stops polling without sleeps in tests.
  </behavior>
  <action>Create `ChannelReadinessResult` and `ProbeChannelReadiness(ctx, channelID, modelName, group, requestPath)` for PHASE-33-CHANNEL-READINESS. Require channel 10 when invoked for `reranker-gte-multilingual-v1`; verify enabled status, model ability, Advanced Custom incoming `/v1/rerank` with converter `jina_rerank_to_tei_native`, and group. Call `model.InitChannelCache()` synchronously, then poll the same `CacheGetRandomSatisfiedChannel` path until it selects channel 10 or the bounded context ends. Return fixed reason codes and only ready/channel/model/ability/route/cache/attempt/elapsed metadata. Inject poll/cache/selection functions for deterministic tests.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./service ./controller -run 'Test.*ChannelReadiness' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All readiness matrix cases from Plan 33-02 pass.
    - No fixed sleep is used as readiness proof.
    - Result metadata contains no channel key, base URL, token, or body.
  </acceptance_criteria>
  <done>The service proves readiness through the production selection path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the protected readiness route</name>
  <files>controller/channel_readiness.go, router/channel-router.go</files>
  <read_first>controller/codex_oauth.go, controller/channel_readiness_test.go, router/channel-router.go, router/channel_router_test.go, service/channel_readiness.go</read_first>
  <behavior>
    - POST /api/channel/:id/readiness is admin-only and requires ChannelOperate.
    - Success/failure returns the fixed sanitized result envelope.
    - Invalid id, cancellation, and timeout are bounded and observable.
  </behavior>
  <action>Add controller `ProbeChannelReadiness` using a bounded request context and common.MaskSensitiveInfo only for generic messages. Register POST `/:id/readiness` in channelPermissionRoutes with `authz.ChannelOperate`. Default model to the canonical reranker alias only for channel 10; validate provided model/group/path values server-side. Keep the current AdminAuth group and do not expose readiness on a public route.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./controller ./router -run 'Test.*ChannelReadiness' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Route identity/permission test passes.
    - Controller response field allowlist matches the Phase 33 test contract.
    - Unauthorized requests cannot trigger cache rebuild/poll.
  </acceptance_criteria>
  <done>An authenticated bounded readiness endpoint is available to provisioning/release tooling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail closed on reranker create or re-enable readiness failure</name>
  <files>controller/channel.go</files>
  <read_first>controller/channel.go, service/channel_readiness.go, model/channel_cache.go, controller/channel_readiness_test.go</read_first>
  <behavior>
    - Creating or re-enabling channel 10 invokes sync/readiness before success is reported or smoke is allowed.
    - Failure restores the prior disabled state and rebuilds cache.
    - Non-reranker channels retain existing behavior.
  </behavior>
  <action>At the existing successful create/update-status cache-sync seams, detect channel 10 or the canonical governed reranker alias and run ProbeChannelReadiness before returning an operationally ready success. For a re-enable failure, restore the prior disabled status, persist it with existing GORM methods, call InitChannelCache again, and return a bounded sanitized failure. For a newly created but not-ready channel, leave it disabled rather than deleting it. Do not affect unrelated channels, abilities, user quota, or Phase 29/30 k3s cutover.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./controller ./router ./service -run 'Test.*(ChannelReadiness|ChannelStatus|AddChannel)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Failed enable/create paths end disabled with cache rebuilt.
    - Successful path proves channel 10/alias/ability/route selection before response.
    - Unrelated channel mutation tests remain green.
  </acceptance_criteria>
  <done>Provisioning cannot enter smoke or live traffic with stale/inconsistent reranker routing.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `ChannelReadinessResult`, fixed `ReadinessReason*` codes, and `ProbeChannelReadiness` service/controller functions.
- Admin POST `/api/channel/:id/readiness` route.
- Fail-closed reranker create/re-enable readiness hook.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin request to cache/database | Privileged endpoint triggers shared routing state rebuild. |
| Database/ability/cache to selection | Inconsistent state must not be presented as ready. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-24 | Elevation of Privilege | readiness endpoint | high | mitigate | AdminAuth plus ChannelOperate. |
| T-33-25 | Denial of Service | cache poll | high | mitigate | Bounded context, fixed interval, no anonymous access. |
| T-33-26 | Tampering | enable rollback | high | mitigate | Persist prior disabled state and rebuild cache on any failure. |
| T-33-27 | Information Disclosure | readiness response | medium | mitigate | Fixed metadata allowlist and masked generic messages. |
</threat_model>

<verification>
Run controller/router/service readiness suites through the CPU wrapper; no production channel mutation occurs in this plan.
</verification>

<success_criteria>
- Plan 33-02 readiness tests pass.
- Channel 10 cannot be reported ready from DB state alone.
- Failed readiness restores disabled state and blocks smoke.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-09-SUMMARY.md` when done.
</output>
