---
phase: 25-embedding-governor-auto-workload-inference
plan: "02"
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - relay/embedding_handler.go
  - relay/embedding_handler_test.go
autonomous: true
requirements:
  - PHASE-25-GOVERNED-MODEL-SCOPE
  - PHASE-25-AUTO-WORKLOAD-INFERENCE
  - PHASE-25-HEADER-OVERRIDE-COMPATIBILITY
  - PHASE-25-TEI-BATCH-SAFETY
  - PHASE-25-CLIENT-SMOKE-VALIDATION
must_haves:
  truths:
    - "D-01: relay uses the public model alias before upstream model mapping for governed scope checks."
    - "D-02: relay preserves X-Embedding-Workload as metadata and does not reinterpret valid overrides."
    - "D-03: relay passes only InputCount and InputChars metadata to the governor."
    - "D-04: governed TEI requests with more than 4 input items fail closed before upstream dispatch."
    - "D-05: relay tests prove no-header metadata capture and the >4 TEI cap contract."
  artifacts:
    - "relay/embedding_handler.go enforces a governed TEI max input count of 4."
    - "relay/embedding_handler_test.go covers header/no-header metadata and fail-closed cap behavior."
  key_links:
    - "relay/embedding_handler.go -> embeddinggovernor.IsGovernedModel uses the service scope from Plan 25-01."
    - "dto.EmbeddingRequest.GetInputStats -> embeddinggovernor.Request keeps raw input outside governor state."
---

<objective>
Wire the Phase 25 service contract into the relay and enforce the TEI client batch cap.

Purpose: Prove unlabeled requests carry enough metadata for automatic inference and prevent governed local TEI from receiving unsupported arrays larger than 4.
Output: Relay metadata coverage and fail-closed cap behavior for D-01 through D-05.
</objective>

## Artifacts this phase produces

- `relay.maxGovernedTEIInputCount = 4`
- Expanded relay metadata tests in `relay/embedding_handler_test.go`
- `TestEmbeddingHelperRejectsGovernedInputAboveTEICap`

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-RESEARCH.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-01-SUMMARY.md
@.planning/phases/phase-20-go-native-model-router/20-04-SUMMARY.md
@relay/embedding_handler.go
@relay/embedding_handler_test.go
@dto/embedding.go
@dto/embedding_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand relay governor metadata capture tests</name>
  <files>relay/embedding_handler_test.go</files>
  <read_first>
    @relay/embedding_handler.go
    @relay/embedding_handler_test.go
    @dto/embedding.go
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-01-SUMMARY.md
  </read_first>
  <behavior>
    - Header-present batch requests still pass `Workload="batch"`, `InputCount=2`, and `InputChars` to governor per D-02 and D-03.
    - No-header single-string request passes `Workload=""`, `InputCount=1`, and computed `InputChars` before governor acquisition per D-03 and D-05.
    - No-header two-item array passes `Workload=""`, `InputCount=2`, and computed `InputChars`, enabling service classifier batch inference per D-03.
    - Error paths do not include raw input text per D-03 and D-05.
  </behavior>
  <action>Refactor `TestEmbeddingHelperPassesGovernorRequestMetadata` or add a table-driven helper in `relay/embedding_handler_test.go` using the existing `acquireEmbeddingGovernor` hook, `gin.CreateTestContext`, `common.SetContextKey`, and `dto.EmbeddingRequest`. Add cases for header batch, no-header single string, and no-header two-item array. Keep assertions on `captured.Model`, `captured.ChannelID`, `captured.ChannelName`, `captured.Workload`, `captured.InputCount`, and `captured.InputChars`. Do not assert raw request body content and do not add direct `encoding/json` use.</action>
  <acceptance_criteria>Relay tests prove the governor receives public model alias plus metadata for both header and no-header requests, without raw input leakage.</acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./relay -run '^TestEmbeddingHelperPassesGovernorRequestMetadata$' -count=1</automated>
  </verify>
  <done>No-header relay metadata capture is deterministic and compatible with the classifier implemented in Plan 25-01.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed for governed TEI input arrays over four items</name>
  <files>relay/embedding_handler.go, relay/embedding_handler_test.go</files>
  <read_first>
    @relay/embedding_handler.go
    @relay/embedding_handler_test.go
    @service/embeddinggovernor/governor.go
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
  </read_first>
  <behavior>
    - `embedding-gte-v1` with `InputCount=5` returns HTTP 400 invalid request before governor acquisition and before upstream dispatch per D-04.
    - `X-Embedding-Workload: interactive` cannot bypass the governed TEI cap per D-04.
    - Error text names the max count `4` and does not contain raw input strings per D-03 and D-05.
    - Non-governed models keep the existing no-op governor behavior and are not capped by this local TEI rule per D-01.
  </behavior>
  <action>Choose the fail-closed TEI cap explicitly because `25-PATTERNS.md` found no safe transparent sub-batch recomposition analog. In `relay/embedding_handler.go`, add `const maxGovernedTEIInputCount = 4` and validate immediately after `publicModelName := embeddingReq.Model` plus `inputStats := embeddingReq.GetInputStats()`, before request conversion and before `acquireEmbeddingGovernor`. If `embeddinggovernor.IsGovernedModel(publicModelName)` is true and `inputStats.InputCount > maxGovernedTEIInputCount`, return `types.NewErrorWithStatusCode` with `types.ErrorCodeInvalidRequest`, HTTP 400, `types.ErrOptionWithSkipRetry()`, and a concise message such as `embedding-gte-v1 supports at most 4 input items per request`. Add tests proving governed arrays over 4 fail without calling `acquireEmbeddingGovernor`, including a case with `X-Embedding-Workload: interactive`. Keep unknown model no-op behavior by not applying this cap when `IsGovernedModel` is false.</action>
  <acceptance_criteria>Governed local TEI arrays larger than 4 cannot reach upstream, while normal governed requests and unknown-model behavior continue through the existing relay flow.</acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./relay -run '^(TestEmbeddingHelperPassesGovernorRequestMetadata|TestEmbeddingHelperRejectsGovernedInputAboveTEICap)$' -count=1</automated>
    <automated>/usr/local/go/bin/go test ./dto ./relay ./service/embeddinggovernor -count=1</automated>
  </verify>
  <done>Phase 25 TEI max client batch size invariant is enforced fail-closed at the relay boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> relay | Untrusted embedding payload and workload header enter `/v1/embeddings`. |
| relay -> governor | Metadata-only model/workload/count/chars controls queue class and cap checks. |
| relay -> TEI upstream | Converted OpenAI-compatible embedding request leaves the router process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04 | Denial of Service | `EmbeddingHelper` governed TEI cap | high | mitigate | Reject governed `embedding-gte-v1` requests with `InputCount > 4` before governor acquisition and upstream dispatch per D-04. |
| T-25-05 | Tampering | `X-Embedding-Workload` override | medium | mitigate | Keep header as classifier metadata only; enforce the TEI cap independently so `interactive` cannot bypass input-count safety per D-02 and D-04. |
| T-25-06 | Information Disclosure | relay errors and tests | high | mitigate | Assert cap and governor reject errors exclude raw input strings, bearer tokens, and serialized request bodies per D-03 and D-05. |
| T-25-SC | Tampering | package installs | low | accept | No npm, pip, or cargo install is part of this plan; if an install is introduced, stop for package legitimacy audit and human verification. |
</threat_model>

<verification>
Run the task commands, then the focused package gate:

<automated>/usr/local/go/bin/go test ./dto ./relay ./service/embeddinggovernor -count=1</automated>

Graphify freshness after code changes:

<automated>node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status</automated>
</verification>

<success_criteria>
- Relay metadata tests cover header and no-header cases.
- Governed local TEI input arrays over 4 fail closed with HTTP 400.
- Header override remains compatible but cannot bypass TEI input-count safety.
- No public `*-batch` alias, Python/model-detailed owner, sidecar, or extra container is introduced.
</success_criteria>

<output>
Create `.planning/phases/25-embedding-governor-auto-workload-inference/25-02-SUMMARY.md` when done.
</output>
