---
phase: 33-reranker-reliability-observability-and-readiness
plan: "16"
type: execute
wave: 9
depends_on:
  - "33-15"
files_modified:
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
  - docs/ATIUS-RERANKER-RELIABILITY.md
  - .planning/config.json
  - .githooks/post-commit
  - .githooks/pre-push
  - scripts/phase33-post-execution-closeout.sh
  - scripts/test-phase33-post-execution-closeout.sh
  - /home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md
autonomous: true
requirements:
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Final Router docs, GBrain, Obsidian, and retained evidence exactly reproduce every authoritative identity/digest/state/verdict field from 33-LIVE-EVIDENCE.json."
    - "Canonical Omni preflight and direct live verification still pass before the retained clean worktree is removed."
    - "The retained Omni worktree must exist, be registered, clean, and pinned at task start; it is removed only after every runtime and knowledge-system readback succeeds."
    - "Graphify asynchronous auto-update is disabled; every Phase 33 terminal index runs synchronously at pre-push through the repository 20%-CPU wrapper."
    - "The versioned post-commit hook completes in less than 10 seconds: canonical Phase 33 commits only invalidate prior PASS and atomically record mode-0600 PENDING/NO-GO plus HEAD, using a bounded foreground helper allowlist and no Graphify, wrapper, closeout, network, container, or build."
    - "Pre-push synchronously performs any missing or stale terminal closeout, waits with at least the configured Graphify build timeout plus safety margin, and blocks until the ignored status proves PASS for the exact pushed HEAD, stable graph-input fingerprint, idle Graphify state, and three nonempty queries."
  artifacts:
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
      provides: "Final synchronized identity, metrics state, runtime verdict, knowledge readbacks, and cleanup result"
    - path: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
      provides: "Authoritative exact planning/execution identities, candidate/running digests, metrics state, verdict, rollback, synchronization, and cleanup fields"
    - path: docs/ATIUS-RERANKER-RELIABILITY.md
      provides: "Operator contract aligned with the promoted runtime and state-aware metrics semantics"
    - path: scripts/phase33-post-execution-closeout.sh
      provides: "Branch-agnostic synchronous Graphify closeout over ignored graphify-out storage"
    - path: scripts/test-phase33-post-execution-closeout.sh
      provides: "Disposable Git/Graphify lifecycle fixtures for PASS and every required fail-closed state"
    - path: .githooks/post-commit
      provides: "Fast canonical Phase 33 commit invalidator that writes only ignored PENDING/NO-GO state"
    - path: .githooks/pre-push
      provides: "Synchronous terminal closeout consumer and push blocker for missing, stale, drifted, or non-PASS state"
    - path: /home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md
      provides: "Cross-session operational record without secrets"
  key_links:
    - from: .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
      to: docs/ATIUS-RERANKER-RELIABILITY.md
      via: "same candidate/running digest, metrics state, final verdict, and rollback status"
    - from: docs/ATIUS-RERANKER-RELIABILITY.md
      to: GBrain and Obsidian
      via: "sanitized planning/execution identity and state-aware operational truth"
    - from: .githooks/post-commit
      to: .git/phase33-post-execution-closeout.json
      via: "sub-10-second atomic PASS invalidation to non-authorizing PENDING/NO-GO plus HEAD after docs(33-16) and docs(phase-33) commits"
    - from: .githooks/pre-push
      to: scripts/phase33-post-execution-closeout.sh
      via: "foreground invocation for a relevant pushed HEAD, followed by exact marker, fingerprint, idle-state, and query readback before network mutation"
---

<objective>
Synchronize final Phase 33 operational truth and clean up the retained release worktree.

Purpose: Preserve the verified deployment and its complete-or-partial metrics semantics across Router docs, GBrain, Obsidian, Graphify, and future fork-sync operations while keeping commit creation fast and making pre-push the consumed terminal freshness gate.
Output: Consistent sanitized records, versioned and locally installed post-commit/pre-push enforcement, disposable lifecycle tests, and safe retained-worktree removal.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/config.json
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-PATTERNS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-15-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
@docs/ATIUS-RERANKER-RELIABILITY.md
@scripts/podman-admin.sh
@scripts/smoke-reranker-readiness.py
@/home/ubuntu/.codex/gsd-core/workflows/execute-phase.md
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/scripts/release-preflight.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synchronize exact JSON-authoritative operational truth</name>
  <files>.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md, docs/ATIUS-RERANKER-RELIABILITY.md, /home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-15-SUMMARY.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - docs/ATIUS-RERANKER-RELIABILITY.md
    - scripts/smoke-reranker-readiness.py
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/modules/fork-sync/bin/deploy.sh
    - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified/scripts/release-preflight.sh
  </read_first>
  <action>Treat `33-LIVE-EVIDENCE.json` as the sole authority for the seven release values. Extract its exact `planning_identity`, `execution_identity`, `candidate_digest`, `running_digest`, `metrics_state`, `final_verdict`, and `rollback_status`. Recompute the deterministic manifest from the sorted immutable Phase 33 plan set and require exact equality with both `planning_identity_manifest` and `planning_identity`; generic labels or an arbitrary `sha256:` cannot pass. Require `execution_identity` to equal exactly the candidate OCI `org.opencontainers.image.revision`, require the current running image to carry that same label, require candidate/running digests to be identical, metrics state exactly partial or complete, verdict exactly PASS, and rollback exactly not_required. Compare the current container digest directly with both recorded digests. Require the retained Omni worktree to exist and remain registered, clean, and at `expected_commit == origin_main_commit`; capture `git worktree list --porcelain` and its rc before evaluating exact registration. Run its absolute `release-preflight.sh`, `deploy.sh --verify-live CANDIDATE`, and local/public smoke so readiness plus the 20-accepted/21-rejected batch contract are re-proven before durable writes.

Update the Router runbook and live evidence Markdown using exact literal lines for all seven authoritative values. Synchronize GBrain under fixed slug `router-ai-atius-phase-33-reranker-readiness`; both page and timeline must contain those same exact lines. Synchronize the Obsidian note through the approved sessionful bridge with the same exact lines. Read every sink back and compare exact values from JSON. Include state-aware legacy/v2 semantics and candidate-only formula proof without secrets. Negative-scan Router runbook, live JSON/Markdown, Obsidian, GBrain page/timeline readback files, and retained headless DOM/trace evidence with an explicit rc contract: only rc 1 is clean, rc 0 finds a secret, and rc greater than 1 is a scan/tool failure. Do not run Graphify and do not remove the worktree in this task.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail
lev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
evidence=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
runbook=docs/ATIUS-RERANKER-RELIABILITY.md
obs=/home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md
ui=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE
gpage=$(mktemp)
gtimeline=$(mktemp)
registry=$(mktemp)
cleanup_tmp(){ rm -f -- "$gpage" "$gtimeline" "$registry"; }
trap cleanup_tmp EXIT
require_secret_absent(){
  local rc
  set +e
  rg -n -e '"'"'(Authorization: Bearer [A-Za-z0-9]|sk-[A-Za-z0-9]{8}|api[_-]?key[=:][[:space:]]*[^$&lt;{])'"'"' -- "$@" &gt;/dev/null
  rc=$?
  set -e
  case "$rc" in
    1) return 0 ;;
    0) printf "secret-shaped material found\n" &gt;&amp;2; return 1 ;;
    *) printf "secret scan failed rc=%s\n" "$rc" &gt;&amp;2; return "$rc" ;;
  esac
}
planning=$(python3 -c '"'"'import hashlib,json,re,sys; from pathlib import Path; lev=Path(sys.argv[1]); phase=Path(".planning/phases/33-reranker-reliability-observability-and-readiness"); paths=sorted([*phase.glob("33-*-PLAN.md"),phase/"33-CONTEXT.md",phase/"33-RESEARCH.md",phase/"33-UI-SPEC.md",phase/"33-VALIDATION.md"],key=lambda p:p.as_posix()); assert len(paths)==20 and all(p.is_file() for p in paths); entries=[{"path":p.as_posix(),"sha256":hashlib.sha256(p.read_bytes()).hexdigest()} for p in paths]; identity="sha256:"+hashlib.sha256(json.dumps(entries,sort_keys=True,separators=(",",":")).encode()).hexdigest(); assert re.fullmatch(r"sha256:[0-9a-f]{64}",identity); d=json.load(open(lev)); assert d["planning_identity_manifest"]==entries and d["planning_identity"]==identity; print(identity)'"'"' "$lev")
execution=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); x=d["execution_identity"]; assert x and x not in ("current","latest"); print(x)'"'"' "$lev")
candidate=$(python3 -c '"'"'import json,re,sys; d=json.load(open(sys.argv[1])); x=d["candidate_digest"]; assert re.fullmatch(r"sha256:[0-9a-f]{64}",x); print(x)'"'"' "$lev")
running=$(python3 -c '"'"'import json,re,sys; d=json.load(open(sys.argv[1])); x=d["running_digest"]; assert re.fullmatch(r"sha256:[0-9a-f]{64}",x) and x==d["candidate_digest"]; print(x)'"'"' "$lev")
state=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["metrics_state"] in ("partial","complete"); print(d["metrics_state"])'"'"' "$lev")
verdict=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["final_verdict"]=="PASS"; print(d["final_verdict"])'"'"' "$lev")
rollback=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["rollback_status"]=="not_required"; print(d["rollback_status"])'"'"' "$lev")
test "$running" = "$candidate"
test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$running"
test "$(podman image inspect "$running" --format '"'"'{{ index .Labels "org.opencontainers.image.revision" }}'"'"')" = "$execution"
omni=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified" and d["expected_commit"]==d["origin_main_commit"]; print(d["worktree_path"])'"'"' "$manifest")
expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest")
test -d "$omni"
set +e
git -C "$omni" worktree list --porcelain &gt;"$registry"
list_rc=$?
set -e
test "$list_rc" -eq 0
set +e
rg -Fx -- "worktree $omni" "$registry" &gt;/dev/null
registered_rc=$?
set -e
case "$registered_rc" in
  0) ;;
  1) printf "required worktree is not registered\n" &gt;&amp;2; exit 1 ;;
  *) printf "worktree registry scan failed rc=%s\n" "$registered_rc" &gt;&amp;2; exit "$registered_rc" ;;
esac
test "$(git -C "$omni" rev-parse HEAD)" = "$expected"
test -z "$(git -C "$omni" status --porcelain)"
"$omni/scripts/release-preflight.sh" "$PWD" --mode fork-deploy --deploy-config "$omni/modules/fork-sync/projects/atius-router/deploy.yaml"
"$omni/modules/fork-sync/bin/deploy.sh" atius-router "$PWD" --verify-live "$candidate"
python3 scripts/smoke-reranker-readiness.py --target local
python3 scripts/smoke-reranker-readiness.py --target public
slug=router-ai-atius-phase-33-reranker-readiness
/home/ubuntu/.local/bin/gbrain get "$slug" &gt;"$gpage"
/home/ubuntu/.local/bin/gbrain timeline "$slug" &gt;"$gtimeline"
for sink in "$evidence" "$runbook" "$gpage" "$gtimeline" "$obs"; do
  rg -Fx -- "Planning identity: $planning" "$sink" &gt;/dev/null
  rg -Fx -- "Execution identity: $execution" "$sink" &gt;/dev/null
  rg -Fx -- "Candidate digest: $candidate" "$sink" &gt;/dev/null
  rg -Fx -- "Running digest: $running" "$sink" &gt;/dev/null
  rg -Fx -- "Metrics state: $state" "$sink" &gt;/dev/null
  rg -Fx -- "Final verdict: $verdict" "$sink" &gt;/dev/null
  rg -Fx -- "Rollback status: $rollback" "$sink" &gt;/dev/null
done
test -d "$ui"
require_secret_absent "$runbook" "$lev" "$evidence" "$obs" "$gpage" "$gtimeline" "$ui"
test -d "$omni"'</automated>
  </verify>
  <acceptance_criteria>
    - Current runtime digest equals both exact JSON candidate/running digests and direct readiness/rerank 20/21 checks pass.
    - Router runbook/live evidence, GBrain page/timeline, and Obsidian contain exact JSON values for all seven authoritative fields.
    - Generic identity labels or an arbitrary digest cannot satisfy readback.
    - The registered, clean, pinned retained worktree still exists; Graphify has not run yet.
  </acceptance_criteria>
  <done>Every runtime/document/memory sink exactly matches the machine-readable live authority, while the retained worktree remains eligible for controlled cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Install and prove the Git-consumed closeout, then remove the verified worktree</name>
  <files>.planning/config.json, .githooks/post-commit, .githooks/pre-push, scripts/phase33-post-execution-closeout.sh, scripts/test-phase33-post-execution-closeout.sh, .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json</files>
  <read_first>
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
    - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
    - docs/ATIUS-RERANKER-RELIABILITY.md
    - /home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md
    - .planning/config.json
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - scripts/podman-admin.sh
    - /home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
    - /home/ubuntu/.codex/gsd-core/workflows/execute-phase.md
  </read_first>
  <action>At task start, fail unless the exact retained worktree exists on disk and in the Git worktree registry, has empty porcelain status, and remains at the manifest's expected origin/main commit. Recompute and exactly match the planning manifest/hash, require execution identity to equal the running image source-revision label, re-extract all seven authoritative JSON values, compare the current running digest directly, and require exact Task 1 readbacks from Router docs, GBrain page/timeline, and Obsidian. `git worktree list --porcelain` itself must succeed before exact-path matching; an initially absent or unregistered worktree is a failure, never idempotent success.

Keep `.planning/config.json` at `graphify.enabled=true` and set `graphify.auto_update=false`; asynchronous/default-branch indexing is unauthorized because it bypasses the 20% CPU wrapper and cannot prove terminal HEAD freshness. Create executable `.githooks/post-commit`, `.githooks/pre-push`, `scripts/phase33-post-execution-closeout.sh`, and `scripts/test-phase33-post-execution-closeout.sh`. The closeout is branch-agnostic and accepts an exact `--expected-head`; it must preserve every dirty/untracked user change in the shared `use_worktrees=false` checkout. Read phase-completion inputs from the expected commit, not from mutable working-tree files: require 16 PLANs, 16 SUMMARYs each containing `## Self-Check: PASSED`, `33-VERIFICATION.md` frontmatter `status: passed`, ROADMAP's handler-owned Phase 33 checkbox plus `16/16 plans complete` and sixteen checked plan rows, all eight canonical REQUIREMENTS checkboxes/table rows complete, and STATE frontmatter `status: completed` with progress `16/16/100`. These predicates deliberately match the real `requirements mark-complete` and `phase.complete` handlers; do not rely on static review-status prose.

Before and after every closeout, require the expected commit's `graphify.auto_update=false`, absence of the shared checkout's `.planning/graphs/.rebuild.lock`, and `.planning/graphs/.last-build-status.json` absent or not `running`; malformed status is failure. Compute a deterministic SHA-256 fingerprint from the exact commit tree (`git ls-tree -r -z --full-tree EXPECTED_HEAD`), then create a unique detached clean temporary Git worktree at that HEAD. Run the only indexer inside that clean worktree through its `./scripts/podman-admin.sh profile-run -- bash -lc 'graphify update .'`; never index the dirty shared checkout. Require the temporary `graphify-out/graph.json` `built_at_commit` equal to expected HEAD and run `graphify query ... --graph graphify-out/graph.json` for `RecordRelaySample`, `RerankHelper`, and `PerformanceHealthPanel`, each with at least one `NODE`. Atomically publish only those validated outputs into the original checkout's ignored `graphify-out/`, never tracked planning/docs. Remove the temporary worktree through Git, prove both registry and filesystem absence, and preserve original/cleanup rc independently so cleanup failure cannot be swallowed. Recheck current HEAD, commit-tree fingerprint, auto-update, lock, build-status, and unchanged shared-checkout porcelain snapshot before returning the exact marker `PHASE33_POST_EXECUTION_CLOSEOUT=PASS`.

The `post-commit` hook is a fast invalidator only. Filter exact canonical Phase 33 subjects `docs(33-16):...` and `docs(phase-33):...`, including phase completion, todo closure, and PROJECT evolution. For a match, resolve only the new HEAD and replace any prior PASS atomically with mode-0600 ignored `.git/phase33-post-execution-closeout.json` containing exactly `status=PENDING`, `release_closeout=NO-GO`, and `head=<new HEAD>`; PENDING is never authorizing and carries no fingerprint. Use shell builtins plus a declared exact foreground helper allowlist: `git rev-parse`, `git show`, `mktemp`, `chmod`, `mv`, and `rm` only, with bounded counts and every invocation awaited; expose the stable audit literal `POST_COMMIT_HELPER_ALLOWLIST=git-rev-parse,git-show,mktemp,chmod,mv,rm`. Use `umask 077`, a same-directory temporary file, `chmod 600`, and atomic `mv`; remove only that validated temp on failure. The hook must never call the closeout, Graphify, `podman-admin.sh`, a hash runtime, network client, container/build tool, `timeout`, or any other indexer; must never use `&`, `nohup`, `setsid`, `disown`, or leave a background/orphan/surviving helper; and must never change tracked docs/planning/graph files. Unrelated commits return immediately without mutating the marker. Production latency must remain less than 10 seconds and the disposable fixture must require less than 2 seconds, keeping canonical `gsd_run query commit` safely below its 10-second process timeout even after HEAD advances.

The range-aware `pre-push` hook is the sole consumed terminal closeout and the sole owner of the complete commit-tree fingerprint. Parse every stdin ref update before network mutation, ignore deletions/unrelated ranges, and fail closed on malformed refs/range inspection. For each distinct relevant non-delete `local_sha` containing a canonical Phase 33 subject, require pushes with multiple distinct relevant heads to be split. Treat every PENDING/NO-GO marker as non-authorizing regardless of HEAD; compute the deterministic SHA-256 fingerprint from the complete pushed commit tree at pre-push, and accept an existing PASS only when its HEAD and fingerprint match those freshly derived values. If exact current PASS is absent or stale, parse and validate integer `.planning/config.json` `graphify.build_timeout`, compute `closeout_wait_seconds=build_timeout+120`, require the result be at least 720, and invoke `timeout --foreground --signal=TERM --kill-after=30s "${closeout_wait_seconds}s" scripts/phase33-post-execution-closeout.sh --expected-head "$local_sha"` exactly once. Wait for that foreground process to finish and atomically record PASS only after rc 0; timeout rc 124/137 and every other nonzero record NO-GO and block. The `git push` caller must allow at least the same 720 seconds. After closeout, recompute and revalidate PASS marker, exact pushed HEAD, complete-tree fingerprint, `graphify.auto_update=false`, absent rebuild lock, non-running build status, `built_at_commit`, and positive query counts for all three governed terms before allowing push. A closeout or revalidation failure atomically records NO-GO and blocks. An exact current PASS retry skips Graphify and permits push after the same readbacks. No commit path runs Graphify, and no commit is created after this pre-network gate.

Install with `git config --local core.hooksPath .githooks` only after a fail-closed compatibility transaction. Capture the current getter rc/value in ignored `.git/phase33-hooks-install-state.json`: rc 1 means unset and may proceed; rc 0 with exact `.githooks` is idempotent; any other configured path is a hard conflict and must remain untouched. On write, readback, syntax, executable-bit, or fixture failure, restore the exact prior setting (including truly unset), prove restoration with rc-aware readback, write NO-GO install state, and exit nonzero. Never silently overwrite or copy unknown user hooks.

The fixture script must create disposable Git repositories with fake lightweight wrapper/Graphify executables while exercising the real hooks and closeout. It must prove: incomplete phase returns rc 3 without indexing; wrong HEAD fails; commit-tree fingerprint drift fails; wrapper failure propagates; mismatched `built_at_commit` produces stale/commit-stale failure; an empty query fails; and temporary-worktree cleanup failure propagates. For `post-commit`, invoke the real hook with PATH shims that log and forward only the declared helper allowlist to real binaries. Prove an unrelated subject causes zero marker/helper changes; a canonical subject completes in less than 2 seconds; invalidates an existing PASS into atomic mode-0600 JSON with exactly PENDING/NO-GO/new HEAD and no fingerprint; uses only `git rev-parse`, `git show`, `mktemp`, `chmod`, `mv`, and `rm` in bounded foreground counts; invokes Graphify, `podman-admin.sh`, closeout, hash runtimes, network clients, containers, and build tools zero times; contains no `&`, `nohup`, `setsid`, or `disown`; and leaves no background/orphan/surviving helper after return. For `pre-push`, prove it derives the complete-tree fingerprint itself; blocks missing, PENDING, NO-GO/stale, wrong-HEAD, drifted-fingerprint, wrapper-failure, stale-build and empty-query states; invokes the fake closeout, wrapper, and fake Graphify exactly once each in the foreground for a missing/stale PASS; waits for a controlled short slow-closeout fixture to finish before it allows; and leaves no orphan. An exact current PASS retry must not increment any invocation count, while a failed closeout retry reruns once and can pass. The happy-path origin checkout must contain unrelated dirty and untracked user fixtures before the hook, remain byte-for-byte/porcelain-identical afterward, and still pass because Graphify ran in the detached worktree. Also prove a preexisting noncanonical `core.hooksPath` is preserved and install aborts. The fixture must clean every temporary worktree/directory and fail if cleanup cannot be proven; syntax/token greps alone are insufficient.

Only after scripts, hooks, handler-format fixtures, exact readbacks, and secret scans pass, remove the retained worktree through `/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard`, prove filesystem and registry absence with rc-aware checks, and then record `knowledge_sync_status=pass`, `retained_worktree_cleanup=pass`, `post_execution_graphify_closeout=git-hooks-pre-push`, `post_commit_state=PENDING`, `graphify_auto_update=false`, and `release_closeout=NO-GO` in JSON/Markdown. That NO-GO remains until pre-push has synchronously produced and revalidated exact current PASS. Do not create or claim `33-16-SUMMARY.md` inside the task; the canonical executor creates it afterward, and its fast post-commit hook invalidates the marker without running Graphify.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail
lev=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.json
manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
evidence=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-EVIDENCE.md
runbook=docs/ATIUS-RERANKER-RELIABILITY.md
obs=/home/ubuntu/GitHub/obsidian-vault/AiSecondBrain/Projects/router-ai-atius/phase-33-reranker-readiness.md
ui=.planning/phases/33-reranker-reliability-observability-and-readiness/33-LIVE-UI-EVIDENCE
guard=/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard
closeout_script=scripts/phase33-post-execution-closeout.sh
closeout_test=scripts/test-phase33-post-execution-closeout.sh
post_commit_hook=.githooks/post-commit
pre_push_hook=.githooks/pre-push
config=.planning/config.json
gpage=$(mktemp)
gtimeline=$(mktemp)
registry=$(mktemp)
registry_after=$(mktemp)
closeout_check=$(mktemp)
cleanup_tmp(){ rm -f -- "$gpage" "$gtimeline" "$registry" "$registry_after" "$closeout_check"; }
trap cleanup_tmp EXIT
require_secret_absent(){
  local rc
  set +e
  rg -n -e '"'"'(Authorization: Bearer [A-Za-z0-9]|sk-[A-Za-z0-9]{8}|api[_-]?key[=:][[:space:]]*[^$&lt;{])'"'"' -- "$@" &gt;/dev/null
  rc=$?
  set -e
  case "$rc" in
    1) return 0 ;;
    0) printf "secret-shaped material found\n" &gt;&amp;2; return 1 ;;
    *) printf "secret scan failed rc=%s\n" "$rc" &gt;&amp;2; return "$rc" ;;
  esac
}
require_pattern_absent(){
  local pattern=$1 file=$2 rc
  set +e
  rg -n -e "$pattern" -- "$file" &gt;/dev/null
  rc=$?
  set -e
  case "$rc" in
    1) return 0 ;;
    0) printf "forbidden hook operation found in %s\n" "$file" &gt;&amp;2; return 1 ;;
    *) printf "hook scan failed rc=%s file=%s\n" "$rc" "$file" &gt;&amp;2; return "$rc" ;;
  esac
}
check_graph_idle(){
  test ! -e .planning/graphs/.rebuild.lock
  if test -e .planning/graphs/.last-build-status.json; then
    python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert isinstance(d,dict) and d.get("status") != "running"'"'"' .planning/graphs/.last-build-status.json
  fi
}
omni=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified" and d["expected_commit"]==d["origin_main_commit"]; print(d["worktree_path"])'"'"' "$manifest")
expected=$(python3 -c '"'"'import json,sys; print(json.load(open(sys.argv[1]))["expected_commit"])'"'"' "$manifest")
planning=$(python3 -c '"'"'import hashlib,json,re,sys; from pathlib import Path; lev=Path(sys.argv[1]); phase=Path(".planning/phases/33-reranker-reliability-observability-and-readiness"); paths=sorted([*phase.glob("33-*-PLAN.md"),phase/"33-CONTEXT.md",phase/"33-RESEARCH.md",phase/"33-UI-SPEC.md",phase/"33-VALIDATION.md"],key=lambda p:p.as_posix()); assert len(paths)==20 and all(p.is_file() for p in paths); entries=[{"path":p.as_posix(),"sha256":hashlib.sha256(p.read_bytes()).hexdigest()} for p in paths]; identity="sha256:"+hashlib.sha256(json.dumps(entries,sort_keys=True,separators=(",",":")).encode()).hexdigest(); assert re.fullmatch(r"sha256:[0-9a-f]{64}",identity); d=json.load(open(lev)); assert d["planning_identity_manifest"]==entries and d["planning_identity"]==identity; print(identity)'"'"' "$lev")
execution=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); x=d["execution_identity"]; assert x and x not in ("current","latest"); print(x)'"'"' "$lev")
candidate=$(python3 -c '"'"'import json,re,sys; d=json.load(open(sys.argv[1])); x=d["candidate_digest"]; assert re.fullmatch(r"sha256:[0-9a-f]{64}",x); print(x)'"'"' "$lev")
running=$(python3 -c '"'"'import json,re,sys; d=json.load(open(sys.argv[1])); x=d["running_digest"]; assert re.fullmatch(r"sha256:[0-9a-f]{64}",x) and x==d["candidate_digest"]; print(x)'"'"' "$lev")
state=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["metrics_state"] in ("partial","complete"); print(d["metrics_state"])'"'"' "$lev")
verdict=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["final_verdict"]=="PASS"; print(d["final_verdict"])'"'"' "$lev")
rollback=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["rollback_status"]=="not_required"; print(d["rollback_status"])'"'"' "$lev")
test "$(podman inspect container-router-ai-atius --format '"'"'{{.Image}}'"'"')" = "$running"
test "$(podman image inspect "$running" --format '"'"'{{ index .Labels "org.opencontainers.image.revision" }}'"'"')" = "$execution"
test -d "$guard"
test -d "$omni"
set +e
git -C "$guard" worktree list --porcelain &gt;"$registry"
list_rc=$?
set -e
test "$list_rc" -eq 0
set +e
rg -Fx -- "worktree $omni" "$registry" &gt;/dev/null
registered_rc=$?
set -e
case "$registered_rc" in
  0) ;;
  1) printf "required worktree is not registered\n" &gt;&amp;2; exit 1 ;;
  *) printf "worktree registry scan failed rc=%s\n" "$registered_rc" &gt;&amp;2; exit "$registered_rc" ;;
esac
test "$(git -C "$omni" rev-parse HEAD)" = "$expected"
test -z "$(git -C "$omni" status --porcelain)"
slug=router-ai-atius-phase-33-reranker-readiness
/home/ubuntu/.local/bin/gbrain get "$slug" &gt;"$gpage"
/home/ubuntu/.local/bin/gbrain timeline "$slug" &gt;"$gtimeline"
for sink in "$evidence" "$runbook" "$gpage" "$gtimeline" "$obs"; do
  for marker in "Planning identity: $planning" "Execution identity: $execution" "Candidate digest: $candidate" "Running digest: $running" "Metrics state: $state" "Final verdict: $verdict" "Rollback status: $rollback"; do
    rg -Fx -- "$marker" "$sink" &gt;/dev/null
  done
done
test -d "$ui"
require_secret_absent "$runbook" "$lev" "$evidence" "$obs" "$gpage" "$gtimeline" "$ui"
python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["graphify"]["enabled"] is True and d["graphify"]["auto_update"] is False'"'"' "$config"
check_graph_idle
for executable in "$closeout_script" "$closeout_test" "$post_commit_hook" "$pre_push_hook"; do test -x "$executable"; bash -n "$executable"; done
"$closeout_test"
set +e
hooks_path=$(git config --local --get core.hooksPath)
hooks_rc=$?
set -e
test "$hooks_rc" -eq 0
test "$hooks_path" = .githooks
install_state=$(git rev-parse --git-path phase33-hooks-install-state.json)
test -f "$install_state"
python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["status"]=="PASS" and d["installed_hooks_path"]==".githooks" and d["readback"]==".githooks"'"'"' "$install_state"
for marker in "--expected-head" "--check-prerequisites-only" "33-VERIFICATION.md" "PHASE33_POST_EXECUTION_CLOSEOUT=PASS" "graphify-out/graph.json" "RecordRelaySample" "RerankHelper" "PerformanceHealthPanel"; do rg -F -- "$marker" "$closeout_script" &gt;/dev/null; done
rg -F -- "./scripts/podman-admin.sh profile-run -- bash -lc" "$closeout_script" &gt;/dev/null
rg -F -- "docs(33-16):" "$post_commit_hook" &gt;/dev/null
rg -F -- "docs(phase-33):" "$post_commit_hook" &gt;/dev/null
rg -F -- "phase33-post-execution-closeout.json" "$post_commit_hook" "$pre_push_hook" &gt;/dev/null
rg -F -- "PENDING" "$post_commit_hook" &gt;/dev/null
rg -F -- "NO-GO" "$post_commit_hook" &gt;/dev/null
rg -F -- "POST_COMMIT_HELPER_ALLOWLIST=git-rev-parse,git-show,mktemp,chmod,mv,rm" "$post_commit_hook" &gt;/dev/null
rg -F -- "--expected-head" "$pre_push_hook" &gt;/dev/null
rg -F -- "$closeout_script" "$pre_push_hook" &gt;/dev/null
rg -F -- "720" "$pre_push_hook" &gt;/dev/null
rg -F -- "timeout --foreground --signal=TERM --kill-after=30s" "$pre_push_hook" &gt;/dev/null
require_pattern_absent "(graphify|podman-admin|scripts/phase33-post-execution-closeout\\.sh|curl|wget|ssh|podman|docker|buildah|python3|node[[:space:]]|sha256sum|nohup|setsid|disown|timeout)" "$post_commit_hook"
require_pattern_absent "(^|[[:space:]])&amp;([[:space:]]|$)" "$post_commit_hook"
set +e
"$closeout_script" --check-prerequisites-only --expected-head "$(git rev-parse HEAD)" &gt;"$closeout_check" 2&gt;&amp;1
closeout_rc=$?
set -e
test "$closeout_rc" -eq 3
rg -F -- "PHASE33_POST_EXECUTION_CLOSEOUT=NO-GO" "$closeout_check" &gt;/dev/null
require_secret_absent "$runbook" "$lev" "$evidence" "$obs" "$gpage" "$gtimeline" "$closeout_script" "$closeout_test" "$post_commit_hook" "$pre_push_hook" "$ui"
check_graph_idle
git -C "$guard" worktree remove "$omni"
test ! -e "$omni"
set +e
git -C "$guard" worktree list --porcelain &gt;"$registry_after"
list_after_rc=$?
set -e
test "$list_after_rc" -eq 0
set +e
rg -Fx -- "worktree $omni" "$registry_after" &gt;/dev/null
absent_rc=$?
set -e
case "$absent_rc" in
  1) ;;
  0) printf "removed worktree remains registered\n" &gt;&amp;2; exit 1 ;;
  *) printf "worktree absence scan failed rc=%s\n" "$absent_rc" &gt;&amp;2; exit "$absent_rc" ;;
esac
python3 -c '"'"'import json,sys; p=sys.argv[1]; d=json.load(open(p)); d["knowledge_sync_status"]="pass"; d["retained_worktree_cleanup"]="pass"; d["post_execution_graphify_closeout"]="git-hooks-pre-push"; d["post_commit_state"]="PENDING"; d["graphify_auto_update"]=False; d["release_closeout"]="NO-GO"; open(p,"w").write(json.dumps(d,indent=2,sort_keys=True)+"\n")'"'"' "$lev"
printf "\nKnowledge synchronization: PASS\nRetained worktree cleanup: PASS\nPost-execution Graphify closeout: GIT-HOOKS-PRE-PUSH\nPost-commit state: PENDING\nGraphify auto update: DISABLED\nRelease closeout: NO-GO\n" &gt;&gt;"$evidence"
for marker in "Knowledge synchronization: PASS" "Retained worktree cleanup: PASS" "Post-execution Graphify closeout: GIT-HOOKS-PRE-PUSH" "Post-commit state: PENDING" "Graphify auto update: DISABLED" "Release closeout: NO-GO"; do rg -Fx -- "$marker" "$evidence" &gt;/dev/null; done
python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["knowledge_sync_status"]=="pass" and d["retained_worktree_cleanup"]=="pass" and d["post_execution_graphify_closeout"]=="git-hooks-pre-push" and d["post_commit_state"]=="PENDING" and d["graphify_auto_update"] is False and d["release_closeout"]=="NO-GO"'"'"' "$lev"
check_graph_idle'</automated>
  </verify>
  <acceptance_criteria>
    - The retained worktree exists, is registered, clean, and pinned when Task 2 begins; absence at entry fails.
    - Exact authoritative values and all Task 1 readbacks pass again before cleanup.
    - Cleanup PASS is recorded only after both filesystem and registry absence are proven.
    - `33-16-SUMMARY.md` is absent from task ownership and is left for the canonical executor after verification.
    - `graphify.auto_update=false`; no lock/running status exists; all real indexing is routed through the 20%-CPU wrapper.
    - Disposable fixtures exercise real handlers, a dirty origin checkout, detached clean-worktree indexing, every specified failure, post-commit latency below 2 seconds with only bounded allowlisted foreground helpers and zero Graphify/network/container/build invocations, synchronous pre-push wait, and retry/allow/block behavior.
    - Existing noncanonical hooksPath is never overwritten; actual local readback is exactly `.githooks`, with rollback state retained under `.git`.
    - The closeout refuses the incomplete phase with exact rc 3; final Graphify output and PASS/NO-GO state live only in ignored `graphify-out/` and `.git` paths.
    - Live evidence remains `release_closeout=NO-GO` with `post_execution_graphify_closeout=git-hooks-pre-push` and `post_commit_state=PENDING` until pre-push synchronously creates and revalidates exact current PASS.
    - Canonical 33-16/phase-33 commits consume the fast invalidation lifecycle automatically; the relevant `git push` caller allows at least 720 seconds so pre-push owns the only terminal closeout before network mutation.
  </acceptance_criteria>
  <done>The exact retained worktree is safely removed, asynchronous Graphify is disabled, post-commit remains below the commit timeout by only recording PENDING/NO-GO, and pre-push synchronously produces and enforces the current terminal closeout while release remains fail-closed.</done>
</task>

</tasks>

<git_lifecycle_closeout>
## Consumed Git lifecycle closeout

`core.hooksPath=.githooks` is installed and read back during Task 2. The fast `post-commit` hook ignores unrelated commits and, after exact `docs(33-16):...` or `docs(phase-33):...` commits, only replaces prior PASS with atomic mode-0600 PENDING/NO-GO plus the new HEAD. It uses only bounded, awaited foreground `git rev-parse`/`git show`/`mktemp`/`chmod`/`mv`/`rm` helpers, performs no Graphify, wrapper, closeout, hash, network, container or build work, leaves no surviving helper, and must return in less than 10 seconds; the fixture tightens this to less than 2 seconds.

The range-aware `pre-push` hook is the synchronous terminal closeout and release enforcement point. For a relevant pushed local SHA, missing/stale PASS causes one foreground closeout against that SHA; the hook waits at least the configured 600-second build timeout plus margin (720 seconds allowed by the push caller), atomically records PASS only on success, then revalidates exact pushed HEAD, commit-tree fingerprint, disabled auto-update, idle Graphify state, `built_at_commit`, and three nonempty queries before network mutation. Exact current PASS retries skip reindexing. `execute:post`, commit hooks, asynchronous auto-update, syntax checks, and a historical PASS never satisfy this gate.
</git_lifecycle_closeout>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Final synchronized `33-LIVE-EVIDENCE.md` and Router operator runbook.
- Final authoritative `33-LIVE-EVIDENCE.json` with Git-hook/NO-GO closeout state; the canonical executor creates `33-16-SUMMARY.md` afterward.
- Versioned fast-invalidation post-commit and synchronous-closeout pre-push hooks, the governed closeout script, and disposable lifecycle fixtures; terminal graph/status outputs remain ignored.
- Matching sanitized GBrain page/timeline and Obsidian operational note.
- Verified removal of the retained clean Omni worktree after successful readback.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production runtime to durable records | Live identity and state are copied into multiple long-lived evidence systems. |
| credentialed knowledge bridges | GBrain and Obsidian updates cross authenticated local/remote interfaces. |
| repository to Graphify | Changed source/docs/planning artifacts become graph nodes and edges. |
| Git worktree registry to filesystem | Cleanup removes a retained release-control checkout. |
| Git hooks to push boundary | Post-commit cheaply invalidates stale proof; pre-push performs and authorizes the terminal proof before remote mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-16-01 | Repudiation | final cross-system evidence | high | mitigate | Extract seven exact authoritative values from live JSON and require exact-line readback from every sink. |
| T-33-16-02 | Information Disclosure | GBrain, Obsidian, docs, and graph artifacts | high | mitigate | Persist aggregate metadata only and scan durable outputs for credential-shaped content. |
| T-33-16-03 | Tampering | stale or empty Graphify result | high | mitigate | Disable auto-update; make post-commit invalidate PASS; make pre-push synchronously index the exact pushed commit in a detached clean worktree, bind PASS to HEAD/tree fingerprint and three positive queries, and block on mismatch. |
| T-33-16-04 | Denial of Service | retained-worktree cleanup | medium | mitigate | Require the exact path to exist and be registered/clean/pinned at task entry; prove disk and registry absence before recording PASS. |
| T-33-16-05 | Tampering | existing Git hook configuration | high | mitigate | Refuse a noncanonical hooksPath without mutation; transactionally install/read back `.githooks` and restore the exact prior setting on any failure. |
| T-33-16-06 | Repudiation | dirty shared checkout | high | mitigate | Snapshot/preserve shared porcelain and run pre-push Graphify only in a temporary detached worktree at expected HEAD, with fail-closed registry/filesystem cleanup. |
| T-33-16-07 | Denial of Service | commit and push hook latency | high | mitigate | Keep post-commit below 10 seconds using only bounded awaited allowlisted foreground helpers, with a fixture threshold below 2 seconds and zero indexer/network/container/build calls; require the push caller to allow at least 720 seconds while foreground pre-push waits for the CPU-capped closeout and blocks on timeout/failure. |
| T-33-16-SC | Tampering | package installs | high | mitigate | No package installation is authorized; all tooling already exists and any install attempt aborts. |
</threat_model>

<verification>
- Runtime remains on the promoted immutable digest while final records are synchronized.
- GBrain, Obsidian, Router docs, and live evidence reproduce all exact JSON-authoritative fields.
- The exact retained worktree exists at Task 2 entry and is removed only after prerequisite readback; cleanup PASS follows absence proofs.
- Plan verification proves real hook installation plus sandbox lifecycle behavior; canonical Git commits only invalidate proof, and pre-push synchronously performs/enforces the final sync.
</verification>

<success_criteria>
- Phase 33 final operational truth is queryable, consistent, sanitized, and state-aware across every required system.
- No cleanup occurs before successful direct readback.
- No source/runtime mutation is introduced by the final synchronization plan.
- Release remains NO-GO until the ignored status matches the current pushed HEAD/tree fingerprint and pre-push accepts it.
</success_criteria>

<output>
The canonical executor creates `.planning/phases/33-reranker-reliability-observability-and-readiness/33-16-SUMMARY.md` after task verification. Installed post-commit invalidates PASS for that commit and all later canonical Phase 33 commits without indexing; a relevant `git push` with at least a 720-second caller allowance invokes the synchronous pre-push closeout as the final fail-closed release gate.
</output>
