---
phase: 33-reranker-reliability-observability-and-readiness
plan: "12"
type: execute
wave: 5
depends_on: ["33-03", "33-07", "33-08", "33-09", "33-11"]
files_modified:
  - k8s/router-ai-atius/configmap.yaml
  - scripts/smoke-reranker-readiness.py
  - tests/test_clianything.py
  - docs/ATIUS-RERANKER-RELIABILITY.md
autonomous: true
requirements:
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-CHANNEL-READINESS
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Router config preserves both governor aliases and canonical unbounded static ceilings 0/0 without performing k3s cutover."
    - "One sanitized script proves readiness, valid rerank requests of up to 20 documents, and deterministic 21-document rejection against candidate/local/public targets."
    - "Runbook documents client sub-batches of at most 20 with stable index recomposition, candidate gates, immutable rollback, and evidence hygiene."
  artifacts:
    - path: "k8s/router-ai-atius/configmap.yaml"
      provides: "Canonical governed alias/ceiling persistence"
    - path: "scripts/smoke-reranker-readiness.py"
      provides: "Sanitized readiness and rerank evidence"
    - path: "docs/ATIUS-RERANKER-RELIABILITY.md"
      provides: "Operator/client/release/rollback contract"
  key_links:
    - from: "scripts/smoke-reranker-readiness.py"
      to: "/api/channel/10/readiness"
      via: "Vault-hydrated admin credential"
      pattern: "channel/10/readiness"
    - from: "k8s/router-ai-atius/configmap.yaml"
      to: "service/embeddinggovernor"
      via: "exact EMBEDDING_GOVERNOR_* environment"
      pattern: "embedding-gte-v1,reranker-gte-multilingual-v1"
---

<objective>
Persist the governed runtime contract and add sanitized, repeatable candidate/local/public validation plus operator guidance.

Purpose: Prepare exact artifacts that omni release guards and later promotion plans can verify without leaking content.
Output: Configmap correction, smoke script/tests, and complete reranker reliability runbook.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-03-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-07-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-08-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-09-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-11-SUMMARY.md
@k8s/router-ai-atius/configmap.yaml
@scripts/smoke-embeddings.py
@tests/test_clianything.py
@docs/ATIUS-RERANKER-RELIABILITY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist exact governor aliases/ceilings and implement sanitized smoke</name>
  <files>k8s/router-ai-atius/configmap.yaml, scripts/smoke-reranker-readiness.py</files>
  <read_first>k8s/router-ai-atius/configmap.yaml, scripts/smoke-embeddings.py, controller/channel_readiness.go, relay/rerank_handler.go, AGENTS.md</read_first>
  <action>Set the configmap source values exactly to `EMBEDDING_GOVERNOR_ENABLED=true`, `EMBEDDING_GOVERNOR_MODELS=embedding-gte-v1,reranker-gte-multilingual-v1`, `EMBEDDING_GOVERNOR_MAX_CONCURRENCY=0`, and `EMBEDDING_GOVERNOR_BATCH_CONCURRENCY=0`; do not apply the configmap or perform Phase 29/30 cutover. Create a Python smoke patterned after smoke-embeddings.py with `candidate|local|public` modes, environment-only admin/user credentials and bounded timeout. Its mandatory first network operation is authenticated POST `/api/channel/10/readiness`; it must assert channel 10, canonical alias, ability, route and cache readiness and exit nonzero without sending any rerank when readiness is false. Only then send a synthetic rerank containing up to 20 documents and a deterministic 21-document 4xx case. Restrict output to target mode, commit/digest, status, channel/model, result count/indices, fixed reason code, and elapsed time. Scrub auth/query/documents/request/provider bodies on all success/error paths.</action>
  <verify>
    <automated>python3 -m py_compile scripts/smoke-reranker-readiness.py &amp;&amp; python3 -c 'import yaml; d=yaml.safe_load(open("k8s/router-ai-atius/configmap.yaml")); x=d["data"]; assert x["EMBEDDING_GOVERNOR_MODELS"]=="embedding-gte-v1,reranker-gte-multilingual-v1" and x["EMBEDDING_GOVERNOR_MAX_CONCURRENCY"]=="0" and x["EMBEDDING_GOVERNOR_BATCH_CONCURRENCY"]=="0"'</automated>
  </verify>
  <acceptance_criteria>
    - Exact alias and 0/0 values are present in source only.
    - Missing credential exits before network; every HTTP operation has a bound.
    - Script output allowlist excludes all sensitive request/upstream content.
  </acceptance_criteria>
  <done>Runtime source and smoke tooling express the complete governed reranker contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete smoke tests and the client/operator/release runbook</name>
  <files>tests/test_clianything.py, docs/ATIUS-RERANKER-RELIABILITY.md</files>
  <read_first>tests/test_clianything.py, scripts/smoke-reranker-readiness.py, docs/ATIUS-RERANKER-RELIABILITY.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md</read_first>
  <behavior>
    - Smoke contract test passes for source/output/redaction/target coverage.
    - Client docs keep max 20 and explain chunkOffset + returned index without claiming transparent global reranking.
    - Operator docs require readiness, candidate labels/digest, old digest capture, local/public readback, and automatic rollback.
  </behavior>
  <action>Make the Plan 33-03 smoke test pass and add exact helper-level redaction/shape cases. Extend the runbook with the selected timeout from Plan 33-07; seven outcome meanings/formulas/partial history; cache/readiness reason codes; client-side chunks of at most 20 with `global_index = chunk_offset + result.index`; explicit warning that the Router does not globally rerank or rebill across chunks; exact config/channel/source invariants; governed test/build commands; OCI labels; candidate isolation; immutable live digest capture; promotion/rollback order; local/authenticated public UI/API readback; and evidence allowlist. Document only Vault profile/variable names, never values.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; python3 -m unittest tests.test_clianything.Phase19ProviderRoutingTests.test_smoke_reranker_readiness_contract -v; set +e; env -u ATIUS_ROUTER_TOKEN -u ATIUS_ROUTER_ADMIN_TOKEN python3 scripts/smoke-reranker-readiness.py --target local; smoke_rc=$?; set -e; test "$smoke_rc" -eq 2'</automated>
  </verify>
  <acceptance_criteria>
    - Python smoke tests and no-token exit pass.
    - Runbook includes max 20, stable indices, no transparent Router batching, selected timeout, candidate-before-promotion, rollback, and redaction.
    - No secret literal or live request body appears in source/docs/tests.
  </acceptance_criteria>
  <done>Operator/client evidence procedures are executable and privacy-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full Router pre-release regression gate</name>
  <files>scripts/smoke-reranker-readiness.py, docs/ATIUS-RERANKER-RELIABILITY.md</files>
  <read_first>.planning/phases/33-reranker-reliability-observability-and-readiness/33-VALIDATION.md, scripts/atius-user-quota-guard.sh, patches/atius-user-quota-unlimited.patch, scripts/podman-admin.sh</read_first>
  <action>Run the complete Phase 33 focused Go suites, frontend test/typecheck, smoke contract, PT-BR smoke, config assertions, and `scripts/atius-user-quota-guard.sh` through the Router resource wrapper. Confirm the wrapper reports the 20%/0.8 CPU profile before and after. Do not build or restart production in this plan; record only pass/fail and counts in the summary.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc '/usr/local/go/bin/go test ./pkg/perf_metrics ./model ./middleware ./controller ./relay ./relay/channel ./relay/channel/advancedcustom ./service/embeddinggovernor -count=1 &amp;&amp; cd web/default &amp;&amp; bun run typecheck &amp;&amp; bun test src/features/performance-metrics' &amp;&amp; bash scripts/atius-user-quota-guard.sh</automated>
  </verify>
  <acceptance_criteria>
    - Backend/frontend/guard gates pass under the resource wrapper.
    - CPU readback remains at or below 0.8 CPU.
    - No runtime, image tag, channel, DB, or public route is changed.
  </acceptance_criteria>
  <done>Router source is ready for omni release protection and candidate build.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `scripts/smoke-reranker-readiness.py` with candidate/local/public modes.
- Canonical configmap alias/ceiling values.
- `docs/ATIUS-RERANKER-RELIABILITY.md` client/operator/release runbook.
- `test_smoke_reranker_readiness_contract` passing implementation coverage.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vault-hydrated credentials to smoke targets | Credentials authorize admin/public probes. |
| Source config to future runtime | Incorrect aliases/ceilings can remove or overconstrain governed service. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-33 | Information Disclosure | smoke/docs | high | mitigate | Strict output allowlist, redaction tests, Vault variable names only. |
| T-33-34 | Tampering | governor config | high | mitigate | Exact alias/0/0 assertions and downstream omni protection. |
| T-33-35 | Denial of Service | heavy verification | medium | mitigate | All heavy checks through 20%/0.8 CPU wrapper. |
</threat_model>

<verification>
Run all three task gates; inspect summary evidence for sanitized counts/status only. Stop before any build/deploy.
</verification>

<success_criteria>
- Exact config, smoke, docs, and test contracts exist.
- Full focused source regression is green under CPU cap.
- No runtime mutation occurs.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-12-SUMMARY.md` when done.
</output>
