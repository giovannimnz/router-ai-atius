---
phase: 33-reranker-reliability-observability-and-readiness
plan: "11"
type: execute
wave: 3
depends_on: ["33-03", "33-05", "33-10"]
files_modified:
  - web/default/src/features/performance-metrics/types.ts
  - web/default/src/features/performance-metrics/lib/outcome-summary.ts
  - web/default/src/features/dashboard/components/overview/performance-health-panel.tsx
  - web/default/src/features/dashboard/components/models/performance-overview.tsx
  - web/default/src/features/dashboard/components/performance-metrics-panels.test.tsx
autonomous: true
requirements:
  - PHASE-33-WEIGHTED-DASHBOARD
  - PHASE-33-METRICS-OUTCOME-TAXONOMY
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Both dashboard surfaces use one pure helper over raw totals and never average model percentages."
    - "Request success and service availability are separately labeled; all seven outcomes remain visible."
    - "Loading, complete, partial, empty, error, and refetching states match the approved accessible UI contract."
    - "Deterministic server-rendered component fixtures cover both panels, PT-BR, the 8/9 incident, mobile/dark classes, and accessibility; unchanged components fail the tests."
  artifacts:
    - path: "web/default/src/features/performance-metrics/lib/outcome-summary.ts"
      provides: "Pure weighted/partial aggregation"
    - path: "performance-health-panel.tsx"
      provides: "Four KPIs, seven outcomes, top models, and state handling"
    - path: "performance-overview.tsx"
      provides: "Compact weighted request/service strip"
    - path: "performance-metrics-panels.test.tsx"
      provides: "Direct component-render regression suite for both dashboard surfaces"
  key_links:
    - from: "outcome-summary.ts"
      to: "types.ts"
      via: "schema-v2 overall/model raw totals"
      pattern: "PerfOutcomeTotals|schema_version"
    - from: "performance-health-panel.tsx"
      to: "outcome-summary.ts"
      via: "deriveOutcomeSummary for every loaded/partial state"
      pattern: "deriveOutcomeSummary"
---

<objective>
Implement the approved weighted, partial-aware reranker health UI in both existing dashboard surfaces.

Purpose: Make request outcome and serving availability answerable without redesign or semantic overclaiming.
Output: Additive TypeScript contract, shared pure helper, two updated React panels, and direct deterministic component-render tests.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.agents/skills/vercel-react-best-practices/SKILL.md
@.agents/skills/shadcn-ui/SKILL.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-03-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-05-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-10-SUMMARY.md
@web/default/src/features/performance-metrics/types.ts
@web/default/src/features/performance-metrics/api.ts
@web/default/src/features/dashboard/components/overview/performance-health-panel.tsx
@web/default/src/features/dashboard/components/models/performance-overview.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive types and the shared weighted/partial helper</name>
  <files>web/default/src/features/performance-metrics/types.ts, web/default/src/features/performance-metrics/lib/outcome-summary.ts</files>
  <read_first>web/default/src/features/performance-metrics/types.ts, web/default/src/features/performance-metrics/api.ts, web/default/src/features/performance-metrics/lib/format.ts, web/default/src/features/performance-metrics/lib/outcome-summary.test.ts, .planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md</read_first>
  <behavior>
    - Helper prefers complete backend overall, otherwise sums only complete raw model rows.
    - Missing required fields returns partial/null; legacy-only rows never enter weighted overall.
    - Exact UI-SPEC formulas and fixed outcome order are used.
  </behavior>
  <action>Extend PerfSummaryAllData additively with optional `schema_version`, `overall`, and per-model raw counters/totals; keep all legacy fields. Create `deriveOutcomeSummary` returning discriminated complete/partial values for requestSuccess, serviceAvailability, averageLatency, throughput, seven outcome rows, and partial reasons. Sum raw integers before one division, use latency_sample_count and generation_ms denominators, preserve null for zero/missing denominators, and never return NaN/Infinity. Do not add `any`, packages, network calls, or UI concerns to the helper.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'cd web/default &amp;&amp; bun test src/features/performance-metrics/lib/outcome-summary.test.ts &amp;&amp; bun run typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - Every Plan 33-03 helper test passes.
    - Legacy types remain source-compatible.
    - Helper has no React/browser dependency and no simple-average code.
  </acceptance_criteria>
  <done>One tested calculation contract serves both panels.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PerformanceHealthPanel with semantic KPIs/outcomes/states</name>
  <files>web/default/src/features/dashboard/components/overview/performance-health-panel.tsx, web/default/src/features/dashboard/components/performance-metrics-panels.test.tsx</files>
  <read_first>web/default/src/features/dashboard/components/overview/performance-health-panel.tsx, web/default/src/features/performance-metrics/lib/outcome-summary.ts, web/default/src/features/channels/components/codex/codex-credential-panel.test.tsx, web/default/src/i18n/locales/pt.json, .planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md</read_first>
  <action>Preserve the existing full-width panel shell/query key/24-hour window and replace local simpleAverage calculations with deriveOutcomeSummary. Extract/export a deterministic presentational `PerformanceHealthPanelView` receiving explicit query/render state while keeping the existing container/query API unchanged, so tests render the real panel with `renderToStaticMarkup` rather than inspect source strings. Render four KPI cells in exact order, all seven outcome rows in fixed order, and up to six top-model rows. Implement approved loading, complete, partial, zero-denominator, empty, error, and refetching behavior using existing Skeleton, Empty, Alert, Button, Badge, and Tooltip; keep retry:false and cached values during refetch. Use semantic section/aria-labelledby/list or dl markup, visible labels/counts/percentages, 44px recovery target, 2-column mobile/4-column sm grid, dark-theme-compatible tokens, no horizontal scroll, and PT-BR keys from Plan 33-10. Create the component test with node:test, assert, React server rendering, and pt.json following the existing Codex panel pattern. Include the 8-success/9-request fixture and assertions for loading, partial, zero, empty, error, refetch, PT-BR labels, fixed outcome order, accessibility attributes, mobile width/grid classes, dark tokens, and absence of semantic overclaims. The fixture must import and render the exported real view so unchanged production JSX fails. Do not add a browser/test package.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'cd web/default &amp;&amp; bun test src/features/performance-metrics/lib/outcome-summary.test.ts src/features/dashboard/components/performance-metrics-panels.test.tsx &amp;&amp; bun run typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - Four KPIs, seven outcome classes, six-model limit, and approved states exist.
    - Error recovery refetches the same query and automatic retry remains disabled.
    - No new chart/package/route/theme/animation or unapproved spacing/typography is introduced.
    - Direct rendered fixtures cover loading, partial, zero, empty, error, refetch, PT-BR, fixed outcome order, accessibility, mobile/dark styling, and the 8/9 case.
  </acceptance_criteria>
  <done>The overview panel communicates request and service health correctly and accessibly.</done>
</task>

<task type="auto">
  <name>Task 3: Extend PerformanceOverview with the same weighted semantics</name>
  <files>web/default/src/features/dashboard/components/models/performance-overview.tsx, web/default/src/features/dashboard/components/performance-metrics-panels.test.tsx</files>
  <read_first>web/default/src/features/dashboard/components/models/performance-overview.tsx, web/default/src/features/dashboard/components/performance-metrics-panels.test.tsx, web/default/src/features/performance-metrics/lib/outcome-summary.ts, .planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md</read_first>
  <action>Replace the compact strip's simple-average logic with deriveOutcomeSummary and extract/export a deterministic `PerformanceOverviewView` receiving explicit render state while preserving the existing container/query API. Show translated Request success and Service availability beside latency/throughput while preserving the bordered flex-wrap shell and top-model badges. Per-model badges may keep legacy request success, but their accessible label/tooltip must say Request success. Render partial/zero values with the helper contract and preserve responsive wrapping without horizontal scrolling. Extend `performance-metrics-panels.test.tsx` to import and directly server-render both real views for the same loading/partial/zero/empty/error/refetch/8-of-9 fixtures; assert PT-BR labels, fixed seven-outcome order where present, semantic headings/names, 44px recovery control, mobile flex/grid behavior, dark-theme tokens, cached data during refetch, and no NaN/Infinity. Add an explicit mutation guard assertion that fails if either view omits `deriveOutcomeSummary`-derived request success/service availability or retains the old single-success-only markup.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'cd web/default &amp;&amp; bun test src/features/performance-metrics src/features/dashboard/components/performance-metrics-panels.test.tsx &amp;&amp; bun run typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - Both panels import the same helper and contain no simpleAverage implementation.
    - Service availability appears beside request success with accessible names.
    - Frontend focused tests and typecheck pass through the CPU wrapper.
    - The deterministic test directly renders both panels and fails against either unchanged pre-Phase-33 component.
  </acceptance_criteria>
  <done>Both dashboard surfaces share one correct weighted contract.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- TypeScript `PerfOutcomeTotals`, additive schema-v2 summary fields, `OutcomeSummary`, and `deriveOutcomeSummary`.
- Extended PerformanceHealthPanel and PerformanceOverview render states/labels/outcomes.
- `performance-metrics-panels.test.tsx` direct-render fixtures for all approved states, PT-BR, accessibility, responsive/dark styling, and the 8/9 incident.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Metrics API to React UI | Partial/legacy values can misrepresent availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-30 | Tampering | health calculations | high | mitigate | One tested raw-total helper; no percentage averaging. |
| T-33-31 | Information Disclosure | UI rendering | low | accept | API contains aggregate counts only and React renders fixed labels. |
| T-33-32 | Denial of Service | query recovery | low | accept | Existing retry:false and explicit refetch avoid retry amplification. |
</threat_model>

<verification>
Run focused Bun tests and typecheck through `podman-admin.sh`; browser-level visual/a11y readback occurs in Plan 33-15 headless.
</verification>

<success_criteria>
- The observed 8/9 case renders 88.89 request success and 100 service availability in helper tests.
- Both panels use raw weighted values and approved state/copy/accessibility contracts.
- No simple-average helper remains in either component.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-11-SUMMARY.md` when done.
</output>
