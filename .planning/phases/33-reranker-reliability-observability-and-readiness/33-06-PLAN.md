---
phase: 33-reranker-reliability-observability-and-readiness
plan: "06"
type: execute
wave: 3
depends_on: ["33-01", "33-05"]
files_modified:
  - middleware/distributor.go
  - controller/relay.go
  - service/quota.go
  - service/text_quota.go
autonomous: true
requirements:
  - PHASE-33-METRICS-OUTCOME-TAXONOMY
  - PHASE-33-ROUTING-AVAILABILITY
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "A recorder is created after normalized model/group resolution and is available before channel selection."
    - "Both no-channel paths finish routing_unavailable before abort; relay success/error paths finish once."
    - "Quota settlement/refund and valid-personal-token behavior remain unchanged while success recording moves to the recorder."
  artifacts:
    - path: "middleware/distributor.go"
      provides: "Pre-relay recorder creation and routing finalization"
    - path: "controller/relay.go"
      provides: "Origin-aware final error classification"
    - path: "service/quota.go"
      provides: "Terminal success hook for non-stream usage settlement"
    - path: "service/text_quota.go"
      provides: "Terminal success hook for text/stream settlement"
  key_links:
    - from: "middleware/distributor.go"
      to: "pkg/perf_metrics.TerminalRecorder"
      via: "Gin context stores a recorder before CacheGetRandomSatisfiedChannel"
      pattern: "NewTerminalRecorder|OutcomeRoutingUnavailable"
    - from: "service/quota.go"
      to: "controller/relay.go"
      via: "success/error converge on one Finish call"
      pattern: "FinishTerminalOutcome"
---

<objective>
Wire exact-once terminal outcomes across pre-relay routing, relay errors, and successful quota settlement.

Purpose: Make routing failures visible and prevent retries or duplicated finish paths from inflating terminal request counts.
Output: Four production call sites using the Plan 33-05 recorder without changing billing/quota decisions.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-01-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-05-SUMMARY.md
@middleware/distributor.go
@controller/relay.go
@service/quota.go
@service/text_quota.go
@scripts/atius-user-quota-guard.sh
@patches/atius-user-quota-unlimited.patch
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Record routing availability before controller selection</name>
  <files>middleware/distributor.go</files>
  <read_first>middleware/distributor.go, middleware/distributor_test.go, pkg/perf_metrics/types.go, service/channel.go</read_first>
  <behavior>
    - Normalized model/group metadata creates one recorder before selection.
    - Selection error and nil channel both finish routing_unavailable exactly once.
    - Client validation before selection is client_validation only when normalized model metadata exists.
  </behavior>
  <action>For PHASE-33-ROUTING-AVAILABILITY, create/store the sanitized TerminalRecorder after model and using-group resolution but before preferred/random channel selection. Finish `OutcomeRoutingUnavailable` immediately before both existing 503 aborts, retaining their external status/error response. Route eligible normalized local validation through `OutcomeClientValidation`; leave exits without safe model metadata unrecorded rather than creating arbitrary-cardinality labels. Do not change selection, affinity, token limits, status codes, or error messages.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./middleware -run 'Test.*(NoChannel|RoutingUnavailable|Terminal)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Both service.CacheGetRandomSatisfiedChannel error and nil-channel branches finish routing_unavailable before abort.
    - Existing client response contract remains unchanged.
    - No arbitrary error text/path becomes a metric dimension.
  </acceptance_criteria>
  <done>Pre-controller routing availability is observable once per request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Converge relay error and success paths on one terminal finish</name>
  <files>controller/relay.go, service/quota.go, service/text_quota.go</files>
  <read_first>controller/relay.go, controller/relay_test.go, service/quota.go, service/text_quota.go, pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go</read_first>
  <behavior>
    - Final error class is selected from origin/type/status rather than status alone.
    - Successful quota settlement finishes success with output/generation totals once.
    - Retry attempts do not finish terminal metrics.
  </behavior>
  <action>Replace the asynchronous mutable RelayInfo failure sample with a synchronous sanitized recorder Finish after the retry loop. Map local validation, governor, routing/get-channel, transport/deadline, completed upstream HTTP, and invalid response origins to the fixed outcomes from Plan 33-05. Update only the two existing success-recording sites in service/quota.go and service/text_quota.go to finish `OutcomeSuccess` with their existing output token totals; preserve PreConsumeBilling, refund, violation fee, subscription-to-wallet fallthrough, and settlement ordering exactly. Retain RecordRelaySample only as an internal compatibility bridge for non-migrated callers.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc '/usr/local/go/bin/go test ./controller ./service -run "Test.*(SingleTerminal|RerankRetry|Quota|Billing)" -count=1 &amp;&amp; bash scripts/atius-user-quota-guard.sh'</automated>
  </verify>
  <acceptance_criteria>
    - No async closure captures mutable RelayInfo for terminal metrics.
    - Fail-first/succeed-second produces one success terminal and attempt accounting remains separate.
    - `scripts/atius-user-quota-guard.sh` passes and quota/billing tests remain green.
  </acceptance_criteria>
  <done>Every classified request finishes one terminal outcome without regressing quota behavior.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Request context wiring for `TerminalRecorder` and `FinishTerminalOutcome` across middleware/controller/service seams.
- Origin-aware terminal classification call sites for routing and relay completion.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Middleware to controller/service | Recorder state crosses multiple lifecycle owners. |
| Relay outcome to billing settlement | Metrics wiring must not change financial authorization/accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-16 | Tampering | terminal lifecycle | high | mitigate | One recorder plus exact-once tests across retries. |
| T-33-17 | Elevation of Privilege | quota path | high | mitigate | No billing logic changes; run quota guard and focused billing tests. |
| T-33-18 | Information Disclosure | async logging | medium | mitigate | Synchronous sanitized metadata; no mutable context/error body capture. |
</threat_model>

<verification>
Run focused middleware/controller/service tests and the fork's quota guard through the CPU wrapper.
</verification>

<success_criteria>
- No-channel requests enter routing availability.
- Terminal count stays one across retry/error/success paths.
- User quota/subscription/wallet behavior is unchanged and guarded.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-06-SUMMARY.md` when done.
</output>
