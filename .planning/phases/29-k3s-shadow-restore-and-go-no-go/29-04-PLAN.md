---
phase: 29-k3s-shadow-restore-and-go-no-go
plan: 04
type: execute
wave: 4
depends_on:
  - 29-03
files_modified:
  - scripts/k3s-router-go-no-go.sh
  - scripts/k3s-router-rollback-check.sh
  - docs/K3S-MIGRATION.md
autonomous: true
requirements:
  - PHASE-22-K3S-PREFLIGHT
  - PHASE-22-STATEFUL-DATA
  - PHASE-22-RUNTIME-PARITY
must_haves:
  truths:
    - "A decisao inicia em no-go e so muda para go quando toda evidencia obrigatoria e atual, consistente e verde."
    - "Cleanup/estabilidade, label, Secret, quota, backup, restore, Retain de todos os PVs, placement, imagem, ClusterIP, smoke e CLIAnything sao verificados em conjunto."
    - "Podman continua ativo/testavel e Apache continua apontando ao runtime publico atual."
    - "O artefato final autoriza a Phase 30 ou registra blockers objetivos, sem tocar trafego publico."
  artifacts:
    - path: "scripts/k3s-router-go-no-go.sh"
      provides: "Agregador fail-closed e artefato formal de decisao"
    - path: "scripts/k3s-router-rollback-check.sh"
      provides: "Prova read-only de rollback Podman e edge intacto"
    - path: "docs/K3S-MIGRATION.md"
      provides: "Runbook executavel da Phase 29 e contrato de entrada da Phase 30"
  key_links:
    - from: "scripts/k3s-router-go-no-go.sh"
      to: "29-01/02/03 evidence"
      via: "checksums, timestamps, identities e resultados obrigatorios"
      pattern: "decision.*go|no-go"
    - from: "scripts/k3s-router-rollback-check.sh"
      to: "127.0.0.1:3000 e Apache"
      via: "smoke Podman local e leitura do upstream publico sem mutacao"
      pattern: "127.0.0.1:3000"
---

<objective>
Integrar todas as provas da Phase 29 e emitir GO ou NO-GO formal, preservando edge publico e rollback Podman.

Purpose: Impedir que a Phase 30 comece com evidencia parcial, stale ou contraditoria.
Output: Gate integrado, prova de rollback e artefato sanitizado de decisao.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-VALIDATION.md
@.planning/debug/phase29-diskpressure-audit.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-03-SUMMARY.md
@scripts/k3s-router-rollback-check.sh
@docs/K3S-MIGRATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar agregador integrado GO/NO-GO</name>
  <files>scripts/k3s-router-go-no-go.sh</files>
  <behavior>
    - Teste 1: default e no-go; qualquer input ausente, stale, checksum divergente ou gate falso mantem no-go e lista a causa.
    - Teste 2: go exige cleanup >=20GiB/alvo >=25%/estabilidade 5min, label exclusivo, Secret keys, cpu.max <=800m, backup fresco, restore validado, todos os PVs Retain, placement srv1, digests concordantes, ClusterIP, smoke completo e CLIAnything k3s.
    - Teste 3: artefato inclui timestamps, commit/digest, checksums, gates e estado Podman/Apache, sem segredos ou payloads sensiveis.
    - Teste 4: o script nunca edita Apache, para/reinicia Podman, altera manifests ou remove recursos.
  </behavior>
  <action>Criar agregador com inputs explicitos para os artefatos 29-01/02/03 e janelas de frescor. Verificar cadeia de checksums/identidades, repetir gates read-only críticos e gerar decisao atomica sanitizada. Exigir ClusterIP e rejeitar qualquer evidencia de NodePort, Ingress, hostPort ou edge alterado. O estado inicial e final em erro deve ser no-go; go somente quando todos os gates passarem no mesmo run. Nao converter no-go bem evidenciado em falha de fase: ele e um resultado valido que bloqueia Phase 30.</action>
  <verify>
    <automated>PHASE29_EXECUTE=1 ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-go-no-go.sh --live --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}" --output "$PHASE29_EVIDENCE_DIR/decision.json" &amp;&amp; jq -e '.decision=="go" or (.decision=="no-go" and (.failed_gates|length&gt;0))' "$PHASE29_EVIDENCE_DIR/decision.json"</automated>
  </verify>
  <done>Matriz de fixtures prova que um unico gate vermelho produz no-go e somente o conjunto integral produz go.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provar rollback Podman e edge publico intactos</name>
  <files>scripts/k3s-router-rollback-check.sh</files>
  <behavior>
    - Teste 1: user unit, pod/containers e endpoint local 127.0.0.1:3000 devem estar saudaveis.
    - Teste 2: Apache deve continuar apontando os paths Go ao backend Podman atual; qualquer target k3s na Phase 29 falha.
    - Teste 3: check e read-only por default e nao imprime env, DSN ou tokens.
  </behavior>
  <action>Endurecer o rollback check como prova read-only: validar unit, containers, limites, health local e estado do CLIAnything Podman; inspecionar a configuracao efetiva do Apache sem editar/recarregar e provar que o trafego publico nao foi retargetado. Capturar somente status e checksums sanitizados. O check deve falhar se Podman nao estiver testavel ou se o edge ja apontar ao k3s.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- scripts/k3s-router-rollback-check.sh --live --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}" &amp;&amp; curl -fsS http://127.0.0.1:3000/api/status &gt;/dev/null &amp;&amp; sudo -n apache2ctl -t -D DUMP_RUN_CFG &gt;/dev/null</automated>
  </verify>
  <done>Rollback Podman e edge atual possuem prova automatizada, read-only e consumivel pelo GO/NO-GO.</done>
</task>

<task type="auto">
  <name>Task 3: Documentar execucao e emitir decisao formal</name>
  <files>docs/K3S-MIGRATION.md</files>
  <action>Atualizar o runbook somente com comandos implementados, ordem 29-01 a 29-04, inputs Vault por nomes/path sem valores, contencao de CPU menor ou igual a 20%, paths de evidencia e interpretacao de go/no-go. Registrar ClusterIP como contrato obrigatorio, explicando que a auditoria live superou a recomendacao antiga de NodePort. Executar os checks integrados e anexar ao SUMMARY a decisao sanitizada; se no-go, listar blockers e manter Phase 30 bloqueada. Nao incluir procedimento de cutover, aposentadoria Podman ou mutacao do edge nesta fase.</action>
  <verify>
    <automated>test -s "${PHASE29_EVIDENCE_DIR:?required}/decision.json" &amp;&amp; ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-go-no-go.sh --verify-existing "$PHASE29_EVIDENCE_DIR/decision.json" &amp;&amp; sudo -n k3s kubectl -n router-ai-atius get pods,svc,pvc -o wide</automated>
  </verify>
  <done>Runbook reproduz o fluxo real e o SUMMARY registra go autorizando Phase 30 ou no-go com blockers, sempre com trafego publico intacto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidencias de planos → decision gate | Artefatos podem estar stale, incompletos ou adulterados. |
| k3s shadow → criterio de Phase 30 | Um go incorreto autorizaria mutacao futura do edge publico. |
| Podman/Apache live → rollback evidence | Estado operacional e config host-owned sao lidos sem mutacao. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-13 | Tampering | evidencias GO/NO-GO | high | mitigate | checksums, frescor, identidades cruzadas e output atomico |
| T-29-14 | Repudiation | decisao operacional | medium | mitigate | timestamp UTC, commit/digest, gates e blockers explicitos |
| T-29-15 | Elevation of Privilege | autorizacao Phase 30 | high | mitigate | default no-go e go somente com todos os requisitos verdes |
| T-29-16 | Denial of Service | rollback Podman | high | mitigate | prova read-only de unit, endpoint e Apache antes da decisao |
| T-29-SC | Tampering | cadeia de artefatos | high | mitigate | nenhum install; hashes e identidades imutaveis de imagens/evidencias |
</threat_model>

<verification>
Executar self-tests dos dois gates, verificacao documental e um run integrado live. Conferir que o artefato final nao contem Secrets e que git/runtime evidence prova zero alteracao em Apache e Podman.
</verification>

<success_criteria>
Uma unica decisao formal e reproduzivel resume todos os gates; go libera somente a Phase 30, no-go lista blockers acionaveis, e ambos preservam trafego publico e rollback.
</success_criteria>

<output>
Create `.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-04-SUMMARY.md` when done
</output>
