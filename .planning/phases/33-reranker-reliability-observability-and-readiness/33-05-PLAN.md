---
phase: 33-reranker-reliability-observability-and-readiness
plan: "05"
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - pkg/perf_metrics/types.go
  - pkg/perf_metrics/metrics.go
  - pkg/perf_metrics/flush.go
  - model/perf_metric.go
  - controller/perf_metrics.go
autonomous: true
requirements:
  - PHASE-33-METRICS-OUTCOME-TAXONOMY
  - PHASE-33-WEIGHTED-DASHBOARD
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Every new terminal request contributes exactly one fixed outcome count and request_count remains the legacy terminal denominator."
    - "Persistence is additive and cross-DB; mixed historical windows are marked partial instead of backfilled or treated as zero."
    - "The summary API keeps legacy model fields while adding schema_version=2, raw model totals, and an overall weighted aggregate."
  artifacts:
    - path: "pkg/perf_metrics/types.go"
      provides: "Outcome, TerminalRecorder, raw counters, completeness and API shapes"
    - path: "model/perf_metric.go"
      provides: "Additive columns/upsert/SUM queries"
    - path: "controller/perf_metrics.go"
      provides: "Backward-compatible schema v2 response envelope"
  key_links:
    - from: "pkg/perf_metrics/flush.go"
      to: "model/perf_metric.go"
      via: "all counters drain, restore on failure, and upsert together"
      pattern: "UpsertPerfMetric"
    - from: "controller/perf_metrics.go"
      to: "pkg/perf_metrics/metrics.go"
      via: "QuerySummaryAll returns weighted overall plus models"
      pattern: "QuerySummaryAll"
---

<objective>
Implement the additive terminal-outcome metrics contract from hot bucket through Redis, GORM, and the summary API.

Purpose: Make reranker health mathematically correct without breaking existing consumers or fabricating historical classifications.
Output: Seven-outcome exact-once primitives, cross-DB persistence, weighted aggregates, and additive API schema v2.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-01-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@pkg/perf_metrics/types.go
@pkg/perf_metrics/metrics.go
@pkg/perf_metrics/flush.go
@model/perf_metric.go
@controller/perf_metrics.go

<interfaces>
Preserve `RecordRelaySample(*RelayInfo, bool, int64)` as a compatibility bridge. Add `type Outcome uint8`, `type OutcomeOrigin uint8`, `type TerminalRecorder`, `NewTerminalRecorder(model, group string, startedAt time.Time)`, `(*TerminalRecorder).Finish(outcome Outcome, latency time.Duration, outputTokens, generationMs int64) bool`, and schema-v2 raw totals consumed by the existing `/api/perf-metrics/summary` envelope.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the terminal taxonomy and complete hot/Redis/flush lifecycle</name>
  <files>pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go, pkg/perf_metrics/flush.go</files>
  <read_first>pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go, pkg/perf_metrics/flush.go, service/embeddinggovernor/governor.go, pkg/perf_metrics/metrics_test.go</read_first>
  <behavior>
    - Finish is idempotent and records one of seven outcomes.
    - request_count equals the sum of outcome counters for fully classified new data.
    - Redis, drain, restore-on-error, and merge include every new counter and latency_sample_count.
  </behavior>
  <action>Implement PHASE-33-METRICS-OUTCOME-TAXONOMY with fixed constants `OutcomeSuccess`, `OutcomeClientValidation`, `OutcomeGovernorRejection`, `OutcomeRoutingUnavailable`, `OutcomeTransportTimeout`, `OutcomeUpstreamHTTPFailure`, and `OutcomeInvalidUpstreamResponse`. Add origin-aware pure classification and a request-scoped TerminalRecorder using sync.Once/CAS, copying only model/group/start metadata. Extend Sample, counters, atomicBucket, Redis keys, merge/drain/restore, and RecordRelaySample compatibility so request_count remains terminal_count, success_count remains legacy success, and latency_sample_count increments only when latency is measured. Never retain `*gin.Context`, error text, URL, token, query, document, prompt, or provider body.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./pkg/perf_metrics -run 'Test.*(Outcome|Terminal|Weighted|Partial|Redis)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All seven constants and TerminalRecorder APIs exist.
    - Every counter participates in snapshot, drain, addCounters, Redis write/read, and model merge.
    - Double Finish and flush-restore tests pass.
  </acceptance_criteria>
  <done>The in-memory/Redis taxonomy is complete, idempotent, and privacy-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add cross-DB columns, additive upsert/SUM, and historical completeness</name>
  <files>model/perf_metric.go</files>
  <read_first>model/perf_metric.go, model/main.go, model/perf_metric_test.go, AGENTS.md</read_first>
  <behavior>
    - AutoMigrate adds six failure counters, latency_sample_count, and outcome_schema_version without destructive DDL.
    - Upsert increments all counters and preserves writer schema metadata.
    - Every contributing bucket must carry outcome_schema_version=2 and satisfy the seven-outcome sum invariant; legacy-all-success and mixed windows remain incomplete.
  </behavior>
  <action>Extend PerfMetric, PerfMetricSummary, and PerfMetricSummaryBucket with integer default-zero columns `client_validation_count`, `governor_rejection_count`, `routing_unavailable_count`, `transport_timeout_count`, `upstream_http_failure_count`, `invalid_upstream_response_count`, `latency_sample_count`, and `outcome_schema_version`. Use existing GORM AutoMigrate registration, clause.OnConflict, gorm.Expr, and commonGroupCol only. Every new bucket write/upsert sets and preserves writer marker `outcome_schema_version=2`. Compute completeness from per-bucket evidence before aggregation: every contributing bucket must have marker 2 and its seven counters must sum to its request_count. A legacy-all-success bucket, mixed legacy/v2 window, missing marker, or mismatched v2 bucket is partial even if aggregate sums happen to balance. Exercise real disposable MySQL/PostgreSQL integration when the availability probe finds an existing service profile/DSN; otherwise retain strict GORM-only DryRun and record `CROSS_DB_RESIDUAL_RISK=DryRun-only:no-disposable-profile` as the accepted residual risk. Do not backfill legacy rows, pull database images, remove columns, use JSON/JSONB, or add database-specific operators.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./model -run 'Test.*(PerfMetric|Dialect|Partial)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - SQLite AutoMigrate/upsert/query tests pass.
    - Disposable MySQL/PostgreSQL migration/upsert/SUM tests run when available; otherwise strict DryRun and the explicit accepted residual-risk marker pass.
    - Legacy-all-success, mixed legacy/v2, mismatched-v2, and complete-v2 regressions prove per-bucket marker plus sum semantics.
  </acceptance_criteria>
  <done>Persistence is additive, backward-compatible, and cross-DB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Return additive schema-v2 weighted summary without breaking legacy fields</name>
  <files>pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go, controller/perf_metrics.go</files>
  <read_first>pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go, controller/perf_metrics.go, web/default/src/features/performance-metrics/types.ts, .planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md</read_first>
  <behavior>
    - Existing success_rate, avg_latency_ms, avg_tps, recent_success_rates, and models remain.
    - schema_version=2 and complete/raw totals exist at overall and per-model levels.
    - Ratios divide summed raw totals once and zero denominators remain unavailable rather than fabricated.
  </behavior>
  <action>Extend SummaryAllResult additively with `schema_version: 2`, `overall`, and per-model raw totals for PHASE-33-WEIGHTED-DASHBOARD. Compute request success from success_count/request_count, service availability from success plus routing/transport/upstream/invalid-response counts, average latency from total_latency_ms/latency_sample_count, and throughput from output_tokens/generation_ms. Preserve the current `{success:true,data:...}` controller envelope and hours/group filtering. Expose aggregate numbers only and keep legacy model calculations for compatible consumers.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- /usr/local/go/bin/go test ./pkg/perf_metrics ./model ./controller -run 'Test.*(Outcome|PerfMetric|Weighted|Summary|Partial)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - API JSON remains additive and legacy fields are unchanged.
    - Weighted, 8/9, routing, latency denominator, and partial-history tests pass.
    - No sensitive or unbounded-cardinality field enters persistence/API.
  </acceptance_criteria>
  <done>The backend contract can drive both dashboard surfaces without simple averages.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Go symbols `Outcome`, seven `Outcome*` constants, `OutcomeOrigin`, `TerminalRecorder`, `NewTerminalRecorder`, and `Finish`.
- PerfMetric fields for six failure counters, `LatencySampleCount`, and `OutcomeSchemaVersion`.
- Schema-v2 `overall` and per-model raw-total response shapes with `complete`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Concurrent request paths to metrics stores | Multiple exits can race to finalize one request. |
| Redis/GORM to dashboard API | Partial or tampered counters can produce false health. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-13 | Tampering | terminal recorder | high | mitigate | sync.Once/CAS plus persisted sum invariant. |
| T-33-14 | Repudiation | historical metrics | medium | mitigate | Explicit completeness; no inferred backfill. |
| T-33-15 | Information Disclosure | metrics schema | high | mitigate | Fixed counters and normalized model/group only; no error/body dimensions. |
</threat_model>

<verification>
Run the full focused backend command from 33-VALIDATION.md through `scripts/podman-admin.sh`; verify schema migration on a disposable fixture before any live DB use.
</verification>

<success_criteria>
- Plan 33-01 tests are green.
- API output is additive, weighted, and partial-aware.
- SQLite/MySQL/PostgreSQL compatibility gates pass at their planned depth.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-05-SUMMARY.md` when done.
</output>
