---
phase: 33-reranker-reliability-observability-and-readiness
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/default/src/features/performance-metrics/lib/outcome-summary.test.ts
  - tests/test_clianything.py
autonomous: true
requirements:
  - PHASE-33-WEIGHTED-DASHBOARD
  - PHASE-33-RERANK-BATCH-CONTRACT
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Frontend tests prove weighted values differ from a simple mean and preserve zero versus missing semantics."
    - "The 8/9 client-validation case renders 88.89% request success and 100% service availability."
    - "Smoke contract tests require sanitized candidate/local/public readiness and rerank evidence."
  artifacts:
    - path: "web/default/src/features/performance-metrics/lib/outcome-summary.test.ts"
      provides: "Pure weighted/partial calculation contract"
    - path: "tests/test_clianything.py"
      provides: "Sanitized reranker smoke script contract"
  key_links:
    - from: "outcome-summary.test.ts"
      to: "web/default/src/features/performance-metrics/types.ts"
      via: "schema v2 raw counters and complete/partial state"
      pattern: "terminal_count|serviceAvailability|partial"
    - from: "tests/test_clianything.py"
      to: "scripts/smoke-reranker-readiness.py"
      via: "environment-only auth and redacted output"
      pattern: "smoke-reranker-readiness"
---

<objective>
Create Wave 0 frontend aggregation and Router smoke-contract tests.

Purpose: Prevent a UI-only arithmetic patch and ensure live evidence tooling is privacy-safe before implementation.
Output: One Bun pure-unit test and focused Python contract cases.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-VALIDATION.md
@web/default/src/features/performance-metrics/types.ts
@web/default/src/features/performance-metrics/lib/format.ts
@web/default/src/features/dashboard/components/overview/performance-health-panel.tsx
@web/default/src/features/dashboard/components/models/performance-overview.tsx
@scripts/smoke-embeddings.py
@tests/test_clianything.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock weighted, zero, partial, and legacy frontend semantics</name>
  <files>web/default/src/features/performance-metrics/lib/outcome-summary.test.ts</files>
  <read_first>web/default/src/features/performance-metrics/types.ts, web/default/src/features/performance-metrics/lib/format.ts, web/default/src/features/dashboard/components/overview/performance-health-panel.tsx, web/default/src/features/dashboard/components/models/performance-overview.tsx, .planning/phases/33-reranker-reliability-observability-and-readiness/33-UI-SPEC.md</read_first>
  <behavior>
    - Raw totals are summed before one division for request success, service availability, latency, and throughput.
    - 8 success plus 1 client validation yields 88.89 request success and 100 service availability.
    - Missing fields produce partial/null; a zero denominator produces null; a complete zero class with terminal_count greater than zero produces 0%.
    - Legacy per-model success remains displayable but cannot enter a weighted overall KPI.
  </behavior>
  <action>Create Bun/node:test cases for PHASE-33-WEIGHTED-DASHBOARD using the approved formulas and fixed seven-outcome order from 33-UI-SPEC.md. Use uneven traffic volumes, the observed 8/9 case, one routing failure, latency_sample_count distinct from terminal_count, missing v2 fields, a legacy-only model, and assertions excluding NaN/Infinity. Target a new pure helper module `outcome-summary.ts`; do not render React or add a test package.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'cd web/default; set +e; bun test src/features/performance-metrics/lib/outcome-summary.test.ts &gt;/tmp/phase33-w0-ui.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "Cannot find module.*outcome-summary|Module not found.*outcome-summary" /tmp/phase33-w0-ui.log'</automated>
  </verify>
  <acceptance_criteria>
    - The test names explicitly cover weighted, 8/9, routing failure, zero denominator, complete zero class, partial, legacy, and latency denominator cases.
    - All expected percentages use numeric comparisons before locale formatting.
    - The governed test passes after helper implementation or fails only because `outcome-summary.ts` is absent.
  </acceptance_criteria>
  <done>The frontend calculation contract is fixed before Plans 33-10/33-11.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock sanitized reranker readiness/smoke behavior</name>
  <files>tests/test_clianything.py</files>
  <read_first>tests/test_clianything.py, scripts/smoke-embeddings.py, .planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md</read_first>
  <behavior>
    - Missing runtime tokens exits before network with a documented non-zero status.
    - Candidate, local, and public targets run readiness, one synthetic request of at most 20 documents, and a deterministic 21-document rejection.
    - Retained output contains status/count/channel/model/digest/elapsed only and redacts Authorization, tokens, query, documents, request body, and provider body.
  </behavior>
  <action>Add focused source/behavior tests for the target `scripts/smoke-reranker-readiness.py` under PHASE-33-RERANK-BATCH-CONTRACT and PHASE-33-TEST-DOC-LIVE-EVIDENCE. Require environment variables for base URLs and Vault-hydrated credentials, bounded HTTP timeouts, channel 10 plus `reranker-gte-multilingual-v1`, a synthetic non-sensitive fixture, stable client index examples, and a no-token pre-network exit. Do not encode secrets or production documents.</action>
  <verify>
    <automated>set +e; python3 -m unittest tests.test_clianything.Phase19ProviderRoutingTests.test_smoke_reranker_readiness_contract -v &gt;/tmp/phase33-w0-smoke.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "smoke-reranker-readiness.py.*(missing|No such file)" /tmp/phase33-w0-smoke.log</automated>
  </verify>
  <acceptance_criteria>
    - The test rejects scripts that print auth headers, token values, query text, documents, or response bodies.
    - The test requires candidate/local/public modes, readiness, valid requests of up to 20 documents, and an invalid 21-document case.
    - RED state is attributable only to the missing smoke implementation.
  </acceptance_criteria>
  <done>Smoke safety and coverage are locked before Plan 33-12 creates the script.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `outcome-summary.test.ts`: new pure-unit contract for weighted and partial calculations.
- `test_smoke_reranker_readiness_contract`: new Python contract test for sanitized live evidence tooling.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Metrics API to browser | Partial/legacy server data drives health claims. |
| Runtime credentials to smoke script | Secrets authorize probes but must never enter evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-07 | Tampering | weighted helper | high | mitigate | Uneven-volume and partial fixtures block fabricated health. |
| T-33-08 | Information Disclosure | smoke output | high | mitigate | Contract tests reject secret/body fields and require environment-only auth. |
| T-33-09 | Spoofing | target selection | medium | mitigate | Explicit candidate/local/public base modes and authenticated readiness. |
</threat_model>

<verification>
Run the governed Bun test and focused Python unittest; accept RED only for the two implementation files intentionally absent in Wave 0.
</verification>

<success_criteria>
- Weighted and evidence behavior is test-defined before implementation.
- No package installation or locale write occurs.
- All assigned requirement IDs are represented in task actions.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-03-SUMMARY.md` when done.
</output>
