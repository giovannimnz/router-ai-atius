---
phase: 25-embedding-governor-auto-workload-inference
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - service/embeddinggovernor/governor.go
  - service/embeddinggovernor/governor_test.go
autonomous: true
requirements:
  - PHASE-25-GOVERNED-MODEL-SCOPE
  - PHASE-25-AUTO-WORKLOAD-INFERENCE
  - PHASE-25-HEADER-OVERRIDE-COMPATIBILITY
  - PHASE-25-TEI-BATCH-SAFETY
  - PHASE-25-CLIENT-SMOKE-VALIDATION
must_haves:
  truths:
    - "D-01: embedding-gte-v1 is the default governed model and unknown models still no-op."
    - "D-02: explicit X-Embedding-Workload values keep priority over automatic inference."
    - "D-03: unlabeled arrays with at least 2 text items classify as batch through metadata only."
    - "D-04: daily automatic concurrency remains min=1, initial=2, max=3, batch_concurrency=1."
    - "D-05: service tests cover model scope, classifier outcomes, overrides, and metadata privacy."
  artifacts:
    - "service/embeddinggovernor/governor.go exposes governed-model and workload-classifier helpers."
    - "service/embeddinggovernor/governor_test.go covers Phase 25 service contracts."
  key_links:
    - "LoadConfigFromEnv -> normalizeConfig -> New -> Acquire uses the same classifier contract."
    - "relay/embedding_handler.go can depend on embeddinggovernor.IsGovernedModel after this plan."
---

<objective>
Implement the service-level governor contract for Phase 25.

Purpose: Make the governed model scope and workload classifier explicit, testable, and safe before relay wiring depends on it.
Output: `EMBEDDING_GOVERNOR_AUTO_WORKLOAD`, threshold `2`, governed-model helpers, classifier helpers, and service tests for D-01 through D-05.
</objective>

## Artifacts this phase produces

- `service/embeddinggovernor.Config.AutoWorkload bool`
- `EMBEDDING_GOVERNOR_AUTO_WORKLOAD=true`
- `defaultBatchInputCountThreshold = 2`
- `EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD=2`
- `(*Governor).IsGovernedModel(model string) bool`
- `embeddinggovernor.IsGovernedModel(model string) bool`
- `(*Governor).ClassifyWorkload(req Request) string` returning `batch` or `interactive`
- Focused service tests in `service/embeddinggovernor/governor_test.go`

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-RESEARCH.md
@.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
@.planning/phases/phase-20-go-native-model-router/20-03-SUMMARY.md
@.planning/phases/phase-20-go-native-model-router/20-04-SUMMARY.md
@service/embeddinggovernor/governor.go
@service/embeddinggovernor/governor_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit governor scope and classifier contract</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    @service/embeddinggovernor/governor.go
    @service/embeddinggovernor/governor_test.go
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-CONTEXT.md
    @.planning/phases/25-embedding-governor-auto-workload-inference/25-PATTERNS.md
  </read_first>
  <behavior>
    - `embedding-gte-v1` is governed by default and `embedding-gte-v1-batch` is not in `BatchModels` per D-01.
    - Unknown models return no lease and no reject from `Acquire` per D-01.
    - Header `batch` and `bulk` classify as batch; `interactive` and `realtime` classify as interactive per D-02.
    - Invalid or absent header falls through to automatic metadata inference when `AutoWorkload` is enabled per D-02 and D-03.
    - No-header single input with count `1` stays interactive; no-header input count `2` becomes batch per D-03.
    - Character threshold still marks a single large input as batch without storing raw input text per D-03.
  </behavior>
  <action>Add `AutoWorkload bool` to `Config`, load it from `EMBEDDING_GOVERNOR_AUTO_WORKLOAD` with default `true`, and set the Phase 25 default `defaultBatchInputCountThreshold` to `2` per D-03. Add `(*Governor).IsGovernedModel(model string) bool` as a public wrapper for existing `applies`, plus package-level `IsGovernedModel(model string) bool` that uses the global governor so relay code can check the same scope per D-01. Add `(*Governor).ClassifyWorkload(req Request) string` returning exactly `batch` or `interactive`, keeping the current priority order: valid explicit header, automatic `InputCount` and `InputChars` thresholds when `AutoWorkload` is true, then `BatchModels`. Keep private `isBatch(req Request) bool` as a wrapper around `ClassifyWorkload` so existing queue/accounting code remains localized. Do not add raw embedding input, request bodies, Authorization headers, tokens, or channel secrets to `Request`, `Snapshot`, tests, or log strings.</action>
  <acceptance_criteria>`LoadConfigFromEnv()` returns `AutoWorkload=true`, `BatchInputCountThreshold=2`, `Models["embedding-gte-v1"]=true`, `BatchModels` empty, and `Models["embedding-gte-v1-batch"]=false`; classifier tests prove D-01, D-02, and D-03 without raw text leakage.</acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestAcquireNoopsForNonGovernedModel|TestLoadConfigUsesDailySafeDefaults|TestWorkloadMetadataClassifiesUnlabeledLargeInputsAsBatch|TestWorkloadHeaderOverridesMetadataClassification|TestLoadConfigNormalizesWorkloadMetadataThresholds)$' -count=1</automated>
  </verify>
  <done>Governor scope and classifier behavior are explicit, exported where relay needs them, and covered by deterministic service tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve safe defaults, split metrics, and metadata privacy</name>
  <files>service/embeddinggovernor/governor.go, service/embeddinggovernor/governor_test.go</files>
  <read_first>
    @service/embeddinggovernor/governor.go
    @service/embeddinggovernor/governor_test.go
    @.planning/phases/phase-20-go-native-model-router/20-03-SUMMARY.md
  </read_first>
  <behavior>
    - Automatic governor bounds stay `MinConcurrency=1`, `InitialConcurrency=2`, `MaxConcurrency=3`, `BatchConcurrency=1` per D-04.
    - `AutoWorkload=false` disables metadata thresholds but does not disable valid header overrides per D-02.
    - Batch and interactive EWMA/counters remain separate after classifier refactoring per D-04.
    - Snapshot JSON remains aggregate-only and excludes `input_count`, `input_chars`, `workload`, `channel_name`, `authorization`, `token`, and `secret` per D-03 and D-05.
  </behavior>
  <action>Update `testConfig()` to set `AutoWorkload=true` so service tests match production defaults. Add or update tests for `EMBEDDING_GOVERNOR_AUTO_WORKLOAD=false` using `LoadConfigFromEnv()` and `New(testConfigWith(...))`: valid headers still override, absent/invalid headers do not use count/chars thresholds, and configured `BatchModels` remains the final fallback. Re-run existing split-feedback and snapshot privacy tests after changing `isBatch` so D-04 accounting and D-05 privacy do not regress. Keep `common.Marshal` in tests for snapshot JSON and keep Testify `require`/`assert` style.</action>
  <acceptance_criteria>Safe concurrency defaults are unchanged, auto-workload can be disabled deliberately, and aggregate snapshot privacy remains intact.</acceptance_criteria>
  <verify>
    <automated>/usr/local/go/bin/go test ./service/embeddinggovernor -run '^(TestLoadConfigUsesDailySafeDefaults|TestWorkloadHeaderOverridesMetadataClassification|TestSplitLatencyMetricsTrackInteractiveAndBatchSeparately|TestBatchLatencyDoesNotBlockInteractiveScaleUp|TestSnapshotContainsOnlyAggregateEmbeddingGovernorMetadata)$' -count=1</automated>
  </verify>
  <done>Governor refactor keeps the Phase 20 safety envelope while changing the Phase 25 workload threshold and config surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> governor metadata | Untrusted model name, workload header, input count, and character count affect queue class. |
| environment -> governor config | Runtime env vars control model scope, thresholds, concurrency, and health behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | `(*Governor).ClassifyWorkload` | medium | mitigate | Keep explicit valid header mapping deterministic and test invalid headers falling through to metadata inference per D-02. |
| T-25-02 | Denial of Service | `Config.BatchInputCountThreshold` | medium | mitigate | Normalize invalid or missing `EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD` to `2` and preserve `MaxConcurrency=3` plus `BatchConcurrency=1` per D-03 and D-04. |
| T-25-03 | Information Disclosure | `Request`, `Snapshot`, service tests | high | mitigate | Keep governor request/snapshot metadata-only and run snapshot privacy tests that reject raw input, workload details, channel names, authorization, token, and secret strings per D-03 and D-05. |
| T-25-SC | Tampering | package installs | low | accept | No npm, pip, or cargo install is part of this plan; if an install is introduced, stop for package legitimacy audit and human verification. |
</threat_model>

<verification>
Run the task commands, then run the package gate:

<automated>/usr/local/go/bin/go test ./service/embeddinggovernor -count=1</automated>

Graphify was fresh before planning; after code changes, executor must run:

<automated>node "$HOME/.codex/gsd-core/bin/gsd-tools.cjs" graphify status</automated>
</verification>

<success_criteria>
- D-01 through D-05 service-level behavior is implemented.
- `EMBEDDING_GOVERNOR_AUTO_WORKLOAD` exists and defaults to enabled.
- `EMBEDDING_GOVERNOR_BATCH_INPUT_COUNT_THRESHOLD` default and normalization target are `2`.
- No public batch alias or raw request telemetry is added.
</success_criteria>

<output>
Create `.planning/phases/25-embedding-governor-auto-workload-inference/25-01-SUMMARY.md` when done.
</output>
