---
phase: 21-feat-pt-native-pr
plan: "05"
type: execute
wave: 4
depends_on:
  - 21-02
  - 21-03
  - 21-04
files_modified:
  - .planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md
autonomous: true
requirements:
  - PHASE-21-LOCAL-FIRST-VALIDATION
  - PHASE-21-UPSTREAM-PR-HYGIENE
must_haves:
  truths:
    - Final implementation branch must be one clean upstream-ready commit if promoted.
    - PR duplicate search covers both issues and pull requests.
    - Future PR body must use `.github/PULL_REQUEST_TEMPLATE.md` and disclose AI assistance when required.
    - Leak checks must fail when forbidden fork/planning/runtime/secrets text appears in code diff or PR text.
  artifacts:
    - `.planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md`
  key_links:
    - local validation evidence -> single commit -> PR template body
    - duplicate search (#2924/#5801/#5238/#5245) -> replacement comment/new PR context
---

<objective>
Package the locally validated PT-BR implementation for human review and possible upstream PR submission without leaking fork-only Atius/runtime/planning content.
</objective>

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-17/D-18/D-21/D-22 | Upstream PR hygiene | 21-05 | COVERED | Template, disclosure, and leak gates. |
| CONTEXT | D-19/D-20 | Duplicate search and contaminated history | 21-05 | COVERED | Checks PR #5801, issue #2924, and closed PRs #5238/#5245 before PR. |
| REQUIREMENTS | PHASE-21-UPSTREAM-PR-HYGIENE | No fork-only PR content | 21-05 | COVERED | Failing leak check over diff and PR text. |
| REQUIREMENTS | PHASE-21-LOCAL-FIRST-VALIDATION | Local evidence before PR | 21-05 | COVERED | Handoff collects test/build evidence. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-feat-pt-native-pr/21-02-SUMMARY.md
@.planning/phases/21-feat-pt-native-pr/21-03-SUMMARY.md
@.planning/phases/21-feat-pt-native-pr/21-04-SUMMARY.md
@.github/PULL_REQUEST_TEMPLATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify final implementation diff and single-commit scope</name>
  <files>implementation branch only</files>
  <action>
    Confirm the final implementation diff contains only native language files, wiring, and narrowly justified validation tests/checks. Squash or prepare a single clean commit only after validation passes and Giovanni approves promotion.
  </action>
  <acceptance_criteria>
    - `git diff --name-status upstream/main...HEAD` contains no `.planning/`, Graphify, Obsidian, Podman, provider/router/governor, DB/catalog, runtime docs, secret, or fork-only files.
    - A path allowlist fails on any diff outside native i18n locale/wiring files and narrowly justified validation tests/checks.
    - Commit message is upstream-friendly, e.g. `feat: add Brazilian Portuguese localization`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-status upstream/main...HEAD</automated>
    <automated>node --input-type=module -e "import { execFileSync } from 'node:child_process'; const out=execFileSync('git',['diff','--name-only','upstream/main...HEAD'],{encoding:'utf8'}).trim(); const paths=out?out.split('\\n'):[]; const allow=[/^i18n\\/(i18n\\.go|i18n_test\\.go|locales\\/pt\\.yaml)$/, /^web\\/default\\/scripts\\/sync-i18n\\.mjs$/, /^web\\/default\\/src\\/i18n\\/(config\\.ts|languages\\.ts|locales\\/pt\\.json|locales\\/_reports\\/_sync-report\\.json)$/, /^web\\/default\\/src\\/i18n\\/__tests__\\/[A-Za-z0-9_.-]+\\.test\\.tsx?$/, /^web\\/classic\\/src\\/i18n\\/(i18n\\.js|language\\.js|locales\\/pt\\.json)$/, /^web\\/classic\\/src\\/components\\/layout\\/headerbar\\/LanguageSelector\\.jsx$/, /^web\\/classic\\/src\\/components\\/settings\\/personal\\/cards\\/PreferencesSettings\\.jsx$/]; const bad=paths.filter(p=>!allow.some(r=>r.test(p))); if(bad.length){console.error('unexpected upstream PR paths:', bad); process.exit(1)}"</automated>
    <automated>git diff --check upstream/main...HEAD</automated>
  </verify>
  <done>Implementation branch is small enough for upstream review.</done>
</task>

<task type="auto">
  <name>Task 2: Run failing leak checks over code diff and PR text</name>
  <files>implementation branch and draft text</files>
  <action>
    Use leak checks that return non-zero when forbidden content is found. Apply the same check to the code diff and to any replacement-comment/PR-body draft before posting.
  </action>
  <acceptance_criteria>
    - Leak check fails if forbidden text appears.
    - Forbidden terms include `.planning`, Graphify, Obsidian, Podman, governor, runtime DB names, `/home/ubuntu`, `Atius`, bearer/auth secrets, and fork-only router wording.
    - Protected upstream identity is not removed or renamed.
  </acceptance_criteria>
  <verify>
    <automated>if git diff upstream/main...HEAD -- . | rg -n "Atius|router-ai-atius|\\.planning|Graphify|Obsidian|Podman|governor|DBRouterAiAtius|Bearer|auth\\.json|/home/ubuntu"; then echo "forbidden fork/planning/runtime text found in code diff" >&2; exit 1; fi</automated>
    <automated>for f in .planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md /tmp/new-api-pt-pr-body.md /tmp/new-api-pt-pr-comment.md; do test -e "$f" || continue; if rg -n "Atius|router-ai-atius|\\.planning|Graphify|Obsidian|Podman|governor|DBRouterAiAtius|Bearer|auth\\.json|/home/ubuntu" "$f"; then echo "forbidden fork/planning/runtime text found in PR text: $f" >&2; exit 1; fi; done</automated>
  </verify>
  <done>Leak checks are real gates, not informational greps.</done>
</task>

<task type="auto">
  <name>Task 3: Re-check upstream duplicates and related history</name>
  <files>.planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md</files>
  <action>
    Search upstream PRs and issues for Portuguese/PT-BR work immediately before PR prep. Record issue #2924, PR #5801, and the polluted closed PRs #5238/#5245. If #5801 has changed scope or merged, stop and re-evaluate instead of opening a duplicate.
  </action>
  <acceptance_criteria>
    - `gh issue view 2924` result is recorded.
    - `gh pr view 5801` current file scope is recorded.
    - `gh search issues` and `gh search prs` results are recorded.
    - Replacement comment for #5245 is prepared only if still useful.
  </acceptance_criteria>
  <verify>
    <automated>gh issue view 2924 --repo QuantumNous/new-api --json number,title,state,url,updatedAt</automated>
    <automated>gh pr view 5801 --repo QuantumNous/new-api --json number,title,state,files,url,updatedAt</automated>
    <automated>gh search issues --repo QuantumNous/new-api "Portuguese translation" --json number,title,state,url --limit 20</automated>
    <automated>gh search prs --repo QuantumNous/new-api "Portuguese translation" --json number,title,state,url --limit 20</automated>
  </verify>
  <done>PR preparation accounts for active upstream duplicate risk.</done>
</task>

<task type="auto">
  <name>Task 4: Prepare upstream handoff draft from template</name>
  <files>.planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md, /tmp/new-api-pt-pr-body.md, /tmp/new-api-pt-pr-comment.md</files>
  <action>
    Draft the replacement comment and new PR body using `.github/PULL_REQUEST_TEMPLATE.md`. Keep the PR text human-readable, scoped to native PT-BR support, and free of fork-only references. Include `This contribution was AI-assisted and human-reviewed.` because local git identity is not a recurring upstream core author.
  </action>
  <acceptance_criteria>
    - PR body preserves the upstream template structure.
    - PR body lists local validation evidence from plans 21-02 through 21-04.
    - PR body references related issue/PRs without leaking fork-specific details.
    - Replacement comment for #5245 points maintainers to a clean replacement PR only after a replacement PR URL exists.
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/PULL_REQUEST_TEMPLATE.md</automated>
    <automated>test -f .planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md</automated>
    <automated>rg -n "AI-assisted|human-reviewed|Portuguese|Português|#2924|#5801|go test ./i18n|bun run i18n:sync|bun run build" .planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md</automated>
  </verify>
  <done>Giovanni has a clean upstream handoff draft after local approval.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Leak grep passes even when it finds forbidden content | high | Use `if rg ...; then exit 1; fi` checks. |
| PR duplicates active upstream work | medium | Re-check issue #2924 and PR #5801 immediately before PR. |
| PR body ignores upstream template | medium | Handoff uses `.github/PULL_REQUEST_TEMPLATE.md` verbatim as structure. |
| Local GSD artifacts included in upstream branch | high | Handoff artifacts live outside implementation branch and are excluded by diff/leak gates. |
</threat_model>

<verification>
<automated>git diff --check upstream/main...HEAD</automated>
<automated>node --input-type=module -e "import { execFileSync } from 'node:child_process'; const out=execFileSync('git',['diff','--name-only','upstream/main...HEAD'],{encoding:'utf8'}).trim(); const paths=out?out.split('\\n'):[]; const allow=[/^i18n\\/(i18n\\.go|i18n_test\\.go|locales\\/pt\\.yaml)$/, /^web\\/default\\/scripts\\/sync-i18n\\.mjs$/, /^web\\/default\\/src\\/i18n\\/(config\\.ts|languages\\.ts|locales\\/pt\\.json|locales\\/_reports\\/_sync-report\\.json)$/, /^web\\/default\\/src\\/i18n\\/__tests__\\/[A-Za-z0-9_.-]+\\.test\\.tsx?$/, /^web\\/classic\\/src\\/i18n\\/(i18n\\.js|language\\.js|locales\\/pt\\.json)$/, /^web\\/classic\\/src\\/components\\/layout\\/headerbar\\/LanguageSelector\\.jsx$/, /^web\\/classic\\/src\\/components\\/settings\\/personal\\/cards\\/PreferencesSettings\\.jsx$/]; const bad=paths.filter(p=>!allow.some(r=>r.test(p))); if(bad.length){console.error('unexpected upstream PR paths:', bad); process.exit(1)}"</automated>
<automated>if git diff upstream/main...HEAD -- . | rg -n "Atius|router-ai-atius|\\.planning|Graphify|Obsidian|Podman|governor|DBRouterAiAtius|Bearer|auth\\.json|/home/ubuntu"; then exit 1; fi</automated>
<automated>for f in .planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md /tmp/new-api-pt-pr-body.md /tmp/new-api-pt-pr-comment.md; do test -e "$f" || continue; if rg -n "Atius|router-ai-atius|\\.planning|Graphify|Obsidian|Podman|governor|DBRouterAiAtius|Bearer|auth\\.json|/home/ubuntu" "$f"; then exit 1; fi; done</automated>
<automated>gh issue view 2924 --repo QuantumNous/new-api --json number,title,state,url,updatedAt</automated>
<automated>gh pr view 5801 --repo QuantumNous/new-api --json number,title,state,files,url,updatedAt</automated>
</verification>

<success_criteria>
- Final branch is one clean, upstream-ready PT-BR commit if promoted.
- Duplicate issue/PR state is current.
- Handoff draft uses the upstream PR template and AI-assisted disclosure.
- Code diff and PR/comment draft pass failing leak checks.
- No `.planning/`, Graphify, Obsidian, fork runtime, secrets, or Atius-only content enters the upstream branch or PR text.
</success_criteria>

<output>
Create `.planning/phases/21-feat-pt-native-pr/21-UPSTREAM-HANDOFF.md` and `21-05-SUMMARY.md` outside the upstream PR branch after all code validation passes.
</output>
