---
phase: 33-reranker-reliability-observability-and-readiness
plan: "13"
type: execute
wave: 6
depends_on: ["33-04", "33-05", "33-06", "33-08", "33-09", "33-10", "33-11", "33-12"]
files_modified:
  - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/sync.yaml
  - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
  - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/fork_sync/core/release_preflight.py
  - /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/bin/deploy.sh
  - .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json
autonomous: true
requirements:
  - PHASE-33-FORK-SYNC-PERSISTENCE
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Omni protected paths include the four mandated files and every new Phase 33 carrier needed to preserve taxonomy/readiness/UI."
    - "Release preflight validates exact env/source/test/label/digest invariants without reading credential values."
    - "Deploy builds and smokes an isolated labeled candidate before live tag/restart, while preserving immutable rollback identity."
    - "All Omni protection changes are integrated through the canonical reviewed remote/main path and read back there before candidate build."
    - "A clean retained worktree is pinned to that exact origin/main integration commit and is the only Omni execution authority for Plans 33-14/33-15."
  artifacts:
    - path: "sync.yaml"
      provides: "Protected paths and ordered focused post-sync gates"
    - path: "release_preflight.py"
      provides: "Reranker violation collector and structured fail-closed status"
    - path: "deploy.sh"
      provides: "Candidate-before-promotion and rollback workflow"
    - path: ".planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json"
      provides: "Expected integration commit and retained clean worktree absolute path"
  key_links:
    - from: "sync.yaml"
      to: "test_release_preflight.py"
      via: "protected-path and post_sync assertions"
      pattern: "reranker|perf_metrics|channel_readiness"
    - from: "deploy.sh"
      to: "scripts/smoke-reranker-readiness.py"
      via: "candidate mode before live promotion"
      pattern: "candidate|reranker-governed"
---

<objective>
Implement omni-srv-admin fork-sync and release protection on the serialized Phase 33 worktree.

Purpose: Prevent upstream/main/quota sync or deploy from replacing the governed baseline with an incomplete candidate.
Output: Protected paths, exact preflight collector, candidate build/smoke order, immutable rollback, and green Wave 0 omni tests.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-04-SUMMARY.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-12-SUMMARY.md
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/AGENTS.md
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/sync.yaml
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/fork_sync/core/release_preflight.py
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/tests/test_release_preflight.py
@/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/bin/deploy.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protect every reranker carrier and order post-sync guards first</name>
  <files>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/sync.yaml, /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md</files>
  <read_first>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/AGENTS.md, /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/sync.yaml, /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md, .planning/phases/33-reranker-reliability-observability-and-readiness/33-12-SUMMARY.md</read_first>
  <action>Continue only on the exact worktree/branch/base from Plan 33-04. Add explicit protection for `dto/channel_settings_tei_rerank_test.go`, `k8s/router-ai-atius/configmap.yaml`, `relay/embedding_handler_test.go`, and `relay/rerank_handler.go`, plus `pkg/perf_metrics/`, `model/perf_metric.go`, `controller/perf_metrics.go`, `middleware/distributor.go`, `controller/channel_readiness.go`, `service/channel_readiness.go`, and both Phase 33 dashboard components. Document terminal taxonomy/readiness/context/retry/max-20/config/user-quota invariants and make post_sync run the existing guard, focused Phase 33 tests, exact env/marker checks, and user-quota guard before any build/release action. Keep tei_rerank.go protected through its existing directory rule.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard &amp;&amp; omni srv1-ops resources run builds -- python3 -m pytest modules/fork-sync/cli/tests/test_release_preflight.py -k 'protected or reranker or atius_router' -q</automated>
  </verify>
  <acceptance_criteria>
    - All four mandated paths and new carrier paths are in protected_paths.
    - Guard/focused tests precede build/deploy in post_sync.
    - Docs state exact aliases, 0/0 ceilings, max 20, deb39c92d-equivalent capability, and unchanged user quota behavior.
  </acceptance_criteria>
  <done>Fork sync cannot silently drop Phase 33 or the governed production baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement exact reranker release-preflight violations</name>
  <files>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/fork_sync/core/release_preflight.py</files>
  <read_first>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/fork_sync/core/release_preflight.py, /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/tests/test_release_preflight.py</read_first>
  <action>Add a deterministic `_atius_router_reranker_violations` collector and integrate it into structured preflight. Require the protected/source files, `tei_rerank.go`, `maxGovernedTEIRerankDocuments = 20`, outcome/readiness/timeout/retry markers, channel 10/alias/ability/config values, focused-test success evidence, `org.opencontainers.image.revision`, `com.atius.router.reranker-governed=true`, and immutable candidate digest. Emit fixed code/path/message tuples only; never scan or print environment credential values. Return success only when the collector and existing user-quota checks are empty.</action>
  <verify>
    <automated>cd /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard &amp;&amp; omni srv1-ops resources run builds -- python3 -m pytest modules/fork-sync/cli/tests/test_release_preflight.py -k 'reranker or atius_router' -q</automated>
  </verify>
  <acceptance_criteria>
    - Every Plan 33-04 negative fixture passes with its exact error assertion.
    - Preflight remains non-zero on any reranker or user-quota violation.
    - No secret value enters checks/errors/logs.
  </acceptance_criteria>
  <done>Preflight fails before build/promotion for every missing capability invariant.</done>
</task>

<task type="auto">
  <name>Task 3: Implement digest-bound release modes and integrate them canonically</name>
  <files>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/bin/deploy.sh, .planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json</files>
  <read_first>/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/bin/deploy.sh, /home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard/modules/fork-sync/cli/fork_sync/core/release_preflight.py, scripts/podman-admin.sh, scripts/smoke-reranker-readiness.py</read_first>
  <action>Refactor the existing Atius Router path in `deploy.sh` without replacing its current project/repo interface. Add explicit `--candidate-only`, `--candidate-digest-file PATH`, `--verify-candidate DIGEST`, `--promote-digest DIGEST`, `--rollback-digest DIGEST`, and `--verify-live DIGEST` modes. Candidate-only must build through the governed wrapper with labels `org.opencontainers.image.revision=$SOURCE_COMMIT` and `com.atius.router.reranker-governed=true`, resolve the immutable digest, inspect protected capability/config markers, start an isolated candidate, invoke the Router's `scripts/smoke-reranker-readiness.py --target candidate`, and write a sanitized machine-readable digest/result file without pushing `latest` or restarting live. Verify-candidate must start the existing immutable digest without rebuilding, inspect its exact runtime governor model list, run authenticated readiness and functional rerank, then clean up only its isolated container. Promotion must accept only the validated digest, capture the old live digest first, restart by immutable digest, and verify live digest plus functional smoke. Any failed post-promotion assertion must invoke rollback, prove the running digest equals the old digest and functional rerank is healthy, then exit nonzero so execution cannot continue. Preserve existing `--dry-run` compatibility and never bypass CPU containment or alter Phase 29/30 routing.

After all Plan 33-13 tests pass, serialize canonical integration: inspect branch/base/remote and repo PR policy; commit only the four owned Omni files atomically on `codex/phase33-reranker-guard-20260814`; fetch/rebase or merge the current remote base without discarding unrelated work; push the branch; create a PR using the repository template and the required AI-assisted disclosure after comparing current git identity with core authors; wait for required checks; and merge through the normal non-force policy. If canonical policy explicitly requires a controlled local merge, document that policy and use a non-force, reviewable merge commit instead. Fetch `origin/main`, set `EXPECTED_OMNI_COMMIT` to its exact integrated commit, and byte-compare all four owned files plus required markers.

Create and retain `/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified` as a detached clean worktree at exactly `EXPECTED_OMNI_COMMIT`. If that path already exists, proceed only when it is a registered worktree, clean, and already at the same commit; otherwise fail without deleting or overwriting it. Run final canonical tests/preflight from this retained worktree, never from the dirty shared `/home/ubuntu/GitHub/omni-srv-admin` checkout. Because `cli/omni` is a package directory rather than an executable, first require `cli/omni/__main__.py`, then run `PYTHONPATH="$verified/cli" python3 -m omni --help` as the pinned-entrypoint sanity check and invoke the governed test command through the same `PYTHONPATH="$verified/cli" python3 -m omni ...` entrypoint. Write `33-OMNI-INTEGRATION.json` with only `worktree_path`, `expected_commit`, `origin_main_commit`, `integration_method`, and `backout_commit_parent`; validate all commit fields are full hashes and `worktree_path` equals the fixed absolute path. Do not remove this worktree in Plan 33-13: Plans 33-14 and 33-15 depend on it. Record feature/integration commit IDs and revert-based backout in the summary. Any push, review, merge, check, retained-worktree, entrypoint-sanity, or remote readback failure is fail-closed; never mutate main silently, force-push, reset, or proceed to Plan 33-14.</action>
  <verify>
    <automated>bash -lc 'set -euo pipefail; edit=/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-guard; verified=/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified; manifest=.planning/phases/33-reranker-reliability-observability-and-readiness/33-OMNI-INTEGRATION.json; expected=$(python3 -c '"'"'import json,sys; d=json.load(open(sys.argv[1])); assert d["worktree_path"]=="/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified"; print(d["expected_commit"])'"'"' "$manifest"); test "$(git -C "$verified" rev-parse HEAD)" = "$expected"; test "$(git -C "$verified" status --porcelain | wc -l)" -eq 0; test "$(git -C "$verified" rev-parse origin/main)" = "$expected"; test -f "$verified/cli/omni/__main__.py"; PYTHONPATH="$verified/cli" python3 -m omni --help &gt;/dev/null; PYTHONPATH="$verified/cli" python3 -m omni srv1-ops resources run builds -- python3 -m pytest "$verified/modules/fork-sync/cli/tests/test_release_preflight.py" -q; bash -n "$verified/modules/fork-sync/bin/deploy.sh"; "$verified/modules/fork-sync/bin/deploy.sh" atius-router "$PWD" --dry-run; for f in modules/fork-sync/projects/atius-router/sync.yaml modules/fork-sync/projects/atius-router/UPSTREAM-SYNC-GUARDS.md modules/fork-sync/cli/fork_sync/core/release_preflight.py modules/fork-sync/bin/deploy.sh; do cmp "$edit/$f" "$verified/$f"; done'</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove build/label/digest/isolation/readiness/rerank occur before promotion/restart.
    - Old live digest is captured before mutation and rollback revalidates rerank.
    - Candidate failure leaves live runtime/tag untouched.
    - Canonical remote/main contains byte-identical protected/config/preflight/deploy changes after reviewed non-force integration.
    - Summary records feature/integration commits, integration path, required-check result, remote/main readback, and revert-based backout.
    - Retained worktree is clean, detached at the exact expected origin/main commit, and remains present for dependent plans.
    - `33-OMNI-INTEGRATION.json` contains only deterministic non-secret path/commit/integration/backout metadata.
  </acceptance_criteria>
  <done>The omni release workflow is candidate-first, digest-bound, and rollback-safe.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Omni protected-path/post_sync entries for all Phase 33 carriers.
- `_atius_router_reranker_violations` release-preflight collector.
- Candidate labels `org.opencontainers.image.revision` and `com.atius.router.reranker-governed`.
- Candidate-first deploy and immutable-digest rollback flow.
- Canonically integrated Omni commit/PR identity with remote/main byte readback and revert backout.
- Retained clean `/home/ubuntu/GitHub/worktrees/omni-phase33-reranker-verified` plus `33-OMNI-INTEGRATION.json` handoff for Plans 33-14/33-15.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Upstream sync to fork source | Merge can remove local governed behavior. |
| Source commit to candidate image | Build context/tag may not match reviewed source. |
| Candidate to live runtime | Promotion changes production and requires rollback identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-36 | Tampering | sync/protected paths | high | mitigate | Explicit carrier allowlist and guard-first post_sync. |
| T-33-37 | Spoofing | image identity | high | mitigate | Source/capability labels plus immutable digest. |
| T-33-38 | Denial of Service | promotion | high | mitigate | Isolated candidate smoke and automatic digest rollback. |
| T-33-39 | Information Disclosure | preflight/deploy logs | high | mitigate | Fixed metadata only and credential-value exclusion. |
</threat_model>

<verification>
Run the entire omni preflight test file and shell syntax through its governed resource wrapper; commit changes serially on the dedicated Phase 33 branch.
</verification>

<success_criteria>
- Plan 33-04 Wave 0 tests are green.
- Cross-repo guards cover exact required paths/env/markers/tests/image identity.
- Candidate failure cannot mutate live and rollback digest is mandatory.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-13-SUMMARY.md` in the Router planning tree, including the omni branch commit hash.
</output>
