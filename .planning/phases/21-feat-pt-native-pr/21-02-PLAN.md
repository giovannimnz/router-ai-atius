---
phase: 21-feat-pt-native-pr
plan: "02"
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - i18n/i18n.go
  - i18n/i18n_test.go
  - i18n/locales/pt.yaml
autonomous: true
requirements:
  - PHASE-21-UPSTREAM-NATIVE-I18N
  - PHASE-21-REUSE-EXISTING-TRANSLATIONS
  - PHASE-21-PT-BR-COVERAGE
  - PHASE-21-LOCAL-FIRST-VALIDATION
must_haves:
  truths:
    - "D-04: Backend Portuguese locale must be wired through `i18n/locales/pt.yaml`, `LangPt`, file loading, localizer creation, normalization, and supported languages."
    - "D-05: Backend implementation must not add root-level `i18n/pt.yaml`."
    - "D-09: Backend PT key parity against `i18n/locales/en.yaml` is required as part of 100% PT-BR coverage."
    - "D-11: Backend placeholder and protected string parity must be proven by tests/checks, not assumed."
    - "D-12: Backend tests must protect observable language behavior and must not be reward-hacking coverage tests."
    - Backend PT values must reuse the approved inventory source before translating gaps.
  artifacts:
    - `i18n/locales/pt.yaml`
  key_links:
    - Accept-Language/user setting -> normalizeLang -> supported PT localizer
    - en.yaml key set -> pt.yaml key set -> translated backend API errors
---

<objective>
Add Brazilian Portuguese to the backend through the native upstream `i18n/` package and prove key parity, placeholder parity, and language normalization behavior.
</objective>

## Plan Set Source Audit

| Source | ID | Requirement / feature | Plan | Status | Notes |
|---|---|---|---|---|---|
| CONTEXT | D-04/D-05 | Backend native `i18n/locales/pt.yaml` | 21-02 | COVERED | Explicitly forbids root `i18n/pt.yaml`. |
| CONTEXT | D-09/D-11 | Key and placeholder parity | 21-02 | COVERED | Required in Go tests/checks. |
| REQUIREMENTS | PHASE-21-UPSTREAM-NATIVE-I18N | Native backend i18n surface | 21-02 | COVERED | Uses existing go-i18n embed/list pattern. |
| REQUIREMENTS | PHASE-21-REUSE-EXISTING-TRANSLATIONS | Reuse approved backend source | 21-02 | COVERED | Consumes `21-TRANSLATION-INVENTORY.md`. |

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md
@i18n/i18n.go
@i18n/locales/en.yaml
@i18n/locales/zh-CN.yaml
@i18n/locales/zh-TW.yaml
</context>

<execution_lane>
Read `implementation_worktree` from `.planning/phases/21-feat-pt-native-pr/21-TRANSLATION-INVENTORY.md` before editing. Run all code edits, tests, and `git diff upstream/main...HEAD` checks from that recorded worktree, not from the dirty planning checkout. If the lane is missing, dirty before edits, or not based on current `upstream/main`, stop and repair `21-01`.
</execution_lane>

<tasks>

<task type="auto">
  <name>Task 1: Add backend PT locale from approved reused source</name>
  <files>i18n/locales/pt.yaml</files>
  <action>
    Create `i18n/locales/pt.yaml` from the approved backend reuse source in the inventory. Translate only true upstream-current gaps after reuse. Preserve message IDs, Go template placeholders such as `{{.Max}}`, code fragments, model/API names, URLs, and protected project identity.
  </action>
  <acceptance_criteria>
    - `i18n/locales/pt.yaml` exists.
    - `i18n/pt.yaml` does not exist.
    - PT message IDs match `i18n/locales/en.yaml`.
    - Placeholder tokens match the English source for every shared key.
  </acceptance_criteria>
  <verify>
    <automated>test -f i18n/locales/pt.yaml</automated>
    <automated>test ! -e i18n/pt.yaml</automated>
  </verify>
  <done>Backend PT YAML is present in the upstream-native locale directory.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PT into backend i18n exactly like supported languages</name>
  <files>i18n/i18n.go</files>
  <action>
    Add `LangPt = "pt"`, load `locales/pt.yaml`, pre-create `localizers[LangPt]`, normalize `pt`, `pt-BR`, and `pt_BR` to `pt`, and include `pt` in `SupportedLanguages()`. Keep fallback behavior for unsupported languages unchanged.
  </action>
  <acceptance_criteria>
    - `ParseAcceptLanguage("pt-BR,pt;q=0.9,en;q=0.8")` resolves to `pt`.
    - `IsSupported("pt-BR")` is true via normal normalization.
    - Unsupported languages still fall back to `en`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "LangPt|locales/pt.yaml|SupportedLanguages|pt-BR|pt_BR" i18n/i18n.go</automated>
  </verify>
  <done>Backend can select Portuguese through existing localizer behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add behavior-focused backend i18n tests</name>
  <files>i18n/i18n_test.go</files>
  <action>
    Add deterministic Go tests that protect user-visible behavior: supported language list includes `pt`, Accept-Language and underscore variants normalize to `pt`, PT translation works for a representative message, English fallback remains unchanged, and PT/EN locale files have matching message IDs and placeholder token sets. New or substantially rewritten backend tests must import and use `github.com/stretchr/testify/require` for setup/fatal assertions and `github.com/stretchr/testify/assert` for non-fatal value checks.
  </action>
  <acceptance_criteria>
    - Tests fail if `pt.yaml` is missing, located at root, missing keys, or has placeholder drift.
    - Tests do not assert private implementation details beyond the exported language behavior and locale contract.
  </acceptance_criteria>
  <verify>
    <automated>go test ./i18n || /usr/local/go/bin/go test ./i18n</automated>
  </verify>
  <done>Backend parity and normalization are enforced by local tests.</done>
</task>

<task type="auto">
  <name>Task 4: Check backend diff scope</name>
  <files>implementation branch only</files>
  <action>
    Confirm backend plan changes are limited to the native i18n files and behavior tests. Do not include fork runtime, provider, DB, Graphify, docs, or planning artifacts in the implementation branch.
  </action>
  <acceptance_criteria>
    - Backend diff is limited to `i18n/i18n.go`, `i18n/i18n_test.go`, and `i18n/locales/pt.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>git diff --name-only upstream/main...HEAD -- i18n</automated>
    <automated>git diff --check upstream/main...HEAD -- i18n</automated>
  </verify>
  <done>Backend change is upstream-reviewable as native PT support.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|---|---|---|
| Adding non-native `i18n/pt.yaml` | high | Verify root file does not exist and add tests for native locale path behavior. |
| Placeholder drift breaks localized API errors | high | Backend tests compare placeholder token sets. |
| Unsupported language fallback regresses | medium | Tests keep fallback to English for unknown languages. |
| Translation reuse carries protected/fork strings | high | Consume approved inventory and review protected identity strings. |
</threat_model>

<verification>
<automated>test -f i18n/locales/pt.yaml && test ! -e i18n/pt.yaml</automated>
<automated>go test ./i18n || /usr/local/go/bin/go test ./i18n</automated>
<automated>git diff --check upstream/main...HEAD -- i18n</automated>
</verification>

<success_criteria>
- Backend supports `pt`, `pt-BR`, and `pt_BR` through native normalization.
- Backend PT YAML has the same message IDs and placeholders as English.
- Backend tests pass.
- Backend implementation branch contains no fork/runtime/planning leakage.
</success_criteria>

<output>
Create `.planning/phases/21-feat-pt-native-pr/21-02-SUMMARY.md` outside the upstream PR branch after backend implementation passes.
</output>
