---
phase: 28-branch-hygiene-and-mainline-reconciliation
plan: "03"
type: plan
wave: 3
depends_on:
  - "28-02"
autonomous: false
requirements:
  - PHASE-28-MAINLINE-RECONCILIATION
---

<objective>
Create a clean reconciliation branch from `origin/main`, port only what belongs on fork mainline, validate it, and merge it back to `main`.

This is intentionally not a wholesale merge of `feat/pt-native`.
</objective>

## Deliverables

- fresh worktree/branch created from `origin/main`
- selective replay/cherry-pick/patch-port of the fork work that belongs on main
- merged and validated `main` without depending on the polluted local integration branch

## Tasks

1. Create a fresh reconciliation worktree from `origin/main`.
2. Selectively port:
   - branch hygiene docs
   - finalized 24/25/26/27 fork docs/planning/workflow changes that belong on main
   - milestone split moving 22/23 out of the completed recovery milestone
3. Exclude:
   - PT-native upstream handoff implementation lane
   - stale local integration branch content
   - backup artifacts
4. Validate the reconciliation branch.
5. Merge the reconciliation branch into local `main`, then fast-forward/push to `origin/main` when approved.

## Acceptance

- `main` contains only the changes that make sense for fork mainline
- no reliance on `feat/pt-native` as merge source remains
- 22/23 are correctly represented as future milestone work

## Verification

- `git diff --name-status origin/main...HEAD`
- workflow YAML parse
- focused test/doc validation for the fork changes being ported
- `git log --oneline --decorate -n 20`
