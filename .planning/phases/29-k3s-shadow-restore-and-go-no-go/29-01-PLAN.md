---
phase: 29-k3s-shadow-restore-and-go-no-go
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - k8s/router-ai-atius/postgres.yaml
  - k8s/router-ai-atius/redis.yaml
  - k8s/router-ai-atius/router.yaml
  - scripts/k3s-router-preflight.sh
  - scripts/k3s-router-validate-manifests.sh
  - scripts/k3s-router-apply-shadow.sh
autonomous: false
requirements:
  - PHASE-22-K3S-PREFLIGHT
  - PHASE-22-STATEFUL-DATA
must_haves:
  truths:
    - "O atius-srv-1 tem pelo menos 20 GiB recuperados com seguranca, alvo de 25% livre, DiskPressure=False e taint ausente por cinco minutos continuos."
    - "PostgreSQL, Redis e router exigem o label dedicado atius.com.br/router-ai-atius-node=true por required node affinity."
    - "Os PVCs usam local-path explicitamente e o Service shadow permanece ClusterIP."
    - "A imagem arm64 do router e identificada por digest imutavel e os Secrets existem sem valores versionados ou registrados."
  artifacts:
    - path: "scripts/k3s-router-preflight.sh"
      provides: "Gate fail-closed de espaco, DiskPressure, taint, label, storage, imagem e Secret"
    - path: "scripts/k3s-router-validate-manifests.sh"
      provides: "Validacao estatica e server-side dos invariantes dos manifests"
    - path: "k8s/router-ai-atius/router.yaml"
      provides: "Router pinado por affinity, PVC local-path e Service ClusterIP"
  key_links:
    - from: "scripts/k3s-router-preflight.sh"
      to: "scripts/k3s-router-apply-shadow.sh"
      via: "apply interrompe antes de qualquer mutacao quando o preflight falha"
      pattern: "k3s-router-preflight"
    - from: "k8s/router-ai-atius/router.yaml"
      to: "atius-srv-1"
      via: "requiredDuringSchedulingIgnoredDuringExecution no label dedicado"
      pattern: "atius.com.br/router-ai-atius-node"
---

<objective>
Preparar o node e endurecer manifests/preflight para que nenhum recurso stateful seja aplicado enquanto espaco, DiskPressure, placement, imagem, storage ou Secrets estiverem inseguros.

Purpose: Remover o bloqueio real de scheduling sem mascarar a pressao e estabelecer os invariantes exigidos para o restore estagiado.
Output: Manifests pinados e gates fail-closed prontos, com Service exclusivamente ClusterIP.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/debug/phase29-diskpressure-audit.md
@k8s/router-ai-atius/postgres.yaml
@k8s/router-ai-atius/redis.yaml
@k8s/router-ai-atius/router.yaml
@scripts/k3s-router-preflight.sh
@scripts/k3s-router-validate-manifests.sh
@scripts/k3s-router-apply-shadow.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Endurecer manifests e validacao estatica</name>
  <files>k8s/router-ai-atius/postgres.yaml, k8s/router-ai-atius/redis.yaml, k8s/router-ai-atius/router.yaml, scripts/k3s-router-validate-manifests.sh</files>
  <behavior>
    - Teste 1: os tres PodSpecs possuem requiredDuringSchedulingIgnoredDuringExecution para atius.com.br/router-ai-atius-node=true.
    - Teste 2: os dois PVCs declaram storageClassName local-path e nenhum manifest contem toleration de DiskPressure.
    - Teste 3: o Service router-ai-atius e ClusterIP e a validacao rejeita NodePort, Ingress, hostPort e hostNetwork.
    - Teste 4: imagens e limites respeitam arm64, identidade imutavel e o teto agregado de CPU da fase.
  </behavior>
  <action>Primeiro ampliar as assertions do validador para falharem com os estados proibidos; depois ajustar os manifests. Usar o mesmo bloco de required node affinity nos tres workloads, PVCs explicitamente local-path e recursos de CPU compatíveis com o limite global de 20% do host durante operacoes pesadas. Manter o Service como ClusterIP por decisao vinculante e pela prova live da auditoria; a recomendacao anterior de NodePort em RESEARCH/PATTERNS foi superada. Rejeitar tag latest flutuante como identidade validada, literais de Secret, toleration de DiskPressure, preferred affinity, Ingress, hostPort, hostNetwork e NodePort.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-validate-manifests.sh &amp;&amp; scripts/k3s-router-validate-manifests.sh --static-only</automated>
  </verify>
  <done>O validador prova os invariantes de affinity, local-path, ClusterIP, CPU, imagem e ausencia das construcoes proibidas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Transformar o preflight em gate fail-closed</name>
  <files>scripts/k3s-router-preflight.sh, scripts/k3s-router-apply-shadow.sh</files>
  <behavior>
    - Teste 1: Ready diferente de True, DiskPressure diferente de False ou taint presente encerram com status nao zero.
    - Teste 2: menos de 20 GiB recuperados, menos de 25% livre ou estabilidade inferior a cinco minutos encerram com status nao zero.
    - Teste 3: o label dedicado deve existir em exatamente um node e esse node deve ser atius-srv-1.
    - Teste 4: evidencia ausente de local-path, digest arm64 ou chaves esperadas do Secret encerra com status nao zero sem ler valores.
  </behavior>
  <action>Implementar modo de avaliacao testavel com fixtures e modo live read-only. Medir bytes e percentual do filesystem compartilhado nodefs/imagefs, registrar baseline e exigir recuperacao segura acumulada de 20 GiB, alvo de 25% livre, DiskPressure=False e ausencia da taint por cinco minutos continuos. Exigir exatamente o label dedicado no srv1, local-path, digest imutavel importavel no containerd e presenca somente dos nomes de chaves esperados no Secret. O apply deve chamar preflight e validacao antes de qualquer kubectl apply e manter ordem estagiada. Nenhum comando deve remover taint, alterar eviction thresholds, adicionar toleration ou revelar Secret.</action>
  <verify>
    <automated>bash -n scripts/k3s-router-preflight.sh scripts/k3s-router-apply-shadow.sh &amp;&amp; scripts/k3s-router-preflight.sh --self-test</automated>
  </verify>
  <done>Fixtures negativas falham, fixture saudavel passa e o apply nao consegue ultrapassar um preflight vermelho.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Aprovar conjunto seguro de limpeza ou registrar no-go</name>
  <files>Nenhum arquivo de repo; evidencia operacional fica fora do Git.</files>
  <action>Revisar a allowlist produzida pela execucao, sem ampliar o escopo para paths nao inventariados. Prosseguir somente com o conjunto explicitamente aprovado; se a margem exigida nao puder ser obtida com seguranca, encerrar o plano com no-go objetivo.</action>
  <decision>Aprovar somente os candidatos de limpeza cuja propriedade, retencao e regenerabilidade foram comprovadas, antes da execucao live.</decision>
  <context>A auditoria encontrou consumo principalmente fora do k3s, mas proibiu apagar sem validacao do owner. A execucao deve apresentar paths, bytes recuperaveis, motivo de seguranca, backup quando aplicavel e comando reversivel; nao pode incluir dados, volumes, backups ativos, Secrets ou prune amplo.</context>
  <options>
    <option id="approve-safe-set">
      <name>Aprovar conjunto seguro</name>
      <pros>Permite recuperar espaco com escopo auditado e evidencia antes/depois.</pros>
      <cons>Pode resultar em no-go se o conjunto aprovado nao atingir os gates.</cons>
    </option>
    <option id="no-go">
      <name>Registrar no-go</name>
      <pros>Preserva integralmente o host e o runtime Podman.</pros>
      <cons>Bloqueia os planos seguintes ate existir margem de disco segura.</cons>
    </option>
  </options>
  <verify>
    <automated>test -s "${PHASE29_CLEANUP_CANDIDATES:?defina o artefato sanitizado}" &amp;&amp; ! rg -n 'password|token|secret_value|BEGIN.*PRIVATE KEY' "$PHASE29_CLEANUP_CANDIDATES"</automated>
  </verify>
  <done>Existe aprovacao explicita do conjunto seguro ou no-go registrado; nenhuma limpeza fora da allowlist ocorre.</done>
  <resume-signal>Selecione approve-safe-set ou no-go.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → cleanup gate | Paths locais potencialmente destrutivos entram na lista de limpeza. |
| Vault/runtime → Kubernetes Secret | Valores sensiveis cruzam para o cluster sem passar por Git ou evidencia. |
| manifests → k3s scheduler | Configuracao determina node, storage, rede e consumo de recursos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering | limpeza de disco | high | mitigate | allowlist aprovada, inventario antes/depois, sem prune amplo e checkpoint bloqueante |
| T-29-02 | Information Disclosure | Secret/Vault | high | mitigate | validar nomes de chaves sem ler valores; proibir set -x e payloads em evidencia |
| T-29-03 | Denial of Service | scheduler/PVC | high | mitigate | gate de DiskPressure, affinity obrigatoria e local-path antes do apply |
| T-29-04 | Tampering | imagem do router | high | mitigate | digest arm64 imutavel e identidade registrada sem latest flutuante |
| T-29-SC | Tampering | supply chain de imagens | high | mitigate | usar somente imagens existentes/auditadas e pinadas por digest; nenhuma instalacao de package |
</threat_model>

<verification>
Executar as verificacoes estaticas e self-tests; apos aprovacao do conjunto seguro, executar o preflight live e guardar apenas evidencia sanitizada de bytes, percentuais, conditions, taints, label, storage, digest e nomes de chaves.
</verification>

<success_criteria>
O preflight live fica verde somente com os quatro gates de disco/pressao estabilizados, os manifests passam server-side dry-run, o label e affinity concordam, a imagem e imutavel, Secrets nao vazam e o Service continua ClusterIP.
</success_criteria>

<output>
Create `.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-01-SUMMARY.md` when done
</output>
