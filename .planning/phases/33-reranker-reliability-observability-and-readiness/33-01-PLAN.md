---
phase: 33-reranker-reliability-observability-and-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/perf_metrics/metrics_test.go
  - model/perf_metric_test.go
  - middleware/distributor_test.go
  - controller/relay_test.go
autonomous: true
requirements:
  - PHASE-33-METRICS-OUTCOME-TAXONOMY
  - PHASE-33-ROUTING-AVAILABILITY
  - PHASE-33-TEST-DOC-LIVE-EVIDENCE
must_haves:
  truths:
    - "Wave 0 locks seven mutually exclusive terminal outcomes and the invariant terminal_count equals their sum."
    - "Wave 0 proves a no-channel exit is routing_unavailable and a retried request still has one terminal sample."
    - "Wave 0 distinguishes historical unclassified rows from complete zero-valued outcome classes."
  artifacts:
    - path: "pkg/perf_metrics/metrics_test.go"
      provides: "Outcome, exact-once, weighted-total, Redis drain/restore, and completeness contracts"
    - path: "model/perf_metric_test.go"
      provides: "Additive GORM persistence and dialect-compatibility contract"
    - path: "middleware/distributor_test.go"
      provides: "Pre-relay no-channel accounting contract"
    - path: "controller/relay_test.go"
      provides: "Retry and terminal settlement/accounting contract"
  key_links:
    - from: "middleware/distributor_test.go"
      to: "pkg/perf_metrics/metrics_test.go"
      via: "the same request-scoped terminal recorder and OutcomeRoutingUnavailable"
      pattern: "OutcomeRoutingUnavailable|Finish"
    - from: "controller/relay_test.go"
      to: "service/quota.go"
      via: "successful terminal completion remains once per request"
      pattern: "RecordRelaySample|Finish"
---

<objective>
Create the Wave 0 backend contract tests for the outcome taxonomy, additive persistence, pre-relay routing availability, and exact-once terminal semantics.

Purpose: PHASE-33-METRICS-OUTCOME-TAXONOMY and PHASE-33-ROUTING-AVAILABILITY must fail deterministically before production code changes and protect billing/governor behavior during implementation.
Output: Four focused Go test files that initially fail only because the new Phase 33 contracts do not exist, and pass after Plans 33-05 and 33-06.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-CONTEXT.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-RESEARCH.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-PATTERNS.md
@.planning/phases/33-reranker-reliability-observability-and-readiness/33-VALIDATION.md
@pkg/perf_metrics/types.go
@pkg/perf_metrics/metrics.go
@pkg/perf_metrics/flush.go
@model/perf_metric.go
@middleware/distributor.go
@controller/relay.go
@service/quota.go
@service/text_quota.go

<interfaces>
Existing seams: perfmetrics.RecordRelaySample(info, success, outputTokens), perfmetrics.Record(sample), model.UpsertPerfMetric(metric), service.CacheGetRandomSatisfiedChannel(retryParam), controller.Relay, and the successful recording calls in service/quota.go and service/text_quota.go. New tests target Outcome, OutcomeOrigin, TerminalRecorder, NewTerminalRecorder, Finish, and classified raw counters defined by Plan 33-05.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock taxonomy, aggregate, migration, and partial-history behavior</name>
  <files>pkg/perf_metrics/metrics_test.go, model/perf_metric_test.go</files>
  <read_first>pkg/perf_metrics/types.go, pkg/perf_metrics/metrics.go, pkg/perf_metrics/flush.go, model/perf_metric.go, model/main.go, AGENTS.md</read_first>
  <behavior>
    - Every terminal sample maps to exactly one of success, client_validation, governor_rejection, routing_unavailable, transport_timeout, upstream_http_failure, or invalid_upstream_response.
    - Uneven model volumes produce weighted request success, service availability, latency, and throughput from raw totals rather than a mean of percentages.
    - A contributing bucket is complete only when it carries the v2 writer marker and its seven outcome counters sum to request_count; legacy-all-success and mixed windows remain partial.
    - Additive upsert/query generation stays valid for SQLite, MySQL, and PostgreSQL without JSONB or dialect-only SQL.
  </behavior>
  <action>Create deterministic testify table tests for PHASE-33-METRICS-OUTCOME-TAXONOMY. Name the seven Outcome constants explicitly; assert request_count equals the sum of all seven counters for each new bucket, latency uses latency_sample_count, flush failure restores every counter, and every contributing bucket must carry the v2 writer marker plus the sum invariant. Add regressions for a legacy-all-success bucket with zero failure counters, a mixed legacy/v2 window, a v2 bucket with mismatched totals, and a complete v2 window with a legitimate zero class. Exercise real AutoMigrate/upsert/query on in-memory SQLite. Add DSN-gated real MySQL/PostgreSQL integration subtests using existing dialectors: probe documented disposable service profiles without printing connection values; when a profile/DSN is available, fail unless the real migration/upsert/SUM test runs and passes. When neither service is available, run strict GORM-only DryRun generation with DisableAutomaticPing and emit `CROSS_DB_RESIDUAL_RISK=DryRun-only:no-disposable-profile` as the accepted residual risk. Do not pull images, add packages, use dialect-only SQL, include payload fixtures, random loops, or sleeps.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'set +e; /usr/local/go/bin/go test ./pkg/perf_metrics ./model -run "Test.*(Outcome|Terminal|PerfMetric|Weighted|Partial|Dialect)" -count=1 &gt;/tmp/phase33-w0-metrics.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "undefined:.*(Outcome|TerminalRecorder|OutcomeSchemaVersion|LatencySampleCount)" /tmp/phase33-w0-metrics.log'</automated>
  </verify>
  <acceptance_criteria>
    - `pkg/perf_metrics/metrics_test.go` contains cases for all seven required outcome names, double Finish, uneven volumes, latency_sample_count, Redis drain/restore, and partial history.
    - `model/perf_metric_test.go` runs disposable MySQL/PostgreSQL integration when available; otherwise strict DryRun passes and emits the accepted residual-risk marker.
    - Legacy-all-success, mixed legacy/v2, mismatched-v2, and complete-v2 cases prove marker plus per-bucket sum semantics.
    - The governed command either passes after implementation exists or fails only on named Phase 33 symbols that production plans will add.
  </acceptance_criteria>
  <done>The metrics and persistence contract is executable before production changes and covers PHASE-33-METRICS-OUTCOME-TAXONOMY without sensitive content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock pre-relay routing and one-terminal-request semantics</name>
  <files>middleware/distributor_test.go, controller/relay_test.go</files>
  <read_first>middleware/distributor.go, controller/relay.go, service/quota.go, service/text_quota.go, service/embeddinggovernor/governor.go, relay/constant/relay_mode.go</read_first>
  <behavior>
    - No eligible channel produces one routing_unavailable terminal outcome before controller.Relay runs.
    - Calling terminal Finish twice records one sample.
    - A safe fail-first/succeed-second rerank records one success terminal, while governor leases remain per attempt and settlement/refund remains once per request.
    - Client, governor, upstream 4xx/429, malformed response, and routing failures never enter the safe retry branch.
  </behavior>
  <action>Add focused Gin/request fixtures for PHASE-33-ROUTING-AVAILABILITY and PHASE-33-TEST-DOC-LIVE-EVIDENCE. Inject channel selection and recorder sinks rather than using network or global leftovers. Assert both distributor error and nil-channel branches finish OutcomeRoutingUnavailable once, and assert terminal recording remains separate from governor-attempt accounting and the existing quota settlement/refund paths. Preserve the valid-personal-token behavior guarded by scripts/atius-user-quota-guard.sh; these tests must not change wallet/subscription semantics.</action>
  <verify>
    <automated>./scripts/podman-admin.sh profile-run -- bash -lc 'set +e; /usr/local/go/bin/go test ./middleware ./controller -run "Test.*(NoChannel|RoutingUnavailable|SingleTerminal|RerankRetry)" -count=1 &gt;/tmp/phase33-w0-routing.log 2&gt;&amp;1; rc=$?; if [ "$rc" -eq 0 ]; then exit 0; fi; rg -q "undefined:.*(OutcomeRoutingUnavailable|TerminalRecorder|FinishTerminalOutcome)" /tmp/phase33-w0-routing.log'</automated>
  </verify>
  <acceptance_criteria>
    - Both no-channel exits in the distributor have regression cases that expect one routing_unavailable outcome.
    - Retry fixtures distinguish terminal request count, governor attempt count, and settlement/refund count.
    - The governed command either passes after implementation exists or fails only on the named Phase 33 recorder symbols.
  </acceptance_criteria>
  <done>Routing gaps and retry double-counting are guarded before Plans 33-05/33-06 edit production paths.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- `pkg/perf_metrics/metrics_test.go`: new outcome, exact-once, weighted, Redis, and partial-history tests.
- `model/perf_metric_test.go`: new additive GORM and cross-dialect compatibility tests.
- `middleware/distributor_test.go`: new pre-controller routing availability tests.
- `controller/relay_test.go`: new terminal retry/accounting tests.
</artifacts_this_phase_produces>

<plan_set_source_audit>
## Plan Set Source Audit

This audit covers the complete 16-plan dependency graph across 9 execution waves. Plan 33-16 owns the atomic HEAD-only PENDING post-commit invalidator with bounded awaited foreground `git rev-parse`/`git show`/`mktemp`/`chmod`/`mv`/`rm` helpers, the full-fingerprint synchronous-closeout pre-push, governed closeout script, and disposable fixtures; canonical final commits invalidate stale proof while the next relevant push derives and consumes the terminal proof, so this remains Plan 33-16 scope rather than a seventeenth plan or wave.

| Source | ID | Feature / constraint | Plan(s) | Status |
|---|---|---|---|---|
| GOAL | — | Operationally correct, actionable reranker health with safe release | 01-16, including Git-consumed closeout in 16 | COVERED |
| REQ | PHASE-33-METRICS-OUTCOME-TAXONOMY | Seven outcomes, aggregate-only persistence, cross-DB | 01, 05, 06 | COVERED |
| REQ | PHASE-33-ROUTING-AVAILABILITY | Pre-relay no-channel and exact-once | 01, 06 | COVERED |
| REQ | PHASE-33-WEIGHTED-DASHBOARD | Weighted API/UI with partial history | 03, 05, 10, 11 | COVERED |
| REQ | PHASE-33-CHANNEL-READINESS | Cache sync and authenticated normal-selection readiness | 02, 09, 12-16 | COVERED |
| REQ | PHASE-33-TEI-TIMEOUT-RETRY | Evidence-backed bound and one safe retry | 02, 07, 08 | COVERED |
| REQ | PHASE-33-RERANK-BATCH-CONTRACT | Keep 20-doc cap and client recomposition guidance | 02, 08, 12 | COVERED |
| REQ | PHASE-33-FORK-SYNC-PERSISTENCE | Protected paths, candidate gate, rollback | 04, 12-16 | COVERED |
| REQ | PHASE-33-TEST-DOC-LIVE-EVIDENCE | Tests, docs, candidate/local/public/readback and terminal graph freshness after execution metadata | 01-04, 12, 14-16 with post-commit/pre-push enforcement in 16 | COVERED |
| RESEARCH | — | Additive schema, latency denominator, no inferred historical classes | 01, 05 | COVERED |
| RESEARCH | — | Selection-based readiness, per-attempt context, bounded retry | 02, 07-09 | COVERED |
| RESEARCH | — | Candidate labels/digest and fail-before-promotion | 04, 13-16 | COVERED |
| RESEARCH | — | Disable async Graphify; post-commit atomically writes non-authorizing HEAD-only PENDING via bounded foreground `git rev-parse`/`git show`/`mktemp`/`chmod`/`mv`/`rm`, while pre-push derives the full fingerprint and consumes detached-worktree terminal proof | 16 | COVERED |
| CONTEXT | CTX-01..CTX-08 | All eight unnumbered locked bullets in 33-CONTEXT.md | 05-15 | COVERED |
| CONTEXT | deferred | No governor removal, Router sub-batching, k3s public cutover, or identity changes | — | EXCLUDED |
</plan_set_source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client/distributor to terminal recorder | Untrusted request/error origin becomes bounded aggregate metadata. |
| Hot/Redis metrics to database | Counters cross concurrent and persistence boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-33-01 | Tampering | outcome counters | high | mitigate | Exact-once and sum-invariant tests block inconsistent terminal accounting. |
| T-33-02 | Information Disclosure | test fixtures/logs | high | mitigate | Synthetic metadata only; no documents, prompts, tokens, credentials, or provider bodies. |
| T-33-03 | Repudiation | no-channel path | medium | mitigate | Deterministic pre-controller routing outcome tests make the omission observable. |
</threat_model>

<verification>
Run both focused commands through `scripts/podman-admin.sh`; inspect RED failures to ensure they name only Phase 33 contract symbols, not fixture/setup errors.
</verification>

<success_criteria>
- Four Wave 0 test files exist and are deterministic.
- All three assigned requirement IDs appear in task actions and executable assertions.
- No production file changes in this plan.
</success_criteria>

<output>
Create `.planning/phases/33-reranker-reliability-observability-and-readiness/33-01-SUMMARY.md` when done.
</output>
