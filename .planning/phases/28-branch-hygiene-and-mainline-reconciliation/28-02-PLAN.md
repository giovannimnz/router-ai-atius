---
phase: 28-branch-hygiene-and-mainline-reconciliation
plan: "02"
type: plan
wave: 2
depends_on:
  - "28-01"
autonomous: false
requirements:
  - PHASE-28-PHASE21-CANONICAL-REMOTE
---

<objective>
Promote the clean PT-native Phase 21 lane to the single canonical remote branch for future upstream handoff.

This plan must preserve the option to send native PT-BR to `QuantumNous/new-api` without dragging branch hygiene or fork-runtime work into the PR lane.
</objective>

## Deliverables

- committed Phase 21 handoff lane from `/home/ubuntu/GitHub/containers/router-ai-atius-phase21-upstream`
- single canonical remote PT-native branch
- explicit decision on whether `origin/feat/pt-native-i18n-clean` remains archival or is removed

## Tasks

1. Validate the current diff of `feat/phase21-pt-native-upstream` against `upstream/main`.
2. Ensure the branch scope matches `21-UPSTREAM-HANDOFF.md` and does not reintroduce polluted files.
3. Commit any remaining local Phase 21 PT-native work there in one clean handoff commit or a minimal intentional series.
4. Push/update the canonical remote branch.
5. Record the exact branch name to preserve remotely.

## Acceptance

- canonical PT-native remote branch exists and is push-complete
- branch diff is bounded to the intended upstream handoff scope
- `feat/pt-native` is no longer needed as PT handoff source

## Verification

- `git -C /home/ubuntu/GitHub/containers/router-ai-atius-phase21-upstream diff --name-status upstream/main...HEAD`
- `git -C /home/ubuntu/GitHub/containers/router-ai-atius-phase21-upstream diff --check upstream/main...HEAD`
- `git ls-remote --heads origin 'feat/*pt*'`
