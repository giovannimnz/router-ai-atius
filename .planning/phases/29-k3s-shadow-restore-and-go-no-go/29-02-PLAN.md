---
phase: 29-k3s-shadow-restore-and-go-no-go
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - scripts/k3s-router-backup.sh
  - scripts/k3s-router-restore-rehearsal.sh
autonomous: true
requirements:
  - PHASE-22-STATEFUL-DATA
must_haves:
  truths:
    - "Um dump fresco e criado diretamente do PostgreSQL Podman live na porta host 8745, com identidade cruzada pelo PgBouncer 6432, checksum e validacao estrutural."
    - "Somente namespace/config/Secret/PostgreSQL sobem antes do restore; Redis e router permanecem ausentes."
    - "Todo PV criado ate este estagio e descoberto pelos PVC UIDs, muda para Retain e tem readback confirmado antes do restore."
    - "Backup e restore executam por profile-run com cpu.max no maximo 80000/100000 e a quota e registrada."
    - "O restore ocorre em alvo limpo, falha no primeiro erro SQL e prova identidade, schema e invariantes nao sensiveis."
  artifacts:
    - path: "scripts/k3s-router-backup.sh"
      provides: "Backup fresco, validado, com checksum e metadados sanitizados"
    - path: "scripts/k3s-router-restore-rehearsal.sh"
      provides: "Apply PostgreSQL-only, Retain, restore fail-closed e verificacao"
  key_links:
    - from: "scripts/k3s-router-backup.sh"
      to: "scripts/k3s-router-restore-rehearsal.sh"
      via: "diretorio explicito, checksum e identidade da origem"
      pattern: "K3S_BACKUP_DIR"
    - from: "PVC PostgreSQL"
      to: "PV local-path"
      via: "bind no node dedicado seguido de patch reclaimPolicy=Retain"
      pattern: "persistentVolumeReclaimPolicy.*Retain"
---

<objective>
Produzir um backup fresco confiavel e restaurar o PostgreSQL de forma estagiada no k3s, protegendo o volume antes de importar dados.

Purpose: Provar que dados reais podem ser recuperados no target sem permitir que o router inicialize contra banco vazio.
Output: Scripts de backup/restore e evidencia sanitizada de dump, PV Retain e invariantes restaurados.
</objective>

<execution_context>
@/home/ubuntu/.codex/gsd-core/workflows/execute-plan.md
@/home/ubuntu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-CONTEXT.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-RESEARCH.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-PATTERNS.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-VALIDATION.md
@.planning/debug/phase29-diskpressure-audit.md
@.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-01-SUMMARY.md
@scripts/k3s-router-backup.sh
@scripts/k3s-router-apply-shadow.sh
@k8s/router-ai-atius/postgres.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar e validar backup fresco da origem live</name>
  <files>scripts/k3s-router-backup.sh</files>
  <behavior>
    - Teste 1: PostgreSQL Podman host 127.0.0.1:8745 ausente, ambiguo ou com identidade diferente de DBRouterAiAtius falha antes do pg_dump; PgBouncer 10.1.1.1:6432 deve reportar a mesma identidade sem ser a origem do dump.
    - Teste 2: dump vazio, erro do pg_dump, checksum ausente ou validacao estrutural falha encerra nao zero.
    - Teste 3: evidencia contem timestamp UTC, identidade nao sensivel, versoes, tamanho, checksum e invariantes, nunca DSN, password ou payload de Secret.
  </behavior>
  <action>Usar como origem autoritativa o PostgreSQL do Podman exposto no host em 127.0.0.1:8745 e cruzar database/user/version/invariantes nao sensiveis com o acesso atual via PgBouncer em 10.1.1.1:6432; o dump deve sair da porta 8745 para evitar semantica de pooling. Exigir opt-in e confirmar DBRouterAiAtius antes do pg_dump. Criar dump plain SQL fresco com pipeline que preserve exit status, checksum SHA-256 e metadados sanitizados; validar que o arquivo e nao vazio, parseavel/restauravel e contem os objetos esperados. Nao aceitar o backup antigo de 643 bytes. Fazer o script reexecutar a operacao pesada por ./scripts/podman-admin.sh profile-run, capturar cpu.max e falhar se a quota exceder 800m; nao imprimir credenciais.</action>
  <verify>
    <automated>PHASE29_EXECUTE=1 ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-backup.sh --live --source-host 127.0.0.1 --source-port 8745 --pgbouncer-host 10.1.1.1 --pgbouncer-port 6432 --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}"</automated>
  </verify>
  <done>Casos negativos falham e uma execucao live autorizada produz dump fresco, checksum valido e metadados sanitizados da origem correta.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restaurar PostgreSQL estagiado com PV Retain</name>
  <files>scripts/k3s-router-restore-rehearsal.sh</files>
  <behavior>
    - Teste 1: preflight vermelho, backup invalido, target nao limpo, Redis/router presentes ou PV fora do srv1 bloqueiam o restore.
    - Teste 2: o script aplica apenas namespace/config/Secret/PostgreSQL, espera bind, descobre todo PV criado pelos PVC UIDs e altera/readback cada reclaim policy para Retain antes de importar.
    - Teste 3: qualquer erro SQL falha via psql -X com ON_ERROR_STOP; rerun em banco desconhecido falha com seguranca.
    - Teste 4: identidade, schema, tabelas e contagens/invariantes esperados precisam corresponder para emitir restore_passed.
  </behavior>
  <action>Criar o rehearsal com opt-in explicito e entrada K3S_BACKUP_DIR. Reexecutar preflight, validar checksum/origem, aplicar somente os quatro estagios iniciais sob profile-run e com limite de CPU por operacao <=800m, esperar Postgres Ready e provar PVC/PV/node. Enumerar PVCs da namespace, resolver PV por spec.volumeName e conferir claimRef UID; patch Retain e readback de cada PV existente antes de qualquer import. Exigir target vazio ou recriado de forma controlada, restaurar por psql -X --set ON_ERROR_STOP=on e validar invariantes definidos pelo backup. Evidencia deve incluir cpu.max e excluir Secret YAML, DSN e dados de usuarios; falha deixa Redis/router nao aplicados e registra no-go sem excluir PVC/PV.</action>
  <verify>
    <automated>PHASE29_EXECUTE=1 ./scripts/podman-admin.sh profile-run -- scripts/k3s-router-restore-rehearsal.sh --live --backup-dir "${K3S_BACKUP_DIR:?required}" --evidence-dir "${PHASE29_EVIDENCE_DIR:?required}" &amp;&amp; sudo -n k3s kubectl get pv -o json | jq -e '[.items[] | select(.spec.claimRef.namespace=="router-ai-atius")] | length &gt; 0 and all(.spec.persistentVolumeReclaimPolicy=="Retain")'</automated>
  </verify>
  <done>O PostgreSQL k3s restaurado corresponde ao backup fresco, PVs estao Retain e o router nunca iniciou contra banco vazio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL live → arquivo de backup | Dados de producao saem do runtime para artefato protegido fora do Git. |
| backup → PostgreSQL k3s | SQL nao confiavel para o target atravessa psql. |
| PVC → PV local-path | Lifecycle do claim pode apagar dados se reclaim policy permanecer Delete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-05 | Spoofing | origem PostgreSQL | high | mitigate | confirmar identidade do DB e invariantes antes do pg_dump |
| T-29-06 | Tampering | dump/restore | high | mitigate | SHA-256, target limpo, ON_ERROR_STOP e invariantes pos-restore |
| T-29-07 | Information Disclosure | backup/evidencia | high | mitigate | mode restritivo, fora do Git e evidencia sanitizada sem DSN/Secret |
| T-29-08 | Denial of Service | PV local-path | high | mitigate | patch e read-back de Retain antes do restore; proibir delete no rollback |
| T-29-SC | Tampering | imagens Postgres | high | mitigate | usar imagem previamente auditada e identidade imutavel; sem installs |
</threat_model>

<verification>
Executar backup e restore live somente se 29-01 estiver verde. Confirmar origem 8745/PgBouncer 6432, checksum, cpu.max <=800m, ordem dos workloads, readback Retain de todos os PVs existentes e invariantes do banco; verificar que Redis e router ainda nao existem.
</verification>

<success_criteria>
Backup fresco validado e restore completo no PostgreSQL k3s, com PV Retain confirmado, evidencia sem segredos e nenhum router iniciado antes da validacao dos dados.
</success_criteria>

<output>
Create `.planning/phases/29-k3s-shadow-restore-and-go-no-go/29-02-SUMMARY.md` when done
</output>
